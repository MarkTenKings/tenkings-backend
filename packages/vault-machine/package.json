{
  "name": "@tenkings/vault-machine",
  "version": "0.1.0",
  "private": true,
  "description": "One-writer Windows-local authority for Ten Kings Vault V1 (mock/simulator adapters only)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "tsc -p .",
    "test": "npm run build && node --test tests/*.test.js",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "@tenkings/vault-contracts": "workspace:*",
    "better-sqlite3": "^11.10.0"
  },
  "devDependencies": {
    "@types/node": "20.14.12",
    "@types/better-sqlite3": "^7.6.13",
    "typescript": "5.5.4"
  }
}
