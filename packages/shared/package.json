{
  "name": "@tenkings/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "dev": "tsc -w",
    "test": "pnpm run build && node --test tests/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
