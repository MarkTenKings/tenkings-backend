{
  "name": "@tenkings/ai-grader-simulator",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm --filter @tenkings/shared build && tsc -p .",
    "test": "pnpm run build && node --test tests"
  },
  "dependencies": {
    "@tenkings/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
