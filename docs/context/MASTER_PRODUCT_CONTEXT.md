# Ten Kings Master Product Context

Current ATLAS Back-geometry release — September 22, 2026 UTC (September 21 Pacific): source `053768aa4474f950017c43b659648668631f6b9e` is live at [ATLAS staff](https://atlasgrading.com/admin). The reproduced Pokémon Back failure came from grayscale edges selecting interior artwork; the ATLAS color-edge recovery now requires four observed physical sides and then prepares the printed border. Already-valid Front geometry remains unchanged. Eligible saved cards offer **Detect edges automatically**; refresh Magikarp and use that once for its untouched Back. New eligible cards use the correction during automatic initialization. Original photos and prior iPhone upload recovery remain intact. See [Back-geometry evidence](../atlas/audits/2026-09-22/back-geometry-recovery.md).

Matched image4f9e/privateb95a7bf5, staff2qg5/publicGfz8 and controls22/20 are verified. All four public aliases and production target selectGfz8. Exact593 source files and all16 unchanged native artifacts,594 package/82 staff/10 detector tests, both isolated PG42 groups, all14 CI jobs and four actual-photo physical/printed proofs pass. All20 photo derivatives match local proof byte-for-byte, including complete Front parity. Final checks preserve saved Magikarp intake9/manual1/actions0/approvals0, all14 histories and full staff42/public99+13 ledgers. Hosted verification passes10 routes/49 assets/37 markers. No migration, grant, production card recovery, paid inference or approval occurred. Mark's signed-in saved/new-card optical acceptance remains open; branding remains deferred.

## Previous upload-recovery release snapshot — historical

Previous ATLAS upload-recovery release — September 22, 2026, activated at02:42:49UTC (September21 Pacific): source `a00f988568f29cbe01946213005ee50bd5e6b1d9` was live at [ATLAS staff](https://atlasgrading.com/admin). Both reproduced iPhone format failures are corrected: adaptive HDR JPEGs with a verified SDR base and Apple gain map, and qualifying HEIC SDR images with content-light metadata. The complete original remains untouched; working images retain full resolution and verified orientation/color. Independent Front/Back uploads remain enabled. Failed preparation now refreshes saved-side state and provides **Resume photo** against the retained upload, including when no local journal remains. Mark can refresh, sign in if needed and resume the failed sides without re-uploading. See [the upload-recovery audit](../atlas/audits/2026-09-22/upload-recovery.md).

Matched native image9fdbd82d/private146e1d26, READYstaffGwHjr/public2HkL and controls21/19 are verified. All four public aliases and the production target select2HkL. Exact589source/16native identities,582package/82staff native tests, both isolated PostgreSQL42 groups and all14CI jobs passed. All four actual retained originals passed individually and concurrently with interrupted-save recovery and independent pixel checks on that exact native image; peak604.95MiB. Only the addon and build receipt changed among native artifacts;14 dependency artifacts remain unchanged. Final host/DB readback preserves all14 histories, original uploads, reviews/approvals and complete staff42/public98+13 ledgers. No migration, grant, production card retry, paid inference or report approval accompanied this release. Final hosted checks passed10 routes/49assets and33 expected markers at02:45:45UTC; real signed-in upload acceptance and performance remain Mark's next test. Save & Review Geometry, the interactive draft and half-point final grading remain unchanged. Black/gold branding awaits logo assets.

## Previous intake/performance release and acceptance failure — historical

Upload acceptance failure — September 22, 2026, 01:56 UTC: Mark's new mobile pair and desktop Back photo retain verified originals but fail preparation on the current3cfab release. Read-only state and exact-file offline reproduction identify two format gaps: adaptive HDR JPEGs from iPhone Photos are rejected as multiframe, and the desktop HEIC's content-light metadata is rejected despite its SDR primary. The desktop error also occurs on a single-photo retry; a concurrency cause is not established. Compatibility and recovery fixes are in progress. The successful deployment checks below did not establish real new-card upload acceptance. Saved originals, existing reviews and approvals remain preserved.

Previous ATLAS intake/performance release — September 22, 2026, verified at00:56UTC (September21 Pacific): source `3cfab20a3a5b2717168d628ba34533c1f728e5eb` is live at [ATLAS staff](https://atlasgrading.com/admin). One **Save & Review Geometry** action replaces the separate intake buttons; Front and Back uploads can overlap, Geometry/Findings navigation responds immediately, and repeated database/artifact work is removed. The reproduced Sports-card blocker was an invalid automatic Back outline outside the photo. It now leaves that side for manual outlining and preserves valid Front preparation. Original full-resolution photos, native algorithms, scoring and the interactive half-point report remain unchanged. See [the intake/performance audit](../atlas/audits/2026-09-22/intake-performance.md).

Matched image3af817/private6e40, READYstaffFEfpk/publicCy5k, controls20/18 and all four aliases plus production target are verified. Exact582 source files/16 native identities,517 package/76 staff native tests, both isolated PostgreSQL42 groups and all14 CI jobs passed;10 hosted routes/49 assets pass. No migration or grant changed. Final00:55UTC readback preserves all14 histories, staff42/public97+13 ledgers, Charmander revision10/actions9/approvals0/publications2 and Jamal's saved photos/detailsrevision3 with no manual draft yet. Mark can sign in, reopen Jamal and resume its saved request, then use the manual-outline path as needed. Real new-card optical/performance acceptance and final report approval remain open; black/gold branding awaits logo assets. The shared-host hold was released to Inventory after final verification, so later Inventory-only migrations may advance public counts.

## Previous report-review release snapshot — historical

Report-review release — September 21, 2026, verified at 23:21 UTC: the manual workspace is live at [atlasgrading.com/admin](https://atlasgrading.com/admin) on application source `f00f2a18372f68c63dc5bbe8dada2b7c616104d8`, native image `sha256:34d89ed5977ff59dd39fa907b92392502a043b163091a5a3b6af9086998625d2`. One explicit **Confirm findings** now atomically adopts and measures the remaining displayed compatible saved Astra suggestions while preserving individual corrections/rejections and manual findings. The full interactive draft shows verified Front/Back photographs, selectable exact traces, physical measurements, category thresholds/weights and authoritative calculation details before separate human report approval. See [the report-review audit and release evidence](../atlas/audits/2026-09-21/report-review.md).

The owner's September21 final-grade correction is deployed for new V2 reports: round the raw overall directly to the nearest0.5, with exact quarter-point ties upward (9.8→10;9.7→9.5). Preserve raw and one-decimal calculation detail, existing measurement/category arithmetic and shared legacy grading modules. Historical V1 reports retain their original bytes and grading policy. The explicitly hypothetical seven-proposal-plus-manual-mark replay yields eight findings, raw9.825, detail9.8 and awarded10; it remains pending real human confirmation.

The prior [inspection release](../atlas/audits/2026-09-21/inspection-release.md) remains the qualified viewer/photo baseline: real surrounding photo context,16× zoom and drag pan, focused suggestions, image-only3× magnifier, thin overlays and expanded inspection preserving drafts/focus. Future explicitly requested Astra analyses use versioned context-inclusive detail crops; saved legacy analyses retain their exact layout and hashes. This report-review release changes neither photo processing nor those crop contracts.

Staff `dpl_joerdhDUQXN6UZEpy1nUg1MsVCj2` and public `dpl_7WrKqDV7upJPpdGJcfj1i8zXaAFi` are READY for the same source; the private service, signed HTTPS transport, StaffControl19/STAFF SMS17 and all four public aliases plus the production target match. Native source/boundary checks,496 package tests,63 staff tests, both PostgreSQL42 fixture groups and all14 exact-source CI jobs passed. Final readback verified all578 source files and16 native identities;10 hosted routes and48 actual assets passed. No production migration or grant changed; staff42/public97 successful plus13 historical rolled-back records and all14 manual history sets are retained. See the [connected release procedure](../atlas/runbooks/CONNECTED_MANUAL_RELEASE.md).

Final readback preserved Charmander `522610ad-3b06-4657-bf66-90f510fc9435` at revision9, content hash `7a28c4ac08fa5a3fd5173d00a29fb47e0b992a1375244ab2b3b010ce6924019f`, eight actions, zero approvals and one existing publication. The saved three Front/four Back suggestions remain available for Mark's deliberate corrected confirmation; deployment did not adopt them. The existing one-mark all10 report is mathematically consistent:0.105mm² produces0.002093489248% weighted surface damage, below the unchanged≤0.2% score10 threshold. No paid analysis, human sign-in, finding confirmation or report approval accompanied the release. Fresh ordinary sign-in may be needed; human correction/report acceptance, distinct-card learning and optical accuracy remain open. Autonomous operation, completed slab finishing and SAM remain deferred. Earlier dated statuses below are historical.

## Earlier dated checkpoints

Latest ATLAS direction — September 12, 2026 Pacific: Mark explicitly confines this task to ATLAS Grading and prioritizes an Astra API defect-finding experiment with proprietary human-reviewed defect memory before any SAM 3 integration into the clean workflow. Manual photo/geometry/trace/measurement capability is the existing foundation; manual testing should begin before the detector experiment. Bring the minimum reviewed-memory save/publication/retrieval loop forward; variation/sold-comps research can follow the grading experiment. SAM is a possible later tool if measured findings justify it, not a prerequisite or required new baseline. Reviewed examples must reach the next relevant new request after acknowledged publication; they do not retrain Astra's weights. The exact lesson-publication UI event is still a stage-design detail, with the existing Confirm findings action recommended. See the new owner amendment in the approved blueprint and revised rebuild plan. No detector/learning code, paid request, migration or replacement operator is implemented by this planning update.

Owner approval — September 12, 2026: Mark approved retaining each untouched native iPhone HDR original and using a separately derived, full-resolution SDR working image for the connected manual grading workflow. Implement explicit qualified HDR-primary admission and checked P3/sRGB-to-sRGB RGB8 conversion with original-to-working provenance; retain original HDR metadata and bytes. This authorizes the working-view policy and connected staff intake, automatic shared-engine identification, durable manual review/report integration and intended-runtime verification. It does not certify optical detail/color, change grading or human report authority, or close the unresolved initiating-cause prerequisite for a replacement Astra grading operator. Fresh real sports/Pokémon photo acceptance remains a human check after the build.

Current implementation checkpoint — September 12, 2026: the connected manual candidate now wires actual staff pages to native original uploads, qualified full-resolution SDR working images, unchanged shared eight-field identification, durable paired geometry/defect review and separate exact report approval. The dedicated Vercel staff app remains the approved web host; native work uses a signed private Linux service that reauthenticates ordinary staff access. This supersedes earlier local “not connected” and HDR-policy-pending statements as source implementation only. The three SQL proposals and feature activation remain inactive outside fixtures; serving I, fresh physical-card acceptance and the initiating-cause prerequisite are unchanged. See `docs/atlas/audits/2026-09-12/connected-manual-workflow.md` and its concrete release procedure.

> Current I acceptance outcome (2026-09-11, observed through 19:05 UTC): source `89c55d916130` remains deployed, but the owner’s fresh-card test failed. Automatic first-pair enrollment succeeded; the first run stopped in IDENTITY after database timeouts, and the next card is blocked by the one-distinct-card pilot allowance. A settled FAILED run also lacks manual takeover. The cohort is no longer empty. See the [architecture dissection and repair recommendation](../atlas/audits/2026-09-11/README.md). No application or production-state change was made by this audit; old-card recovery remains canceled.

> Subsequent owner planning direction, September 11: preserve grading-quality originals, review engine-generated boundaries/borders together, support many human/Astra cards without application-imposed card/spend allowances, and discuss every stage before implementation. Manual-first rebuilding is under discussion. These requirements supersede the earlier pilot restrictions for the intended rebuilt workflow; live controls are unchanged. See the [canonical clarification](../specs/TEN_KINGS_V2_FINAL_MASTER_BLUEPRINT.md#owner-direction--quality-parallel-grading-and-step-by-step-design-september-11-2026) and [planning record](../atlas/plans/MANUAL_FIRST_GRADING_DISCUSSION.md).

> Latest confirmed choices: native iPhone Camera originals uploaded from the phone; Front left / Back right in one continuing workspace; future direct-camera intake through the same image path. Owner requests clean engine extraction rather than dismantling the old app, and root-cause evidence before a replacement Astra layer. Details remain in discussion; no rebuild or live change has begun.

> Engine-review follow-up: owner permits omitting historical lessons/maps if that materially speeds the clean build, and requested inspection before optimization. Read-only metadata found 559 Memory examples from 48 cards (0.54 MB), plus 4 current maps; these are distinct from SAM model weights and the ability to learn new lessons. See the [engine inspection and proposed measurements](../atlas/audits/2026-09-11/engine-review.md). No reset or engine change has occurred.

> Shared-engine source correction: consultation with `Complete Ten Kings financial app` identifies current collect application `bb10f235d5bddd2ca30ba19e5389e80d43cd2fec`, whose physical-inventory flow uses eight-field Google Vision + Astra identification and a separate Astra/SoldComps research engine. Earlier 17-field CardAsset OCR comparisons concern an older flow. Owner wants the current engines and relevant learned identity knowledge shared with ATLAS, with inventories separate. The [shared-engine plan](../atlas/plans/SHARED_CARD_ENGINES_DISCUSSION.md) records the verified source, remaining reference-library gaps and proposed detector evaluation. No integration or model replacement has begun.

> Planning continuation: Mark requested a compiled end-to-end recommendation. Start with the [ATLAS Speedster rebuild plan](../atlas/plans/ATLAS_SPEEDSTER_REBUILD_PLAN.md), which separates confirmed owner choices, source observations, proposed changes and unresolved acceptance decisions. It includes the server-HEIC gap, selective recalculation, type-correction learning limitation and per-action result validity. This remains planning; no runtime, model or database change has occurred.

> Fresh Astra extra-high follow-up: owner requested a new independent subagent to resolve the remaining timeout investigation. Matched-size/current-guard isolated tests and the real stubbed operator loop passed; native read-only profiles measured full-payload overhead. Mark subsequently signed in to DigitalOcean. The [authenticated follow-up](../atlas/audits/2026-09-11/database-console-followup.md) found aggregate lock delays up to 62.96 seconds and unlimited slow-query parameter logging omitted by the local fixture. Exact historical causality remains unproven; the console showed only recent lines. Bounded logging and representative logging/runtime tests are added to the plan. Mark will relay a prepared inquiry to DigitalOcean's AI assistant and return its response; this task has sent no outside support message. The owner's cause prerequisite remains open. No application code, production data, provider inference or deployment changed.

> Subsequent provider reply: Mark returned the AI answer reporting that old detailed logs are unavailable without prior forwarding. The [verified reply and controlled logging comparison](../atlas/audits/2026-09-11/provider-reply-review.md) supersede the pending-reply status above. Both matched cases pass at the existing 1 s threshold with zero qualifying image statements. A separate forced-logging sample emits 24.4 MB versus 478 bytes, with about 40 ms additional local cost; it is not a ten-second failure reproduction. Current app/admin roles lack SQL privileges to change these logging settings. The next useful evidence is a representative Linux/runtime operation trace, not another identical Mac pass or a new logging-service dependency. No production change or closure of the cause prerequisite.

Fresh lead checkpoint — September 12 UTC: Mark's new lead uses its own handoff-based branch and five fresh `gpt-6-astra`/`xhigh` specialists in batches of at most three children. The [fresh review](../atlas/audits/2026-09-12/fresh-astra-review.md) sharpens native-original descriptors, shared snapping, CPU/manual measurement, exact-mask parity, late-map protection and full human completion after fenced unknown work. Two isolated native ARM Linux PostgreSQL17.11/Node22 tests now cover real periodic-renewal/receipt overlap with corrected production policy timing; both pass and do not identify the initiating historical cause. The task-owned VM/container were removed. Independent read-only metadata verifies collect research source `a319904273d4b4e4e81d721b699d3a2990975eda` deployed READY; its eight-field identifier is unchanged and research engine v2 supersedes the earlier bb10 research extraction reference. ATLAS serving I is unchanged and remains at failed real-card acceptance. No product rule, replacement operator or production setting changed.

Implementation start — September 12 UTC: Mark instructed the lead to work through the remaining list with fresh Astra Extra High specialists and selected **standard-size sports and Pokémon first**. The foundation work begins with isolated CPU measurement and native-photo contracts; the combined workspace is being prepared for review. Existing 63.5 × 88.9 mm geometry/scoring remains the initial scope. This supersedes the earlier “no rebuild has begun” planning status, but does not claim a serving change, complete manual workflow, settled borderless rule or resolution of the initiating operator timeout. See the [canonical owner clarification](../specs/TEN_KINGS_V2_FINAL_MASTER_BLUEPRINT.md#owner-direction--start-the-rebuild-work-and-initial-card-scope-september-11-pacific--september-12-utc).

Layout approval — September 12 UTC: Mark approved the paired Front-left/Back-right combined geometry layout after trying the sample screen. His Front adjustment was experimentation, not a real-card correction. Borderless grading, image detail, defect/learning approval and automatic Astra continuation remain open.

Defect-review decision — September 12 UTC: Mark selected one **Confirm findings** action for the corrected displayed list after inspecting both sides. Final human report approval stays separate. The deliberate learning-publication event and Astra's automatic continuation rules remain under discussion.

Paired workspace and decoder checkpoint — September 12 UTC: the approved geometry layout now has a controlled React implementation, versioned side actions and unchanged shared snapping; JPEG/PNG/WebP decoding runs in a disposable child with exact-source bindings. Root passed 72 new unit tests and seven synthetic browser scenarios; independent review verified corrected readiness/cancellation/metadata edge cases. See the [checkpoint](../atlas/audits/2026-09-12/workspace-and-decoder-implementation.md). HEIC remains explicitly unsupported, storage/preparation/service integration and subsequent grading stages remain unfinished, and no real-card or optical acceptance is claimed. The owned loopback preview was stopped after checks. Serving I and the initiating-cause prerequisite are unchanged.

Subsequent integration checkpoint — September 12 UTC: the [preparation/storage/identification build](../atlas/audits/2026-09-12/preparation-storage-and-identification.md) adds actual CPU preparation/current proposals to the synthetic geometry harness, an immutable S3/Spaces adapter and the unchanged shared eight-field identifier with injected effects. A native HEVC direct/grid subset now decodes in the disposable worker; Exif/ICC/HDR and general iPhone support remain unqualified. Preparation accepts opaque sRGB RGB8 only. Eight browser scenarios pass; storage, identifier and preparation have independent reviews. The owned local preview is stopped. Authenticated durable app/provider wiring, defect/report/finishing stages, real-card detail and the initiating cause remain open. This supersedes the earlier local implementation gaps above; serving I is unchanged.

Latest manual-build checkpoint — September 12 UTC: the [defect/persistence/photo metadata build](../atlas/audits/2026-09-12/defects-persistence-and-iphone-metadata.md) connects paired trace/type/remove/restore review to actual CPU measurement and ordinary staff authentication with compact PostgreSQL drafts, immutable artifact references and separate exact report approval. The authenticated synthetic browser flow passes nine scenarios; 160 local tests, 15 native database assertion groups and independent reviews support the scoped implementation. Pending edits survive measurement failure/reload and can be retried or discarded. Qualified EXIF/ICC HEIC support is implemented; actual retained HDR iPhone originals remain refused, and the SDR/HDR inspection choice is pending. Production app/provider wiring, real-card optical acceptance, detector/Astra/learning/shared-research integration and finishing remain open. This supersedes the earlier local defect/persistence/report gaps; serving I and the initiating-cause prerequisite remain unchanged.

First foundation checkpoint — September 12 UTC: new native-photo contracts, checked CPU manual measurement and a truthful HUMAN draft-report preview are implemented locally, with 59 passing tests and an independent review. The review corrected overly broad inspection invalidation: identity/printed-border edits preserve valid defect inspection, and Front finding changes do not require repeating Back. Four fresh Astra Extra High specialists worked in this batch; the task-tree limit rejected an additional fresh assignment. The [implementation checkpoint](../atlas/audits/2026-09-12/foundation-implementation.md) records exact code/evidence and limits. Server decoding/storage, manual UI/persistence, shared-engine integration and live card acceptance remain future work. The native-x86 harness has local guard/TLS checks but no native integration run; reviewed access still exposes only a shared serving target. Serving I and the cause prerequisite are unchanged.

## Historical runtime checkpoints

The dated H/G/F/E checkpoints below preserve their original observations. Their runtime identities, pending actions and old-card recovery instructions are historical where superseded by I and the owner's cancellation.

Historical H deployment checkpoint before the fresh-start reset — September 11, 2026: source `6cf6e66434c151c76d92590b1262cb1d1e62af83` is deployed to staff, public and private H. All 14 actual main checks passed; production has 95 public/33 staff migrations and all four canonical role checks pass. Independent readback verifies the G→H private route, exact eight-field health and all four public aliases. All 20 final public/asset GET acceptance checks passed. The owner has since canceled Ja continuation and requested clearing every active card to zero for a fresh test; that clearance is pending verification. The prior Ja sign-in/Resume request is canceled.

The completed H adoption readback found the same Ja Morant run control `PAUSED`, with card revision9 and all three applied attempts, receipts and steps preserved. Owner-only deployment maintenance changed only the eight control bindings, this card/run metadata and one immutable NULL-actor audit. It excluded the proven outage from the remaining active-time allowance: 3,483,543ms remain, with deadline `2026-09-11T13:29:05.990Z`, capped by original expiry. No HUMAN OPERATIONS grant, recovery command or model request was created. The active cohort, exact Ja roster, `ACCOUNTING_ONLY`, processing limit1, original pilot/expiry and all financial history remain intact; Jalen is saved and waiting. Both Charmander recovery and Ja continuation are canceled. Clearing the active cohort does not itself admit a new card: the live bridge roster must be rebound to the exact next verified pair before processing; no empty or wildcard roster is supported.

H removes the confirmed blocking Start-refusal loop and admits a valid retained continuation within the existing shared 12MiB request bound. The earlier 4MiB guard explains repeat re-admission failure; the original generic runner stop remains unproven. Start now reflects actual roster/capacity; definite refusals clear, and unknown replies reconcile the same recorded command automatically. The owner canceled continuation of the preserved paused Ja run and requested a fresh test after active-card clearance.

The active cohort remains `0315c8fa-ba4a-41ec-9c8d-d7aac58d5a4a`, with actual Ja/Jalen pairs saved. Release controls are staff14/workspace10/source9/grading6/image5/operator5/identification4 and STAFF SMS12. Original policy/expiry, one-card processing capacity, financial history and canceled Charmander recovery are preserved. The approved blueprint's product scope is unchanged; [historical H evidence](../atlas/WEBSITE_RELEASE.md#historical-h-deployment-and-evidence) establishes deployment, not completion of the physical grading workflow.

The earlier Start, clearance and G/F/E checkpoints below retain their original observations. Their deployment identities, revision counts, pending release tasks and acceptance claims are historical wherever superseded by this H checkpoint.

Historical Start repair checkpoint — September 11, 2026, 11:04 UTC: the owner’s real iPhone capture saved verified Ja Morant and Jalen Brunson pairs in the new cohort. A stale bridge roster still containing retired Charmander caused Start to fail, while the UI incorrectly offered Start and treated that definite refusal as an uncertain saved request. The owner explicitly requested removal of the blocking yellow recovery panel. The exact Ja Morant card `60d05aed-6ccc-46d7-82d8-97128f7e5575` is now admitted; grading control is revision5 with policy hash `4c32decb12aab03854fc6526b3364723a744cf320d6426f11c5216bcea2860c7`. All other controls, cohort, accounting, expiry and processing limit1 remain as at the clearance checkpoint. Jalen Brunson remains saved and waiting.

Automatic pickup started Ja Morant at 10:54:54 UTC. Run `6674413d-de4f-4359-bd43-5c91fa949986` has three saved/applied photo-inspection responses and no unresolved provider attempt at the 11:04 readback, but its RUNNING/CAPTURE_REVIEW state has an expired lease and has not advanced since 10:56:29; that continuation is being investigated. Evidence `g-ja-morant-progress-1789124693013.private.json` SHA256 `57cbc888ce394ea82e6221360043eb25e74a5ecd4c5704d6a3b1f4b265560a65` is retained under the G handoff root. Completed grading and human approval are not claimed.

The source correction removes the yellow panel/manual recovery/alternate-tab loop, reconciles exact retained commands automatically, clears proven pre-dispatch refusals, and gates Start on actual server admission. Staff tests426/426 and the production build pass; additive migration700 and complete compatible H release remain pending. Production still serves G with95 public/31 staff migrations. The earlier fresh-pair/admission-pending and empty-cohort statements below are historical. Charmander recovery remains canceled.

Historical active-test clearance — September 11, 2026, 10:31:04 UTC: Mark canceled Charmander recovery and directed clearing the old test to move forward. The dry-run found only the original Charmander and its photo-identical “Card 1” draft duplicate in the active cohort. A verified configuration-only rotation retired both from the active workspace into retained history. New active cohort `0315c8fa-ba4a-41ec-9c8d-d7aac58d5a4a` has zero cards and one available processing slot. This retirement is reversible; neither card was physically deleted.

Current controls are staff13/workspace9/source8/grading4/image4/operator4/identification3 and STAFF SMS11. Only the workspace/source/identification cohort bindings and their revisions changed. G source `3f341e64531ea231ad262a3816aecefd9c5cbdc7`, runtime bindings, original pilot, September 16 expiry, `ACCOUNTING_ONLY`, processing limit1, all 32 business-row fingerprints, ledgers and costs remain unchanged. No source change, redeploy, HUMAN OPERATIONS grant or new model call occurred.

The owner recovery plan and recovery sign-in request are canceled; its watcher is stopped. Fresh phone Front/Back capture and exact new-card bridge roster admission remain pending until an actual pair exists; complete end-to-end acceptance is not claimed. Old Mac PhotoIntake drafts can persist in staff-ID-scoped IndexedDB without cohort filtering or a Clear drafts button; no local storage was deleted. The 10:16 release/recovery checkpoint below is historical and does not reinstate the canceled request.

Historical G release/recovery checkpoint — September 11, 2026, 10:16 UTC: recovery source `3f341e64531ea231ad262a3816aecefd9c5cbdc7` passed all 14 main checks in run `34580560213` and now serves staff, public and private G. All four public aliases, exact private health and 20 unauthenticated GET checks pass, including 12 mounted staff assets and the admin camera policy. Production has 95 public/31 staff migrations; all four canonical role checks pass. Verified controls are staff13/workspace8/source7/grading4/image4/operator4/identification2 and STAFF SMS11. The explicit `ACCOUNTING_ONLY` bridge policy is active: application processing dollar ceilings are removed, while actual costs, unknown liabilities, original cohort/expiry and nonmonetary limits remain. SMS test limits were already removed in F.

G includes smaller ledger projections, bounded retries of the same captured response after settled database failures, and explicit human review of the original unconfirmed capture request before one new STEP. Original photos, completed steps, receipts and costs are retained; late abandoned responses can add accounting evidence without changing the resumed card. The original Charmander remains card revision9/run UNKNOWN, with attempt3 DISPATCHED and no saved result. Its $23.65 reservation remains held. No new model request or human operations grant was created by this release. Mark completed normal G sign-in: the 10:11:47 UTC census observed one current session meeting recovery’s freshness check and no HUMAN OPERATIONS grant. At 10:16:50 UTC, that count was zero because the session no longer met the five-minute recovery threshold; this does not undo the successful sign-in. A current HUMAN OPERATIONS grant, renewed authentication for recovery, actual one-STEP recovery through human review, and physical iPhone Front/Back acceptance remain pending. The original two saved receipts remain intact; no report approval or publication is claimed.

The superseded F server was stopped at 10:08:24 UTC after public acceptance. Its original helper then failed a stdout assertion; independent readback at 10:09:26 UTC confirmed F exited, current G health, unchanged routing and all 18 other containers. The stop was not replayed. F and earlier C/D/E containers and rollback files are retained. Exact G deployment identities, receipt hashes and acceptance boundaries are in [the historical G release record](../atlas/WEBSITE_RELEASE.md#historical-g-deployment-and-evidence). Earlier F/C/D/E bindings, spending ceilings and acceptance checkpoints below are historical where superseded by this G checkpoint.

Historical F release — September 11, 2026, 07:02 UTC: merged rapid-intake source `62bdf64f80f7941a122ccb4b96f74fc5f2727a69` passed all 14 main checks and is deployed to staff, public and private F. All four public aliases, exact private health and 20 unauthenticated HTTP requests pass, including 12 mounted staff assets and the admin-only camera policy. Production has 95 public and 29 staff migrations, exactly two new verified scoped grants, and verified staff12/workspace7/source6/grading3/image3/operator3/STAFF SMS10 plus identification1. Customer/legacy bindings and all 17 prior containers were preserved during cutover. A separately verified post-cutover capacity repair stopped only the three obsolete C/D/E servers, retaining their containers and rollback artifacts; current F, preparation, shared services and routes remain unchanged. Exact deployment identities and receipts are in [the current release record](../atlas/WEBSITE_RELEASE.md).

F implements rapid sequential Front/Back phone capture, concurrent photo preparation/uploads, separate OCR/Astra identification images, automatic verified-pair queueing and capacity-gated Astra pickup, with recoverable photos and requests. Mark's removal of staff/customer SMS test count, cost and window enforcement is applied; ordinary phone verification, admission, rate limits, sessions and CSRF remain. The $90 grading/model/worker/infrastructure budget, September 16 at 20:41:57 UTC pilot expiry, one-distinct-card processing cap, exact original-card roster and retained liabilities remain unchanged.

Live acceptance update — September 11, 07:28 UTC: Mark completed normal SMS sign-in, independently observed in the staff workspace. The original Charmander saved response was recovered and its image-region inspection applied at 07:06:22 before the next model request. That next request is still DISPATCHED without a receipt, and the run is UNKNOWN at card revision9 with ASTRA_RUNNER_FAILED. Stopping the obsolete servers freed seven database connections. The missing third result remains unresolved, with its $23.65 reservation retained and total conservative pilot exposure $33.9257. Deployed F has no recovery path for this capture attempt without an actual saved response. Human-review handoff and physical iPhone Front/Back acceptance remain pending. No human approval or publication is claimed. Older C/D/E deployment counts, requirements to collect all ten pairs before processing the first card, mandatory manual queue/Start Astra steps and SMS test limits below are historical where superseded by this checkpoint and the approved blueprint. Preserve their original receipts; do not replay completed release actions.



Owner update — September 11 Pacific: Mark has now removed ATLAS processing dollar ceilings as well as the already-removed SMS test limits. The approved blueprint records the correction. The explicit ACCOUNTING_ONLY policy and capture-request recovery extension are deployed in G. Original-card live recovery remains pending as recorded in the G checkpoint above.

last_verified_at: 2026-09-11
verified_by: Codex from current code, Production runtime/API/database evidence, the owner-approved atomic FAMILY + EXACT Card Map correction, and the 2026-08-18 Color Geometry release-skew incident evidence, in addition to the retained protected-main and Mathematical Calibration V1 evidence below
v2_blueprint_approved_at: 2026-08-06
v2_blueprint_approved_by: Mark Thomas
repo_root_workstation: C:\TenKings\repos\tenkings-rip-it-live
repo_root_droplet: /root/tenkings-backend
default_branch: main
active_feature_branch_example: chore/seed-timeout-hardening
prod_site: https://collect.tenkings.co

## Purpose
This file is stable product/system context.
It is not the source of truth for fast-changing commands or incident state.

## Historical ATLAS implementation and live website checkpoint — September 10, 2026

ATLAS has implemented components for durable phone access/assignments, supervised initialization and bounded Astra review, human report/proposal/identity corrections, exact public approvals, scoped label/NFC/physical finishing, operational recovery and separate trusted-learning decisions pending bank application. The independent grading lead works in `d221`, branch `codex/atlas-grading-workflow-lead-20260909`; outgoing handoff `aae1fd7a682a0b1f3bb6428e3b5dadab7793bf97` and the `b487`, `f4fe` and `9545` tasks are historical checkpoints. Merged C `89478881` implements fresh-photo intake, saved pairing, a waiting queue, exclusive claims, manual original-source preparation/map/report actions, original-photo Astra tools, recorded activity and pause/STEP/takeover. Its private service and finite dispatcher preserve exact saved-command recovery and transactional generation checks;193 complete disposable PostgreSQL scenarios pass. C's private host is accepted and running, and staff/public C and intake are live. Intake controls were verified by read-only reconciliation without SQL replay; all four public aliases and the production target are verified on C. The Mac is unlocked and the authenticated C staff queue/Add Cards page opens; real-card operation remains unproven. Mark's ten-card pilot uses brand-new Front/Back photographs for every card, superseding saved captures; all ten may be queued through the live intake while initial processing remains limited to one distinct card. See [the grading workspace evidence and acceptance](../atlas/GRADING_WORKSPACE.md).

The original core checkpoint passed128 PostgreSQL scenarios,55 paired-web/restart checks and70 built-production denials. The historical September9 website/database checkpoint passed155 PostgreSQL scenarios on93 public plus16 additive ATLAS migrations and no-op replay, including customer ownership, current approval/finishing/shipment and SMS-budget regressions. Three Next15.5.25 applications build; the connected synthetic website passed13 HTTP test groups including process restart. All39 configured-disabled hosted checks passed before login activation. Original committed migration bytes and grading arithmetic are retained. Detailed evidence and remaining inputs are in [ATLAS completion](../atlas/COMPLETION.md), [local acceptance](../atlas/LOCAL_ACCEPTANCE.md) and the [website release](../atlas/WEBSITE_RELEASE.md).

Mark selected his **MacBook for both ATLAS grading and physical NFC encoding** on September 8, using the same ACS ACR1552U reader and FEIJU F8215 tags. The target removes per-card GoToTags/account/app switching: an armed ATLAS session automatically writes, verifies and locks the approved report URL when a fresh tag is placed. Astra may coordinate approved work; deterministic code controls device operations. Human report approval, physical card association, placement/removal and assembly/welding remain explicit. Native discovery, reading and removal/re-presentation checks now pass on his Mac after ACS driver/polling setup. A separate controlled test-URI write passed with full readback in 0.38 seconds on his designated tag, and Mark confirmed phone reading on September 9. The full Mac finishing writer, lock qualification and integrated automatic queue remain incomplete. See [Mac NFC rework](../atlas/MAC_NFC.md).

CPU A2 is admitted and running unchanged with verified HTTPS. All seven additive migrations and three scoped SOURCE/COORDINATOR/OPERATOR logins are installed and verified:95 public/22 staff migrations and zero unfinished migrations. Mark funded RunPod and its reviewed scaling settings are saved; storage and dedicated OpenAI access are verified. Merged C `89478881`, tree `b9772207`, passed all14 PR/main checks and39 private tests. Staff/public C are READY with26/6 environment bindings accepted. Signed private C started at13:25:20UTC, passed health and15/15 HTTPS/denial checks, and its route preserved Caddy/CPU. B's earlier startup failure is historical; its stopped container is retained under its archival name. All four public aliases and the production target are verified on C; customer and legacy bindings are preserved. Public/admin/account HTTPS checks and the www redirect pass. No paid Astra/worker real-card evaluation, graded-card NFC finishing, real print, assembly or weld acceptance has run. See [current deployment evidence](../atlas/WEBSITE_RELEASE.md); older fixture counts below remain historical checkpoints.

`https://atlasgrading.com/admin` and `/account` are deployed with distinct sessions/CSRF, additive database policy and signed forwarding. Public/staff C preserve the original gold/black homepage and accept ordinary ten-digit U.S. input, adding+1 through the provider handoff. StaffControl7/workspace1/source1/STAFF SMS5 are committed and verified, with intake enabled; CustomerControl6 and customer SMS4 remain unchanged. All four public aliases serve C. The Mac is unlocked and the authenticated C staff portal opens; complete real-photo workflow acceptance remains pending. DNS, managed TLS,22 staff migrations and separate serving credentials are configured. Original Site version21 is preserved. Exact bindings and current live verification are in [website release status](../atlas/WEBSITE_RELEASE.md). Claims, preparation, Astra dispatch and production NFC remain inactive. Ten real verified Front/Back pairs and a fresh cost/hold check precede the first processed card; the owner's reviewer certification remains null.

Customer phone-code signup/sign-in, deferred profiles, immutable submission snapshots, dealer drop-off/mail-in and per-card tracking are deployed. Mark confirmed a successful replacement SMS and sign-in after the first code expired. At21:39:09UTC the database confirms one account/session, the consumed replacement and two customer SMS reservations totaling$1; the browser shows the authenticated submission form. At22:02:07UTC the staff test also shows one identity/session and two reservations totaling$1; its empty Review queue is visible. Mark supplied the approved phone/provider access and$100 total SMS/image/Astra cap; the initial owner-only SMS partition is at most$10 and expires September16 at20:41:57UTC. No renewed general authorization is needed. Current release revisions require fresh browser sign-in. Mark prioritizes admin grading/Astra testing before customer-submission iteration. Staff operations, the live ten-card workflow and broader customer admission remain separate work. [Customer access](../atlas/CUSTOMER_ACCESS.md) records the boundaries.

## Source-of-Truth Policy
When there is a conflict, use this order:
1. Runtime evidence (live API behavior, running deploy identity)
2. DB query evidence
3. Current code in checked-out commit
4. Docs

If docs conflict with evidence, update docs in the same session.

## Approved Ten Kings V2 Planning Authority

- Canonical document: `docs/specs/TEN_KINGS_V2_FINAL_MASTER_BLUEPRINT.md`
- Mark approved this final product and architecture blueprint on 2026-08-06.
- Every Codex agent must read it in full at session start as required by `AGENTS.md`.
- It governs future V2 product and architecture decisions, including Speedster's permanent-card foundation and the later inventory, packs, DIRECT, ownership, buyback, shipping, and V1-freeze phases.
- Speedster remains the immediate execution priority, and the blueprint's phase and owner-approval gates remain binding.
- This blueprint describes approved future intent. Code, runtime, and database evidence remain authoritative for which portions are currently implemented or deployed.
- Do not duplicate the full blueprint into context or handoff files; link to the canonical document so there is only one editable copy.

## Product Summary

- Color Geometry is forward-only on `speedster-color-geometry-v2`. Retired Color Geometry engine artifacts are historical audit evidence only: they cannot be parsed, signed, selected, deployed, or used as a runtime rollback. Any incompatible web/worker cutover requires a no-traffic maintenance gate and roll-forward repair to the approved current engine.
Ten Kings is a hybrid physical-digital collectibles platform with:
- Mystery packs and card-level QR identity (`tkp_...`, `tkc_...`)
- Web + kiosk live rip workflows
- Inventory, vault, shipping, and buyback/TKD loops
- Admin ops for packing, staging, kiosk sessions, and label resets

## Core Platform Architecture
Frontend/API:
- Next.js app in `frontend/nextjs-app`
- Next.js API routes for admin, kiosk, variants, refs, packing flows

Data:
- PostgreSQL (DigitalOcean)
- Prisma ORM and migrations (`packages/database/prisma`)

Infra:
- Vercel serves public/product frontend
- DigitalOcean droplet runs backend build/runtime stack
- SER mini-PC runs kiosk display and OBS integration

Media/Live:
- OBS + obs-websocket on SER
- Mux stream per location (`muxStreamId`, `muxStreamKey`, `muxPlaybackId`)
- Kiosk helper drives stage/OBS behavior

## AI Grader Production Architecture (Current Reality)

### Speedster Card Map creation and lookup

- One completed Card Map authoring operation creates two complete immutable revisions atomically: a FAMILY map for the category-aware family key and an EXACT map for the source card. The operator is never asked to choose FAMILY versus EXACT during creation. Either both revisions and current pointers commit after independent persisted-content hash verification, or neither commits.
- Pokémon FAMILY key v2 is Category + human-entered Layout Type (`POKEMON`, `TRAINER`, or `ENERGY`) + Year + Product/Set + Parallel. Sports FAMILY identity remains Category + Year + Manufacturer + Product/Set + Insert + Parallel. Card/Player Name and Card Number remain exact-source identity and provenance only. EXACT key serialization is frozen and unchanged.
- New Pokémon sessions require the human-authoritative layout type in identity. Historical sessions are never rewritten: a historical identity without layout type receives EXACT lookup only until an admin makes one irreversible layout selection while re-saving its Card Map. That selection is stored in a separate append-only, one-row-per-source authority record; it cannot be edited or deleted. Historical Pokémon FAMILY keys remain readable audit history but are runtime-ineligible, cannot filter findings, and cannot be restored or promoted into authority.
- Both revisions begin with the same reviewed Front/Back boundary, anchors, and zones. Each retains the source imagery, evidence hashes, identity, and provenance. At runtime EXACT replaces FAMILY completely; otherwise FAMILY applies to matching siblings; if neither is usable, normal human review continues. Maps never merge and an invalid EXACT map never causes a guess or lower-scope fallthrough.
- Revision hashes are server-owned over one deterministic normalized persisted-content payload. New revision rows are read back and verified inside the same transaction before current pointers or captured-source bindings move. Existing revision history is append-only; restore creates a new immutable revision.
- Draft Export/Import is the durable operator recovery path. A failed save keeps all local geometry available for Retry and Export and must not refresh, navigate, or reset the editor.
- Historical Production evidence includes valid append-only revisions authored from the recovered 2023 Pokémon MEW EN Reverse Holo Squirtle draft: FAMILY v5 `cmspjh5h50003lddtgtdl9o5q` and EXACT v1 `cmspjh5sc0005lddthf23tvt6`. The invalid earlier FAMILY v1-v4 rows remain immutable evidence. The numeric persistence fix is deployed in merge `a1813ea282c1e9e539e3b36b02d9716c975d0c24` and normalizes new immutable payload numbers before both hashing and JSON persistence without changing legacy revision hash verification.
- Current 2026-08-15 Phase 0 evidence supersedes that old revision count as the rescue baseline: Mark's Aug 13 atomic dual save created FAMILY r9 `cmssbc8fi0002jfswkc7ra2rl` and paired EXACT r5 `cmssbc8hf0004jfswua36k5bf` from the same reviewed Front/Back map bodies. FAMILY r8 remains intact as immutable history. Mark confirmed r9 is the intended rescue target: Front handles at the four corners of the printed design area; Back follows the prior Back reference. The completed Charmander card `TKH-001259` pinned r9. Exact-first editing may display the paired EXACT r5 baseline; its map geometry is content-equivalent to r9 even though revision/key metadata and therefore hashes are not equal.
- Layout Key V2 is still an undeployed candidate and must not be described as current Production behavior. Its dated authority/supersession note is `docs/context/SPEEDSTER_LAYOUT_KEY_V2_ADDENDUM_2026-08-15.md`.
- The versioned v2 content/filter contract separates descriptive content type from explicit filter authority, defaults text/logo/border On and artwork/foil/other Off, remains adjustable per zone, and applies deterministic `0.6 mm` physical-card dilation under strict full-contour containment. Historical v1 maps and decisions remain byte-compatible and restorable. The frozen 50-card audit remains `INCONCLUSIVE` (corpus SHA `255e3b81…`, report SHA `71806fab…`): it has 2,292 human outcomes but no compatible 2023 MEW EN Reverse Holo card, no compatible map registration/revision binding, and no exact evidence hashes. On 2026-08-12 Mark explicitly waived that replay gate for Production activation, accepting sole-grader review and the removed-findings audit as the safety net. The recorded authority is `OWNER_WAIVER_2026_08_12`; verification remains `PENDING` and must never be represented as a replay pass. Activation creates new immutable FAMILY and EXACT v2 revisions atomically and does not reinterpret or rewrite v1 history.

- The current Dell workflow is one operator sequence: **Start New Card**, **Capture Front**, flip and **Capture Back**, then one **Approve & Publish** authority.
- `/ai-grader/station` uses Basler/Leimac through the loopback-only token-gated bridge, exact frame identity, direct browser-to-storage asset upload, and atomic durable publication.
- Mathematical Calibration V1 is an explicit, fail-closed machine-grading contract. Normal mathematical acceptance remains controlled by the centralized formulas and thresholds: an ordinary rejected or incomplete run cannot produce or use a finalized/trusted bundle. A normal accepted profile has `status=finalized`, `isCalibrated=true`, and the exact 12-member V1.2 bundle. It never falls back automatically to V0, a manual score, another calibration, another camera, or another image. An authenticated admin may separately adjudicate a persisted failed report by explicitly confirming all four scores. The approved values become the report's sole public grade/reasoning values without reviewer or machine-comparison annotation; the immutable source and adjudication history remain internal and never change calibration authority.
- One exact incident-bound session, `math-cal-v1-20260722-4cfa410c-01`, has a separately versioned product-owner operational policy path. Its mathematical profile remains `status=rejected` and `isCalibrated=false`; all measurements and the complete 36-issue rejection ledger remain unchanged in internal authority and audit records. Its transparent 13-member bundle is operationally eligible only after exact loader and registry verification plus a fresh-human-admin ECDSA-signed `ACTIVE` calibration activation authority over the exact bundle manifest, member ledger, runtime context, rig, and operating context. The owner record is content-addressed decision metadata; the signed activation authenticates and authorizes its Production use. No browser boolean, threshold-pass claim, fallback, cross-session reuse, newest/closest/LKG selection, or automatic rollback is allowed. Public reports do not disclose the owner-acceptance status, mathematical rejection state, exception ledger, decision metadata, or activation signature/bundle provenance. Revocation or supersession remains explicit and append-only. This policy does not modify normal V1.2 12-member mathematical acceptance or authorize any other rejected calibration.
- A V1 session binds the exact card identity and a centering profile before capture. `printed_border_v1` measures a robustly fitted printed frame. `registered_design_template_v1` requires the exact approved, identity/version/hash-bound Ten Kings reference; arbitrary web imagery and normalized-frame symmetry are not reference authority.
- V1 finding review is disposition-only and bound to the exact review-request SHA-256. The operator sees hash-verified True View, all eight directional channels, ROI, segmentation, confidence, and illumination evidence and records `confirmed` or `adjusted`; the operator cannot author measurement confidence. This is evidence review inside report preparation, not a second publication confirmation. **Approve & Publish** remains the sole publication authority.
- Part 2 is `/ai-grader/finish` on a normal authenticated computer: comps review/selection, slab-photo direct upload, label/inventory gates, and inventory completion. It has no local bridge, station token, camera, lighting, or hardware controls.
- `/ai-grader/labels/sheets` manages the unchanged 16-slot Label V1 physical sheets. Publication must preserve exact report/card/label/inventory linkage.
- Public report routes are unauthenticated read-only views and must never expose Dell paths, bridge URLs/tokens, presigned URLs, embedded image bodies, credentials, or hardware controls.
- Large images use direct browser-to-storage PUTs; browser-to-Vercel request and response bodies remain small manifests/metadata. OCR, publication-asset, and slab-photo finalization require the exact planned object identity, byte size, content type, and SHA-256 before use: accept a valid provider-native checksum when present, otherwise let the server pull and hash the same stored object through one bounded stream. ETag, mutable metadata, filename, caller URL, and unbounded reads are never integrity authority, and downloaded verification bytes are never written to disk.
- Raw forensic capture evidence remains local and immutable. Derived normalized/crop/deskew assets supplement rather than replace the raw front/back evidence.
- Mathematical V1 scoring policy, formulas, uncertainty semantics, evidence contracts, and honest limitations are specified in `docs/specs/AI_GRADER_MATHEMATICAL_CALIBRATION_V1.md`. The executable threshold manifest in `packages/shared/src/aiGraderMathematicalCalibrationV1.ts` is the only coefficient, tolerance, and cap authority.
- The production station has one capture profile and one route: `production_fast`. Front and Back raw evidence is lossless TIFF; the exact normalized grading/OCR evidence remains PNG. No Single selector, full-forensic production route, contract preview, alternate capture implementation, or automatic fallback is operator-reachable.
- Successful Back is one atomic handoff: the bridge persists the immutable Back TIFF and hashes, accepts the exact Front and Back side-processing jobs, atomically persists the exact Rapid queue item, and only then releases capture/camera/session ownership to the sessionless `start_new_card` state. The bridge does not create the next session; **Start New Card** is the one explicit next-card action.
- Unpublished cards remain in the existing persisted local **Finish / Review Queue**. The fixed-rig worker runs the complete captured-side pipeline inside its existing one-at-a-time killable worker; one bounded timeout terminates a hung side attempt and permits later pending jobs to advance after termination. The helper's serialized finalization conveyor prepares exact-card grade, report, release, and Label V1 data without a second worker or queue.
- Separately, the station browser orchestrates the existing hosted Google Vision OCR lifecycle after both exact normalized PNGs are verified. Fresh hosted Production authorization must succeed before the helper consumes the one attempt. The durable claim binds the exact queue/session/report identity and one mounted-station-page `attemptOwnerId`; an origin-scoped exclusive Web Lock holds that owner while the page is mounted. Reload, back-forward, or a same-tab station remount reclaims an available persisted owner, while a duplicated live tab must atomically claim a fresh owner. A page observing a foreign in-flight owner queues one exclusive lock waiter: it cannot mutate while the live owner holds the lock, and after release it refreshes the exact queue/session/report/owner state under the acquired lock and terminal-fails only that still-in-flight item. Cleanup aborts and releases the waiter. Only the exact owner can complete or fail its attempt. If Web Locks or owner persistence are unavailable, OCR remains unclaimed with one explicit initialization error. OCR persists one safe result or terminal failure, never retries automatically, and never confirms, publishes, or mutates inventory.
- The persisted local queue authority is `ten-kings-ai-grader-rapid-capture-queue-v2`. An absent queue or empty recognized v1 queue initializes safely as empty v2. A nonempty v1 queue lacks exact accepted side-job identities, so the helper stops before parsing or rewriting it. Before any Dell helper update/restart, a separately authorized read-only preflight must verify the configured queue file is absent or recognized and empty. The only nonempty exception is explicitly authorized parser-maintenance for a recognized v2 queue containing exclusively terminal failed items: record every exact identity and complete item payload first, require regression-proven compatibility in the target helper, and require every item payload to remain unchanged after restart (the queue-level `updatedAt` may refresh). Any unfinished, malformed, unrecognized, nonempty v1, or item-drifted evidence stops rollout and must be reported without migration, recovery, retry, or mutation.
- The hosted Finish queue remains post-publication only. **Approve & Publish** requires the activated exact local queue/session/report identity and cannot authorize another capture or publish another card. The bridge's active-review report bundle embeds that item's exact finalized `productionRelease` before the hosted publish call.

## AI Grader NFC identity architecture (deployed; required policy temporarily disabled)

- The additive NFC schema, hosted routes, attempt-token secret, one-key workstation allowlist, and programming policy are deployed in Production. `AI_GRADER_NFC_PROGRAMMING_ENABLED=true`, while `AI_GRADER_NFC_REQUIRED=false` is the current safe state after the 2026-07-18 browser-pairing bootstrap incident. Do not require NFC again until the reviewed browser/launcher correction and the separately controlled installed-workstation recovery are accepted.
- `static_url_v1` associates one server-generated `https://collect.tenkings.co/nfc/{publicTagId}` with the exact durable Published report, Confirm-authority CardAsset/Item, certificate, and AiGraderLabel. NTAG215 and FEIJU F8215 are registered convenience links, never cryptographic authentication. F8215 permanent consumer write protection does not make its static URL unclonable.
- Hosted status/init/complete require a human AI Grader operator or admin; revoke/replace require a human admin. Service identities and browser role claims cannot authorize programming. Report-lifecycle locks, independently challenged one-time attempts, immutable audit events, and revoke-before-replace remain server enforced.
- Activation requires a current allowlisted finishing-workstation ECDSA P-256 operational attestation over the exact attempt/linkage/readback statement. Legacy NTAG215 keeps its exact v1 direct-PC/SC attestation. F8215 uses additive v2 evidence for the exact profile, GoToTags version, full URL readback, and verified permanent read-only state. Raw tag UID is never accepted; only its SHA-256 fingerprint is persisted after verified readback.
- `/nfc/[publicTagId]` is production-DB-only and resolves only an active, exactly linked, still-public Published report. `/ai-grader/nfc` owns local programming. Finish only shows safe state and opens that route; it has no helper/hardware control.
- The separate .NET 8 Windows helper is loopback/production-Origin/token restricted and owns fixed ACR1552U PC/SC NTAG215 operations. It also contains the asynchronous F8215 adapter around exact GoToTags Desktop `4.37.0.1`: Ten Kings prepares one protected operation, the operator clicks GoToTags **Start Encoding** once, and the helper accepts only a terminal exact-URL/readback/lock result. It signs only complete reviewed evidence with the named current-user, non-exportable Windows CNG P-256 key. The helper is independent of the capture helper and never imports native NFC or GoToTags dependencies into Next/Vercel.
- `AI_GRADER_NFC_PROGRAMMING_ENABLED` and `AI_GRADER_NFC_REQUIRED` are the only NFC feature controls. Disabled programming still permits authenticated status and admin revocation but blocks init/complete/replace before helper contact. NTAG215 and F8215 remain explicit profiles; neither can fall back to the other. When NFC required is false, QR and current inventory behavior are unchanged; when true, Add To Inventory requires exact active NFC inside the locked server transaction.
- GoToTags does not provide a reviewed electronic pre-write blank-state proof. The initial F8215 release instead uses the product-owner-approved audited operational control `operator_fresh_inventory_confirmation_v1`: the operator takes exactly one unused tag from controlled inventory, keeps it off-reader until requested, and quarantines every failed, interrupted, uncertain, previously presented, written, or locked tag. Never describe this control as electronically verified blankness. The terminal callback proves only the final exact URL, readback, and permanent write-protection result.
- The preserved terminal F8215 job remains an exact-attempt physical quarantine. Rapid/OCR source work does not acknowledge, delete, retry, resolve, or otherwise operate that job; its public link remains inactive and the NFC helper remains occupied until the separately audited completed-but-unactivated resolver is authorized.
- Authenticated NFC readiness includes only the redacted `nfcSchemaReady` boolean plus programming-policy/configuration booleans/count. An unapplied additive migration produces controlled unavailable behavior, including a no-store/noindex public HTTP 503; unexpected database failures remain distinct. With NFC-required false, unrelated Finish/report/Publish/inventory workflows do not query the NFC tables. With it true, missing or unverifiable NFC schema blocks inventory.
- The dedicated NFC helper has separate first-install and ordinary-update workflows. Ordinary update stages and verifies the replacement before stopping only the NFC helper, preserves protected token/pairing/CNG/task/shortcut identity, and restores the prior working install on failure. Intentional token/pairing rotation is an explicit maintenance action, never an update side effect.
- The future `NTAG424_DNA` / `ntag424_sun_v1` seam is type-only and unimplemented. No keys, SUN validation, counters, originality proof, or cryptographic success claim exists.
- Operational detail, official hardware references, installation, rollout order, and the exact hardware-approval gate are in `docs/ai-grader-nfc-helper.md`.

## Kiosk Lifecycle (Phase 1)
Stages:
- COUNTDOWN
- LIVE
- REVEAL
- COMPLETE/CANCELLED

Behavior:
- Pack QR starts session
- Countdown auto-advances to LIVE
- Card QR triggers reveal
- Reveal timer auto-completes and returns to attract loop
- LocalStorage/session recovery is supported

## Admin Operations
- Packing stage transitions including `LOADED`
- Active kiosk sessions control
- Pack label reset flow with reset-version semantics
- Variant ref QA tooling for set/card/parallel-level image validation

## Set Ops and Variant Ref System (Current Reality)
- Seeding builds variant/reference data from checklist/player-map sources
- Legacy `ALL` and `NULL` card-number behaviors require careful handling
- Dirty labels can come from encoded source data and JSON-like parallel payloads
- Player-level association for refs is critical for downstream matching quality

## Current Priorities
- Keep the station workflow at four actions: Start New Card, Capture Front, Capture Back, Approve & Publish. New grading sessions require the explicit `mathematical_calibration_v1` contract, exact calibration readiness, and exact card/centering authority; Legacy V0 and an omitted contract cannot start. After those gates pass, Start New Card creates one `production_fast` session and applies the configured production positioning light through the bounded bridge path; complete exact controller acknowledgements are required before capture readiness. Durable Back enqueue returns to a sessionless state and never creates a continuation session.
- Keep one Basler/Pylon capture implementation, one bridge-owned watchdog, one capture lock, and one serialized physical operation. The camera waits only for capture/lighting/terminal ownership: prior PNG conversion, OCR, grading, reporting, review, publication, Finish work, or another item's terminal failure never disables Start New Card. The existing Rapid worker remains one-at-a-time and owns the full killable side pipeline, while its pending representation accepts the twenty side jobs needed for ten captured cards and advances after an exact-item failure or bounded worker timeout.
- Keep the local Finish / Review queue as the only pre-publication queue. It durably refreshes exact identities and processing, review-ready, or failed state. A ready item's persisted OCR suggestions hydrate only untouched fields so normal human correction remains required. No missing OCR field, unpublished item, or unfinished hosted Finish task blocks capture.
- **Add To Inventory** remains the downstream Finish action after applicable NFC, slab-photo, valuation, and label requirements. It is outside the four grading actions and is not a safety mechanism or fallback.
- `Ready` means a fresh, confident, fully visible card-shaped region can be normalized safely. Camera-frame centering and in-plane rotation inside the `35°` normalization envelope are not direct capture blockers; the `10°` target remains a visual placement guide, while full visibility and the fixed-rig `30%-85%` expected-size envelope constrain extreme placement and include the existing 97%-height production guide. `Adjust Card` carries a safe actionable reason for clipped, low-confidence, wrong-aspect, unsafe-size/coverage, or beyond-envelope geometry. The dominant live overlay follows the estimated outer corners. Geometry cannot infer printed top versus bottom, so the operator keeps the printed top generally toward the preview top. Failed/low-confidence/stale detection remains `Not Detected` or `Adjust Card` and never reuses stale geometry.
- Grade ROIs use the fixed `1200x1680` normalized portrait coordinate frame derived from the authoritative full-resolution all-on geometry and reused across accepted-profile and channels `1-8`. Full-resolution processing requires at least a `1000x1400` source crop and no more than `1.2x` upscaling. Camera-frame placement is acquisition and evidence-quality data, never printed-design centering. Historical V0.2 retains its explicit missing-centering redistribution and cap for readability; a new V1 report instead requires measured front/back centering, corners, edges, and surface and applies the fixed four-element weights with no missing-element redistribution. Every original raw role remains immutable and hash-verified.
- Report Edit Mode is a separate authenticated admin authority, never a machine fallback. It requires all four sub-grades, calculates the effective overall only with the centralized V1 weights/caps, stores a source-bundle-bound human adjudication plus reasoned audit chain, and keeps the original machine evidence/result immutable. A saved adjudication may complete an explicitly failed report at the same report URL/NFC identity. `coming_soon` is the report's single persisted presentation state; it exposes only the wall DTO and does not create a second report or NFC identity.
- Capture quality and card condition are separate in V1. Focus, clipping, exposure, darkness, common-mode/specular glare, and directional coverage select or exclude evidence, lower confidence, or require recapture; they never deduct physical-condition points or add sharpness points. Surface candidates are measured only on valid pixels after flat-field correction and common-mode removal. Calibrated illumination-pattern or glare overlap is shown as a limitation mask with zero card deduction, while supported directional defects remain measurable from alternate channels. Fully obscured evidence is insufficient, never a false 10.
- Approximate Leimac direction vectors are transformed by the same authoritative deskew into normalized card coordinates. Missing or incoherent transform provenance suppresses directional normal/relief output; raw dark controls are not mixed with normalized pixels without registered coordinate and dimension parity.
- Public defect findings produced by the normalized path use canonical `1200x1680` card pixels as their only projection source, convert once to bounded normalized fractions, and bind to the exact raw-source and normalized-artifact hashes. They must not be inverse-rotated through raw/display coordinates a second time. Legacy source-display projection is retained only for older non-normalized evidence; findings remain side-scoped and absent when candidates or coherent geometry are absent.
- Failed or stale automatic geometry remains unavailable and no fixture rectangle or invented coordinates may substitute for detection. A visibly rendered, exact-source image may enter a separate explicit four-corner human-authoring state; those deliberate human points are authoritative and must never be represented as automatic geometry.
- Do not claim five seconds per side from software/mock timing. The last supervised Dell control measured about `9442 ms` front and `9243 ms` back; image writes were the largest measured stage. A new hardware comparison requires Mark's explicit approval.
- Stabilize prod `/admin/variant-ref-qa` behavior and counts
- Ensure set/parallel label normalization is correct
- Ensure player-linked reference metadata is persisted and exposed
- Productize manual QA/approval workflows for imported datasets
- Add production UI support for safe set delete/archive operations
