# Staff intake save feedback and GPS acquisition

Mark and office staff have now confirmed the speed and accuracy of physical-phone photo capture and OCR/Astra identification. They report that allowing location does not select a site and that the final Add inventory button appears inert. Three existing Astra Max agents investigate location matching, native browser save behavior, and safe ATLAS reuse while the coordinator owns workspace integration and browser position acquisition.

Chrome and WebKit native button clicks reproduce two save failures: native constraint validation prevents the React submit handler for missing location/type or a date inside closed More details, and application/server errors plus retry controls render above the current scroll position. The fix uses an explicit validation pass, accessible field errors, disclosure/focus of the first invalid field, and feedback/retry actions in the sticky save footer. Existing server validation, exact request persistence, idempotent retry and confirmed-save-only next-card camera behavior remain unchanged.

Browser geolocation will take bounded high-accuracy updates rather than accept only one potentially coarse cached reading. Stop observation on a useful reading, deadline, denial, cancellation or manual selection. Keep phone coordinates in browser memory; show acquisition/accuracy and matching status without sending phone coordinates to Google or the database. The matching/cache work is specified in the companion staff-intake-location-recovery plan. All automatic suggestions remain editable; nearby uncertainty and unavailable site coordinates must be disclosed.

Validation will use native submit-button clicks with missing location/type, hidden empty/future dates, malformed prices, rejected saves and unconfirmed acknowledgments; browser proof includes narrow phone screens and reduced keyboard viewport height. Focused tests cover position refinement, cancellation and deadline cleanup, existing photo/identity/manual-edit boundaries, point cache/matching, and exact save retry. Run scoped lint and the normal production build with database migrations disabled. Record review and release evidence in the session log, retain the current isolated collect lineage, and perform no synthetic production inventory writes. ATLAS changes await the requested intake-surface clarification; its grading evidence must retain original resolution and separate grading authority.
