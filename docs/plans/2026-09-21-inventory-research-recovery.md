# Inventory research recovery — September 21, 2026

This investigation answers Mark's follow-up about why 92 completed jobs have no estimate, whether new cards repeat that outcome, and how existing inventory can recover. It authorizes no fabricated catalog review, guessed identity/price, synthetic stock, destructive operation or blind bulk rerun. The existing approved delivery authority covers implementation and qualified operational work.

## Current release — catalog-independent v6, September 22

Production now serves exact `db465c1c57659233d4841f8679cca0aac3ecdd8f` / `dpl_CyDxyASkDk2HGGRwjDtCGqovtLQX` (`41olp9f3x`). Direct readback at 15:35:59 UTC verifies all five aliases, project target and the sole enabled minute research cron. Postflight at 15:36:22 UTC passes 28 routes and referenced assets, build `HdwiGQMJw0yDYUgMuhh83`. Stable Preview remains the same-source execution-disabled `dpl_2VJgRGbJA1TUm9iikGZQvEX6WtLy` (`p6s7ca002`). Production recovery and `STAFF_INVENTORY_RESEARCH_SALE_DETAILS` are true; catalog/admin-consumer, full-resolution, contribution and diagnostic execution remain off. No new migration, shared-host restart, ATLAS deployment, manual cron or catalog publication accompanied promotion. Vercel explicitly skipped migrations; the previously verified ledger remains 99 active / 13 historical rolled-back entries.

### Retrieval, qualification and preserved limits

Usable saved card identity authorizes bounded eBay candidate retrieval without catalog coverage or verified originals. Missing originals and unresolved details remain visible needs. When usable identity is absent, bounded recognition may propose missing fields; otherwise the worker records the missing identity without fabricating a query. Recovery preserves every nonnull staff fact and exact saved-description binding. Existing individually described cards remain eligible after loading; roster membership is never proof of current possession or a sale. Bulk-origin units require an exact one-unit description with distinct individual photos, and known shared group photos cannot gain authority through a one-unit title edit. No aggregate stock is expanded into invented cards.

Supported positive observations of exact identity and treatment in both distinct verified originals may authorize private v6 matching without asserting catalog approval. Selected value still requires at least two eligible, independently imaged sold listings, matching identity/treatment and raw/grader/grade condition, known actual amounts and the integer-cent arithmetic mean. Seller titles and unreviewed catalog links are insufficient. Unknown accepted Best Offer amounts remain excluded. V1–V5 results and v1 recovery assessments retain their original meanings and byte verification.

A persisted behavior revision makes old backlog assessments due for one immediate recheck rather than waiting six hours. Material new evidence or the versioned engine transition can schedule bounded work; unchanged evidence or a loss-only catalog outage cannot repeatedly spend paid refreshes. Preserve manual retries, prior attempt/results, all four recovery histories and staff review decisions. Existing limits remain: two photo attempts, two scope attempts, three evidence-backed automatic refreshes, nine lifetime research attempts and two explicit manual retries per exact input. The sole cron prioritizes ready research, uses existing intake/capacity fences and runs providers outside transactions. Stale revisions/cancellations/claims remain fenced.

The bounded ordinary-sale resolver permits two item IDs total across three searches, six seconds each, caches failures and does not retry details. Optional catalog/full-resolution flags remain separate. The model ceiling is 120 seconds, clamped to remaining 150-second engine time minus 10 seconds finalization. Optional rounds retain their 84-second cap; the diagnostic remains 170 seconds and worker lease 180 seconds. A slow valid first assessment may leave no room for another refinement.

### Qualification and remaining real-card limitation

Final db465c1c qualification passed 520/520 staff tests across 42 files and 133 focused tests, with scoped lint/diff checks clean and the same 12 unrelated grading-test type errors. The sealed recovery run `20260922-recovery-v2-run-3` passed 26/26 disposable PostgreSQL cases, its migration/no-op preservation checks and cleanup. Its separate 20-pair local contention fixture passed; the immutable summary retains `release_pass:false` and `performance_pass:false` with no whole-release or hosted-performance conclusion. Exact hosted Preview/Production builds independently passed 79-page generation, Linux x64 Sharp 0.34.5, Fluid 300 and private-route qualification.

The corrected Snivy diagnostic on db465c1c completed at 15:23:45.371 UTC in 111.301 seconds: two searches, two detail calls, nine thumbnail reads and two real model responses at 50.268 and 56.330 seconds. The first strict answer preserved raw condition and explicitly unresolved printing/foil. Final output retains 24 possible candidates, zero matched, zero selected and unknown value. The second HTTP-200/completed answer duplicated the `manufacturer` observation across front/back and failed the strict unique-field schema; its optional assessment was rejected while the first valid findings survived. Final comparison states are six assessed, three failed and fifteen unassessed. This is a retained **P2 optional-assessment availability limitation**, not authority to relax identity rules or claim useful matched comps.

Independent review verified all 15 blob hashes and reconstructed both model requests from actual original/candidate JPEGs. All nine listing images are 225-pixel thumbnails; larger advertised URLs were not fetched. Only the 108-cent ordinary auction has verified known sale evidence; the second yearless ending and unhydrated accepted offer remain unknown. Raw terminal SHA `a1ba2de9e0c69b001f1c7d804dbcc6332d11f4a347c92107ee83b6c924d66d58`; independent analysis SHA `6a098a975338b841c5a40a350d57fb4d7fa7bdc91a4ad1815bd564e54cb5ea8e`, under `20260921-v6-real-diagnostic-preparation`. This establishes the timing repair and retained research, not a Snivy price, ordinary fixed-price coverage or all-card accuracy. No further paid diagnostic is implied.

### Latest natural progress — September 22, 15:48 UTC

**Latest canonical checkpoint — September 22, 15:48:10.922 UTC:** 98 eligible cards include nine current v6 results with 77 candidates, 50 assessed comparisons bound to retained image hashes, two matched assessments, zero selected comps and zero estimates. Eight of the nine results retain comparisons; two record an initial comparison failure and one an optional comparison failure. Failure markers can overlap retained comparisons: a completed job means a result was persisted, not that every model assessment succeeded. Thirty-six jobs now have the v2 recovery revision and 36 automatic refresh entries have been appended. Current statuses are 28 queued, one running, four pre-existing failed and 65 complete, including legacy results. All 99 original inputs and history prefixes and all 93 original result payloads remain preserved (84 current, nine in attempt history), with zero stale bindings or integrity failures. This is a dated, incomplete checkpoint; matched labels alone do not establish selected value or measured accuracy.

Latest receipt: `20260922-v6-expanded-postpromotion-outcome-preparation/runs/first-expanded-natural-progress/outcome.json`, SHA `aaeb1d3d3567952d26fefb222c5517a6881da1a81fe4eb09fd50df17e6f20dd4`, under the private TenKingsInventory investigations root. The expanded read-only projection separates retained image-bound assessments from failure markers; null-result attempts retain unknown engine attribution. It does not re-fetch provider responses or independently reverify listing images.

The 50 image-bound assessed comparisons are distinct from the 77 candidate classifications: the latter also include conservative fallback decisions. Nine acquired-image comparisons are marked failed and eighteen candidates are unassessed. Eight results retaining comparisons is not nine successful model jobs, and the two initial-failure markers do not by themselves mean both cards lack later retained comparisons. The original 98-card and historical 92-card cohorts remain separate in the receipt.

### First postpromotion snapshot — retained earlier checkpoint

The canonical RepeatableRead READ ONLY capture at **15:36:24.434 UTC** finds 109 roster units: 98 eligible individual cards and 11 ineligible shared-bulk units. All 98 current bindings are exact; statuses remain 92 complete legacy V2 unknowns, four failed and two queued. Eighty legacy recovery assessments await catalog evidence and eighteen need staff review; none has the new behavior revision yet. There are 1,500 historical candidates across 87 cards, zero selected comps/estimates, zero v6 results and zero appended research attempts at this first observation. Do not present it as a completed v6 pass.

Compared with baseline SHA `c7b81c33fa5f819d2d6279be87187f8f00f03cc74e34f84610bd91218311bde3`, all 99 jobs, saved inputs/descriptions, 93 original nonnull result payloads, manual retry/attempt prefixes, 98 saved recovery states and every photo/scope/source/refresh history prefix are retained. Six original results remain null; no review row existed at either snapshot. Internal stored-result integrity passes. Capture SHA `d08fc7d71dc27c1c1acf7a1222766d13a4628ec714836c28a83d9bbb455d53e2` is at `20260922-v6-timing-postpromotion-outcome-preparation/runs/first-outcome/outcome.json`. The next checkpoints should report natural work separately for all current eligible units, the original 98-card cohort and historical 92-card cohort; null-result attempts carry no engine version. HTTP cron success does not prove job outcomes, and stored claims do not independently verify provider/image bytes. Root released the Inventory coordination hold for ATLAS after this first preservation proof, without waiting for the entire backlog.

### Recovery and rollback boundary — already crossed

The 15:48:10.922 UTC checkpoint proves 36 persisted v2 recovery states and nine v6 results. Legacy 6GK readers are therefore no longer a compatible rollback target. The first new-format recovery state write, including a v2 claim persisting `behavior_revision` before any paid call or estimate, crosses the legacy-reader compatibility boundary. Persisted v6 results also require compatible readers. After that boundary, do not restore 6GK or another legacy serving/cron artifact; roll forward with v6-compatible code and retain all additive schema/history. A newly qualified recovery-disabled artifact pauses automatic recovery and queued automatic refresh attempts but is not a global ordinary-research kill switch. Environment metadata changes do not affect existing artifacts. No SQL reset, deletion or result-history rewrite is a rollback remedy.

Release receipts are in `20260922-v6-timing-production-preparation`: metadata SHA `6797af97d5c192112d49bf080ae82039b273c58b2d0d0979b03520ccf4a75e5c`, route/assets SHA `e7c51549fa101dac3a9337b43a0f219827124be6e9c75e829bbe6d54d276be12`. The current [runbook](../runbooks/DEPLOY_RUNBOOK.md) records operational boundaries. All private evidence directories are under `/Users/markthomas/Library/Application Support/TenKingsInventory/investigations`.

## Historical investigation and earlier releases

The sections below retain the September 21 diagnosis, suggested catalog-first sequence, original qualification and 5Dmq/6GK observations. Their present-tense statements describe those dated snapshots, not the current v6 release. Catalog preparation is no longer a prerequisite for automatic candidate retrieval; earlier counts and receipts are not rewritten.

## Why the result is unknown

The research contract requires an exact supplied catalog reference and a distinguishing feature visible in the original photos before any priced selection. The model prompt, server validator and shared legacy-compatible schema enforce this separately from whether a returned listing visually matches. No references means unresolved research identity, an empty selected set and an unknown value. Recognized intake text remains useful descriptive data; the unknown state does not establish that Astra failed to read the card's name.

The active legacy lookup is a read-only SetOps database query. It requires year, manufacturer, set and card number; exact matching card/parallel/scope rows; approved ingestion/draft provenance and reviewed source timestamps. It does not browse for or create a missing reference. Its caught failures also leave references empty. The first aggregate alone did not distinguish missing records, missing input fields, aliases/number mismatch, approval gaps or lookup errors per card.

Twenty of the 92 jobs retained at least two visually matched candidates. That does not establish two independently imaged, condition-correct listings with eligible verified prices. The catalog gate prevented selection regardless; sale/visual evidence remains a separate check afterward.

## Pre-recovery deployment snapshot

Fresh normal Vercel reads at 01:56–01:58 UTC September 22 confirm tenkings.co still serves bc92b102 / dpl_FXLDCapouMhB7WtULnCTb45QXG31 and all five optional catalog-consumer/admin, sale-detail, full-resolution and contribution flags are absent in Production. New jobs therefore use the current V3 default engine with legacy references. The historical 92 results are V2, but both versions retain the same catalog-backed identity requirement. New individual-card intake/save/enqueue works; a new card without a usable reference can still finish research with no value. The UI release did not solve this coverage dependency or rerun historical work.

## Pre-recovery affected inventory and retry budget

The 02:00:20 UTC read-only audit verified the complete canonical workflow hashes and replay before counting current cards. All **92 complete/unknown jobs are 92 distinct current, on-hand, individually received cards**, with matching latest description/input and no newer research job. They are not stale or cancelled historical duplicates. Eighteen miss at least one legacy lookup field; 74 have all four required fields. Missing fields overlap: year 16, set 8, card number 4 and manufacturer 4.

Ninety currently qualify for the supported Research again action. Two have used both explicit retries; they have not reached nine lifetime attempts. Both have all four required fields, so a missing-field edit is not currently evidenced for either. A genuine description correction still follows the existing new-input path. For an otherwise unchanged exhausted input, the current UI cannot grant a third explicit retry. Recovery needs a supported audited refresh extension tied to a meaningful evidence/engine update, preserving attempt history and bounded work. An actual verified description correction can use the normal new-input path; no ad hoc SQL reset or invented edit is appropriate.

The 99 visible Inventory groups represent 109 current units: 92 unknown individual cards, four failed individual cards, two individual cards without a current job, and one bulk group containing 11 units. Automatic per-card research intentionally excludes that bulk receipt. The broader recovery should also account for the four failed and two unresearched individual cards without conflating them with the 92.

A separate bounded current count confirms the legacy catalog is not globally empty: 72,017 card rows, 6,853 parallels and 43,878 scopes exist. Approved card/parallel/scope joins before exact identity filters total 998,152 rows, which are combinations rather than unique useful references. These counts do not prove coverage for the 92 target cards. The 02:02:22 UTC exact legacy lookup audit then found **zero set-alias hits for all 74 complete descriptions**, before card-number, parallel/scope and approval filters. Thus the immediate cause is missing usable exact catalog bindings under this path, not an empty global catalog. A bounded public-product-label diagnostic covering five frequent product labels (12 jobs) found no close same-year/manufacturer/product-token setId hit, but it does not prove missing catalog data for every card; maker-omitting IDs or other naming differences can evade that diagnostic too. Never use fuzzy similarity as approved identity authority. Map genuine equivalent identities or acquire the missing facts for the actual encountered sets.

## Smallest useful recovery

1. Identify distinct current individual cards and remaining retry budgets; separate genuine description issues, missing catalog coverage, inadequate sold evidence and failed jobs. Preserve all receipts/history and financial fields.
2. Correct only genuinely wrong/missing descriptive fields using existing Edit details & price controls. A genuine change queues its exact new input; do not fabricate edits to escape retry limits or treat typed variant text as catalog authority.
3. Prepare narrow factual catalog coverage for the actual encountered cards/printings, backed by appropriate sources and useful visible diagnostics. Reviewed supported positive evidence in a partial catalog is sufficient; a complete set or image library is unnecessary. A reference image is optional where source-backed text and the saved photos establish the feature. When reused images are needed, their image-use permission, depicted identity and applicability remain separately reviewed. Current sports unknown relationships and Pokémon literal marker recipes must not be represented as useful supported physical-finish evidence merely because they can be published.
4. Qualify and activate the existing shared-catalog consumer against useful approved records. Publishing records without enabling the consumer, or enabling empty catalogs, does not resolve this problem. Qualify sale-detail/image enrichment only for its separately exercised evidence and load effects.
5. Demonstrate one small current-card cohort end to end, then deliberately rerun the other eligible affected inputs in bounded groups. Research again already accepts complete/unknown or failed jobs with fewer than two explicit retries and nine lifetime attempts, binding the exact job/input/description/attempt. Each explicit retry can permit up to three attempts. The current result can update while prior attempt evidence remains retained. Do not bypass limits or overwrite history.
6. Review actual new selections side by side. Report a value only with exact identity, compatible condition and at least two eligible distinct-image sold comparisons. Some rare or unverifiable cards can remain honestly unvalued; neither reference publication nor a retry guarantees 92 prices.

## Source evidence

- `frontend/nextjs-app/lib/server/staffInventoryResearch.ts`: model instructions, validateAnalysis and reference loading.
- `frontend/nextjs-app/lib/server/staffInventoryResearchReferences.ts`: exact legacy lookup and approved-source joins.
- `frontend/nextjs-app/lib/server/staffInventoryResearchCatalog.ts`: positive partial catalog and text-only reference support.
- `frontend/nextjs-app/lib/server/staffInventoryResearchWorker.ts`: feature-gated reference loader.
- `packages/shared/src/staffInventoryResearch.ts`: default V3 and catalog V4 identifiers; compatible identity/result rules.
- `packages/database/src/staffInventoryResearchV2.ts`: retry eligibility, current-result update and immutable attempt history.
- Private read-only receipts: `investigations/20260921-research-unknown-aggregate` and `investigations/20260921-research-recovery-audit` under the existing TenKingsInventory application-support directory.

No provider research, staff decision, catalog publication, activation or business-data mutation was performed for this explanation. Production/source behavior and reusable recovery controls were inspected rather than inferred from tests.


## Owner-approved automatic implementation

Mark subsequently approved ongoing automatic recovery for existing and future cards, not a one-time rerun of the 92. The implementation uses the sole minute research cron, the existing job rows and queue lock, and additive private recovery state. Each current individual input can receive bounded original-photo analysis; completed recognition is reused for cheap catalog checks. Missing facts become private null-field proposals and visible staff review needs. Conflicting human facts are preserved.

A reviewed, useful evidence digest permits a bounded automatic research attempt. Unchanged/empty evidence waits with backoff; provider failures and exhausted limits remain explicit. Manual retry history is retained and automatic attempts cannot exceed the lifetime research limit. Positive catalog authority, exact-photo identity and at least two independent verified sold comparisons still govern values. Research results remain bound to the original description revision while any enriched private context is recorded separately.

Qualification must cover first-time missing details, existing unknown/failed/missing jobs, manual-budget exhaustion, changed-evidence wakeup, stale claims, concurrent workers, intake headroom, provider failure/backoff and reviewed-result protection. Staff UI must show the actual saved recovery status, missing fields/conflicts and the canonical Edit details route. Runtime activation requires the additive migration and qualified artifact; source implementation alone is not proof that the 92 recovered.


## Implementation and qualification

The local candidate adds five private recovery fields to the existing job, with immutable recognition, source-search and automatic-refresh histories. Per exact input: at most two original-photo attempts, two catalog-scope attempts and three distinct evidence-backed automatic research refreshes, within nine lifetime research attempts. Manual retries are retained unchanged. Normal checks back off six hours; unavailable services back off thirty minutes. New queued cards receive the same preflight, and canonical historical individual receipts without a current job are discovered automatically.

The sole cron claims ready research before recovery so a slow recovery backlog cannot starve eligible work. Recovery uses the existing capacity/intake leases, cheap due probes and nonblocking canonical lock acquisition. All providers execute outside transactions. Changed descriptions/cancellations fence old claims. Turning `STAFF_INVENTORY_RESEARCH_RECOVERY_ENABLED` off pauses automatic work, including already queued automatic attempts; staff can still read retained outcomes. Existing optional catalog/sale-detail/image flags retain separate authority.

Missing catalog coverage can produce a reusable unreviewed source-search packet for the exact public product demand. One durable search reservation per demand permits at most two fixed public search requests, ten seconds total, 256KiB per response and three allowlisted direct candidates. It sends public product descriptors, not card names, private IDs or images. These are search results only: no candidate page is imported, fetched as approved evidence or published. Staff see exact blockers and links to the existing catalog review and canonical Edit details controls. Opening or polling a card never starts recovery.

Root Node22.23.2 final broad staff qualification passed423/423 tests. The final sealed disposable PostgreSQL run passed21/21 transaction cases, all98 local migrations and a no-op second deploy; owned processes and temporary database were removed. The migration SQL SHA-256 is `4d0c9374b9f28e896ac7a8e468dec3a783ec5d1959218f5d80b812b61b638c7a`. Four actual-component desktop/mobile renders had no overflow, external requests or browser errors. Full frontend type checking has only the twelve retained unrelated grading-test diagnostics; focused recovery/shared/database checks pass. A test-only acquired-lock barrier verified twenty full recovery replays and twenty actual writer contentions with no yielded recovery claims: save median29.06→43.71ms and p95 37.95→57.94ms passed the unchanged declared local bounds. Controlled local timing is not a claim about hosted saturation or live card coverage. The final review also added a claim-time evidence digest: a superseded automatic assessment cannot fall back to ordinary paid research.

## Production activation and initial observed results

ATLAS explicitly released its shared-host/DB hold after its independent release. Root committed the qualified application as `6bfe43d81a2e16869a3f570d110959d40f8082dd`, applied only the additive recovery migration using managed Prisma 5.22, and verified exact old ledger/schema/all 97 research-row preservation plus a second no-op deployment. The managed ledger is 99 active / 13 historical rolled-back entries. No ATLAS restart, deployment or catalog approval occurred.

Production is `dpl_5DmqXUVGtjUagX1oeKHBic1BYhV4`, immutable `tenkings-backend-nextjs-gig6dzjq7-ten-kings.vercel.app`; all five aliases / project target / sole minute cron were verified at 03:02:36 UTC September 22. Main Preview is `dpl_23hGjdJQQ31J1iboZvQpiEZsGjcx` (`g3q64ycuv`) from the same source. Both 79-page / Linux Sharp 0.34.5 / Fluid 300 builds and private boundaries passed. Production postflight passed 28 routes / six assets, build `zx4IFKyfqyJQiwnOAa9X5`; stable Preview passed six routes / five assets. Vercel builds skipped migrations. Only the new Production recovery execution flag is true; optional catalog/detail/image/contribution/diagnostic controls remain off.

The preactivation snapshot at 02:58:58 UTC replays 402 canonical workflow events and confirms 98 current individual units: 92 unknown, four failed, two without jobs; 11 bulk units remain excluded. Natural cron requests at 03:02:37.319 and 03:03:37.220 UTC returned HTTP 200. A separate 03:04:20 UTC read-only audit confirms 99 total job rows, both missing jobs created, zero stale bindings and eight historical cards assessed/waiting for catalog evidence, with 84 still unchecked. Six source reservations produced eight reused/retained `not_found` packets. No photo call was needed for those first eight complete descriptions. All 97 baseline inputs and original manual-retry/attempt prefixes remained intact; no retries or research attempts were appended and no estimates were produced. These are observed initial results, not full backlog or accuracy completion.

Private receipts: `investigations/20260921-research-recovery-migration`, `20260921-recovery-web-release`, `20260921-recovery-release-qualification`, and `20260921-recovery-outcome-audit`. The latter binds read-only outcome checks to preactivation SHA `7c127d643c2ad2252502b2d842b6a298a7a9c10d9fa5dfc5feb96ac9d118c743`. The process will continue automatically; actual source/applicability review remains required where positive catalog facts are missing.


## Initial pass complete and final source correction

The 03:30:01 UTC canonical audit confirms all 98 current individual cards have an initial recovery assessment: 80 waiting for reviewed catalog evidence and 18 needing staff review. For the original 92, 76 need catalog evidence and 16 need details/conflict review (15 missing-field cases and four conflict cases, overlapping); all 18 original missing-anchor photo analyses completed once. The four previously failed and two previously missing jobs were also assessed. All 97 original inputs/manual-retry/attempt prefixes remain intact, with no missing jobs, stale bindings, due checks or live/expired recovery leases at that observation. No new sold-research attempt or qualified estimate was produced. The durable worker will continue cheap evidence checks and handle new cards automatically.

Live source outcomes exposed a redirecting DuckDuckGo endpoint. The direct canonical endpoint returned a provider challenge during one bounded public-product diagnostic, so the correction also requires exact HTTP200 and expected provider content/structure before recording a completed response. It never submits or bypasses challenges. Existing packets/reservations remain immutable and are not automatically retried; a completed Bing response with no accepted links can still yield a bounded `not_found` packet when the fallback is unavailable. None of these outcomes proves that no catalog facts exist elsewhere.

The correction is live at exact `9e8a9c1d99cce0b4b8a4a5dbde241fc916cdb439` / Production 6GK (ewysxk0rw), with qualified Preview 3Bwd (ocp9wiuex). All five Production aliases/project target/sole cron were verified at 03:33:39 UTC; 28 routes/six assets passed, build `b9EpiUYH5zneUEWK0NAfB`. Both hosted builds and staged private boundaries passed; Production skipped migrations. Fifteen focused source tests and 54 root source/worker integration checks passed. No new environment/database/budget/history change accompanied this source-only correction. Full private proof is in investigations/20260921-recovery-source-fix-release and recovery-source-fix-qualification.

Final 6GK natural cron requests at 03:34:37.681 and 03:35:37.269 UTC returned HTTP 200. Stable Preview 3Bwd passed exact-alias, staff-shell build identity and unsigned research privacy checks. No manual cron invocation or source-packet replay occurred.
