# Evidenced holding corrections — September 9, 2026

Mark authorized finishing correction controls for stock already processed or moved. Add a bounded append-only `stock_corrected` variant to the existing schema-2 journal, through `packages/database/src/cardPlatformV2.ts` only. No new table, permanent-card mutation, customer commerce or hardware action is required.

The event names the selected receipt units, an exact latest physical-state event anchor for each unit, the reason and evidence, and one explicit current-state correction: processing/product for unpacked stock or a product correction that retains the exact current packed state; complete one-card pack replacement or unpacking; or actual non-machine custody. The immutable old event remains visible. Receipt/lot/cycle identity, quantity, acquisition cents and permanent-card links cannot change. Historical reports before the correction remain unchanged.

Only explicitly identified held units outside an active machine batch qualify. Current anchors and full replay reject stale or backdated attempts to bypass later use. Loaded batch membership does not identify the remaining cards after aggregate sales. An actual identified removal/return must establish those holdings before correction; never invent that movement to bypass a refusal. Retired pack IDs remain reserved, custody-to-Location meanings remain immutable, and an existing permanent-card link cannot be demoted below processed.

Implement the strict command/event contracts and independent financial replay together; expose the forms in the existing Purchased lots and batches workspace with its same preview/record/retry path. Corrected product and custody evidence retain their explicit source mappings. Add source replay, guided DOM, disposable PostgreSQL writer/export and temporary financial mirror/Story tests with representative correction, packing, machine loading and aggregate sale facts. Tests are isolated fixtures, never real stock.

The coordinator owns the approved blueprint/model amendment, source release and all live operations. This assignment performs no commit, push, deploy, restart, live migration or real inventory entry. No financial records, secrets, configuration, backups or logs may enter this public source repository.

September 9 release-preparation continuation: the coordinator subsequently delegated the narrow source blueprint amendment and draft PR preparation under Mark's existing authority. Freshly verify main and the live alias, create an isolated release worktree, preserve the original edits, repeat focused tests and the full Node 22 production build with migrations disabled and no production credentials, review the exact diff, then commit/push and open a draft PR. The coordinator retains final acceptance and merge/deploy coordination. No live schema, credential, business-data, provider or hardware operation is included.
