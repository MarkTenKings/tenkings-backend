# Planned inventory sales channel — September 11, 2026

Mark authorizes an optional planned channel for a card or selected batch, entered and edited by staff: Vending machines, Stores, Kiosks, Ten Kings online, eBay, Whatnot, and Amazon, with an explicit custom label supported by the contract. This describes the intended route for the expected selling price. It does not establish an actual sale, financial channel mapping, expected-price amount, current location, or custody.

1. Extend only `item_described.description` with optional nullable `planned_sales_channel`: an exact nonblank string up to 160 characters with no leading/trailing whitespace or control characters. Leave omitted keys absent during parsing and hashing. Add the matching independent financial consumer schema before the coordinator releases a source capable of emitting the field.
2. Keep the existing complete description snapshot semantics for existing fields. Coordinate a narrow compatibility rule for the new field only: an omitted planned channel preserves that unit's prior choice, while an explicit string or null replaces it. Apply the same effective-time replay rule in source and financial consumer without modifying immutable events or old hashes.
3. Expose the derived planned channel in authenticated staff groups, which already separate differing descriptions, and retain the operational financial snapshot's current strict wire shape by omitting the new extra there. The detailed immutable journal retains the complete evidence. Root owns the staff UI for explicit selection, preservation, display, filtering, and editing.
4. Verify legacy hash/optional-key absence, approved and custom choices, malformed inputs, omitted versus null behavior, mixed-unit batches, backdated edits, exact retry, and unchanged price/cost/custody/financial actuals. Exercise the sole source writer in disposable PostgreSQL, then feed its generated journal through the financial consumer using a temporary ledger.
5. Record scope and results in source/financial handoffs and the authorized financial data-model amendment. Neither existing PostgreSQL envelope guards nor SQLite JSON validity guards restrict description keys, so no SQL migration or schema/table/writer change is required. The coordinator owns consumer activation before source release. This subtask performs no commit, deployment, restart, configuration mutation, or live business write.

## Staff interface and release plan (coordinator)

- Put one optional Sales channel selector after the two money fields in Add Inventory section 3 and in Edit details & price. Use the seven approved labels; retain an existing custom label when editing. Show Not decided yet for an unset value, never infer a channel from location or a previous card.
- Keep the channel in saved drafts and exact pending requests. A confirmed single-card save starts the next camera cycle with channel unset, just like the next card’s price and cost.
- Show the saved channel beside expected sale price and in item details. Add a channel filter and channel-aware search, with working clear-filter and location-navigation behavior. Mobile controls retain the existing large touch targets and stack without overflow.
- Extend the existing interaction suite for add/edit/clear, old drafts, exact retry, next-card reset, search/filter, and unchanged gross profit. Review the combined diff, run scoped UI/backend/consumer checks and the production build; activate the compatible financial reader before a code-only collect release.

## Persistence and consumer verification

Scoped persistence/consumer review PASS: the optional field preserves historical hashes, the explicit omission/null rule is identical in both replayers, and description-based grouping keeps different per-unit plans separate. No source SQL or financial schema/writer change is required. Source database build and 46 workflow/staff tests pass; API/privacy12 and scoped lint pass; financial inventory115 and TypeScript pass. Disposable PostgreSQL passes all94 migrations plus unchanged repeat and37 tests across its exact/workflow/staff suites. The 121-event,12-description export replays through actual financial sync with original hashes, exact retries,4,007 described units, matching planned channels and unchanged bank/Story actuals. Its cluster and the temporary financial ledger were removed. Logs and the private export use `/private/tmp/tenkings-planned-channel-*-20260911` names. The coordinator's UI/build and consumer-before-source activation remain separate release steps.
