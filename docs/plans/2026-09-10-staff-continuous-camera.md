# Continuous staff card camera — September 10, 2026

Mark's rapid-entry continuation is implemented in the existing isolated staff release. This assignment owns only the reusable local camera component, its CSS and disposable tests; the coordinator owns the inventory form, identification, location, private upload and save integration.

1. Keep one rear-camera MediaStream for an open Add inventory session. Capture Front, show its preview and a Retake control, then capture Back and deliver both local JPEG files once. No photo or inventory request originates here.
2. Keep the component mounted during identification/pricing. Pause its video and disable retained tracks when hidden. A new confirmed-save cycle clears local previews and resumes the same stream. A closed add modal, unmount, page hide or hidden document stops tracks. If the browser stops playback or a stream, offer an explicit Resume camera action; do not automatically request another permission after asynchronous save.
3. Provide an explicit photo-library mode with separate front/back choices. Reuse the existing bounded, cancellable HEIC preparation helper and preserve an accepted front through a cancelled picker or failed back. Camera capture produces bounded JPEGs from the actual video frame without inventing a card identity or cost.
4. Use the staff workspace's white/sage palette, 44-pixel controls, viewport-safe actions, an accessible dialog and keyboard focus handling. The camera guide is a visual aid, not a detected card boundary or grading claim.
5. Verify sequence, duplicate-click ownership, retake, repeat cycles, library/HEIC integration, permission denial, playback interruption, visibility/unmount cleanup and stale asynchronous completion using synthetic local fixtures only. Inspect phone/tablet/desktop widths in a loopback browser. Record scoped reviewer evidence before coordinator handoff; no production data, deployment, package change or commit is performed by this assignment.

Component contract: `open`, `cycle`, optional `disabled`, optional `initialSource: 'camera' | 'library'`, optional `autoStartCamera`, `onPair(frontFile, backFile)`, `onClose`, optional `onError`. The parent remains the sole owner of confirmed save receipts and increments `cycle` when beginning a fresh pair. It sets `autoStartCamera=false` for an asynchronous next-card open so a previous manual/library entry cannot trigger a new camera permission request; retained live streams still resume.

## Verification and handoff

- Node 22 camera lifecycle tests PASS 11/11: front/back sequencing, retake, duplicate shutter ownership, paused-stream reuse, library cancellation and HEIC helper boundary, stale media completion, denied permissions, explicit resume after an automatic first open, tab/track cleanup, blocked or interrupted playback, cancelled preparation, stale encoding and keyboard focus. Scoped Next lint passes without warnings. TypeScript reports no camera component/test errors; the coordinator owns the complete frontend check.
- Standalone Chrome and WebKit loopback proof passes at 320, 390, 768 and 1440 pixels, including two pairs from one synthetic MediaStream, retained disabled tracks during pricing, retake, cleanup and actual synthetic 12 MP HEIC front/back files through the unchanged local decoder. JPEG outputs retain the actual frame aspect ratio, are bounded within 1400 pixels and remain well below the upload limit. Report: `/private/tmp/tenkings-staff-continuous-camera-proof/report.json`.
- The integrated current Workspace, actual camera/normalizer and local API fixtures pass in Chrome and WebKit at 320, 390 and 1440 pixels plus a 380-pixel keyboard viewport. Two cards save through one camera request and one GPS request. Manual names/prices survive identification, confirmed-save cycles clear card-specific prices, and an intentionally lost save response replays the identical request while creating only one fixture record. The actual map component's unconfigured local presentation is inspected without contacting Google. Screenshots and report: `/private/tmp/tenkings-staff-rapid-workspace-proof/`.
- Scoped foundation/blueprint reviewer verdict: **PASS**. The camera returns local descriptive photos only and adds no HTTP, storage, inventory, grading, financial or database write path. Shared HEIC preparation, parent upload and save authority remain intact. Fixtures are synthetic and loopback-only. Physical iPhone camera permission/hardware and live provider/release checks remain coordinator acceptance; no production photo or inventory record, package dependency, commit or deployment was created by this assignment.
