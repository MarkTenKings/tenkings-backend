# Staff mobile photos and location map — September 10, 2026

Mark requests iPhone HEIC uploads, direct phone camera entry, a simple mobile inventory form, and an alternative location map with inventory pins. Three fresh Astra Max agents own the photo codec, mobile form, and map component. The lead integrates authenticated location coordinates, verifies the real production artifact and releases the scoped change.

1. Convert HEIC/HEIF locally in the browser with a bounded, lazy-loaded decoder; normalize camera/library photos before the existing private checksum-verified upload. Preserve the draft while processing, reject invalid files clearly, and exercise real HEIC bytes in a disposable browser.
2. Offer explicit Take photo and Choose photo controls in add/edit. Use the native rear-camera chooser, touch-sized controls and mobile safe areas. Keep cancellation, asynchronous request ownership and exact inventory-save retry intact. Prevent the concierge overlay from covering the staff form.
3. Reuse the existing Google Maps foundation for a list/map choice and clickable inventory pins. Return coordinates only through the existing authenticated staff workspace. Locations without saved coordinates may resolve their existing address on demand through an authenticated, read-only map-point route using the existing Google geocoder. Map results carry address-match provenance and never create or edit Location records; private HQ remains absent from public APIs and the financial wire stays unchanged.
4. Run focused data/API/UI regressions, responsive visual and HEIC processing checks, scoped lint/types and the Node 22 production build. Record reviewer PASS before commit. Preserve the current collect production lineage, unrelated ATLAS/main work, the `main:false` Git deployment hold, and `RUN_DB_MIGRATIONS=false`.
5. Deploy the reviewed source artifact to the existing collect project, verify immutable deployment/alias plus camera/photo/map UI and financial read compatibility. Do not create sample inventory or upload user photos for acceptance. Real phone camera hardware permission and capture are browser-owned actions; native input wiring and responsive behavior are verified in the available browser.
