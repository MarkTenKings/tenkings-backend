# Staff intake location recovery — September 10, 2026

Mark and office staff confirm that rapid front/back capture and Astra identification work quickly and accurately on phones. Browser location permission is allowed, but the inventory location does not fill automatically. This follow-up fixes location acquisition, matching, and read-only coordinate reuse; it does not change inventory, custody, financial events, saved locations, or provider configuration.

1. Reproduce the deployed `aeae3e87` path with disposable fixtures matching the authenticated map API: saved coordinates plus address-resolved points, the existing geofence radius, delayed/failed reads, coarse and refined phone readings, permission outcomes, and manual/cancelled attempts. Check the API response mapping against the browser adapter before changing it.
2. Keep source coordinates authoritative and reuse successful address-derived coordinates in memory between the map and intake. Cache keys include the existing location ID and exact saved address. Do not cache failed, invalid, or cancelled results; never submit raw phone positions to the server or a map provider.
3. Bound address resolution across the entire attempt and report safe progress. A failed unrelated address must not suppress an otherwise nearby, unambiguous suggestion without explanation. Suggestions remain editable and require the existing staff save confirmation. Preserve ambiguity when multiple known locations could match, reject distant sites or unusable positions, and describe incomplete address coverage honestly.
4. Compare phone accuracy with each existing bounded geofence instead of the unrelated blanket 100 m limit. Distinguish a fully contained reading from a nearby suggestion requiring careful confirmation. Do not infer a custody type from venue classification. Root owns bounded browser position refinement and workspace progress/manual selection behavior; helper interfaces are coordinated before implementation.
5. Run focused helper, map adapter/API, browser cache, and workspace regressions with fixture coordinates only, followed by scoped lint. Record the demonstrated cause and remaining physical-device uncertainty in the handoff. Root coordinates final build, review, and any later release; this subtask performs no commit, deployment, service action, or live business write.
