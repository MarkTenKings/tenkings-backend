# Inventory intake, accuracy and shared-interface acceptance

September 21, 2026. Initial bounded read-only audit used Inventory HEAD `297fd302b866286824fcea79b8f77c6d995ca3db`, application `e286ea0a190bfdc6807fe8ca58c47b34c94b55c1`; main application `895a3b34cc9cd7c8f6d2267ad4d763fb2ff0949d`. The subsequent owner-authorized scorer implementation began at `7c7b61fc36c3e74ba667e3dd5f3c1b74ddaf1320` in the isolated Inventory checkout. This lane now owns only the offline scorer, its separate new schema, focused scorer tests and this packet. Root owns integration and SESSION_LOG. No provider, DB/UI write, configuration, build, deployment, shared-host or ATLAS action was performed by this lane.

**Next useful work: independently review the implemented V4/V5 scorer mapping and assemble independently labeled inputs; prepare the sale-detail off/on load delta after its new durable fixture passes.** Mark confirmed phone testing complete on September 21; root closed that gate on owner confirmation without claiming a new device/build receipt. Keep phone acceptance, controlled load, broad accuracy and cross-app reuse as separate verdicts. None currently establishes the greater-than-90% all-card target.

## Evidence and concrete gaps

| Gate | Standing evidence | Remaining acceptance |
| --- | --- | --- |
| Default-off intake | [Full matrix](../2026-09-17-intake-full-matrix-results.md): 32 blocks, 3,200 measured saves; all frozen timing/repetition/headroom/integrity gates pass. Separate [default cron fixture](../2026-09-17-default-research-cron-functional-fixture.md) passes its wiring gate. | Optional sale-detail/catalog/full-resolution work was off. These passes do not measure camera, mobile network, hosted cold starts or enabled enrichment. Reuse them without rerunning. |
| Exact Preview / phone | Lead's [verified state](verified-state.json): both exact Previews READY; all 172 + 44 + 12 retained archive members rehashed. Prior human desktop read loaded inventory. Mark subsequently confirmed phone testing complete. | New apex-origin routing/auth/private-photo/map checks remain release checks. No repeated phone test is requested. |
| Accuracy tooling | Historical scorer/schema retained. Separate schema-v2 mapping now validates V1–V5 against the actual persisted-result parser; 21 focused synthetic tests and scoped lint pass. Current research panel still hashes to its frozen `880cc1e51d7d22061fdd5b283795052b87b1425e391d41bf19438c86f274759e`. | Independent review and real sealed truth remain necessary; executable fixtures are not measured card accuracy. |
| Real truth | Existing provider/source investigations and pilot preparations are useful development evidence. | No real sealed holdout/label package was supplied or found by scoped repository and private-investigation filename searches; current corpus construction found in tests is synthetic. Reviewer identities, real input manifest, guide, adjudications and labels remain missing. This is not a claim that every private file was inspected. |
| Shared reuse | Inventory has reviewed-publication/lookup/contribution contracts and optional consumers. | Actual reviewed pilot pins/images, ATLAS adapter admission/release and different-physical-card proof in both directions remain open. Incoming ATLAS status below supersedes old adapter assumptions. |

Initial source audit: evaluator SHA-256 was `6d98b3c7ed8623a4450ed2f9b24db2a0af72473fbf6843d44f674debb1cee347`; the frozen [schema-v1](../../../frontend/nextjs-app/scripts/staff-comp-holdout.schema.json) remains `2945cf31153502bcfe3fb5f8bb8908f113b8094765974d5a850f7d5d857fefba`. The implementation's exact files and compatibility hashes are recorded below.

## Frozen denominator and acceptance rules

Retain [existing acceptance §5](../2026-09-16-intake-atlas-acceptance-next-slice.md) and the [approved measurement plan](../2026-09-15-variant-and-sold-comps-improvement.md). These are evaluation rules, not an expansion of product authority.

- **200 unique physical cards**, 50 each in Sports/Pokémon × known-family/new-set; at least 20 independent families. Include raw/graded, ordinary/rare/numbered and look-alikes within both categories. Freeze enrollment without selecting for available sales. Disclose this balanced population and natural intake proportions; do not imply that balanced accuracy equals production-weighted accuracy.
- Freeze exact Front/Back hashes, physical/family identities, input revisions, source/reference bank, reference/image/listing/certificate lineage, build/model/settings and provider tapes. Freeze a **180-day whole-UTC-day window** before evaluation. New-set reference acquisition must use independent sources, with its bank sealed before recipient scoring. Donors, tuning cards, relisted photos and shared source roots cannot become independent holdout evidence.
- Two actual reviewers independent of candidate tuning supply exact/partial/unknown identity, printing/language/edition, grader/grade or defined raw-condition band, independent market availability, and candidate sale/date/final-amount/independent-sale labels. Adjudicate and seal before revealing model decisions. Candidate pools may be prepared blind; labels cannot be inferred from the model's selections or provider's claimed eligibility. Keep unknown truth and unavailable sales explicit.
- **Headline numerator:** a card has at least one independently correct, same-variant, compatible-condition sold attachment within the window; every confident identity/condition assertion is correct; no false sold or verified-price assertion. Audit the union of persisted `matched` comparisons and estimate selections, maximum **24** candidates; never trim errors after evaluation. Possible/tentative candidates do not count. The scorer audits sold/price claims across the retained pool, including nonselected cards.
- **Denominator:** all enrolled cards, including unknown/no-sale, ambiguity, abstention, image failure, timeout and provider failure. A market-available subset is diagnostic only. A valid active/unknown-sale research match is not sold coverage. Unknown offer price is not a verified amount; an estimate is not required for correct-comp coverage.
- Report category/cohort counts, raw/graded/rarity/look-alike results, confident assertion and sold-assertion precision, variant correctness/unresolved, failures, and verified-value coverage separately. A verified value additionally needs at least two independent eligible final USD sale amounts and correct displayed mean/range/count; listing IDs or different crops alone do not prove independent sales.
- Broad **>90%** qualification requires observed all-card coverage >90%, the smaller of two-sided 95% Wilson and stratified family-bootstrap lower bounds >90%, observed confident assertion precision ≥98%, the family/representation gates, and a nonsynthetic independently audited corpus. Retain bootstrap seed `20260916` and 10,000 resamples. Otherwise report measured results/inconclusive; no holdout expansion or tuning after misses. A two-card phone check or catalog pilot cannot satisfy this gate.

## Smallest scorer and labeling deliverable

1. Implemented for review: separately named `inventory-v1-v5-ui-union-20260921` mapping and schema-v2 path. Historical `inventory-v1-v3-ui-union-20260916` and schema-v1 retain their generated bytes/score behavior and reject V4/V5. Both modern paired arms pass the actual strict persisted-result parser, including older results, so a V3 name cannot disguise V5 evidence. V4/V5 results preserve their canonical bytes; result hashes, held-out photo roots, V5 candidate/search/detail/request hashes and timestamps, V4 publication/scope receipts and unknown/current availability are checked before scoring.
2. Freeze the reviewed mapping, labeling guide, private create-new manifest and reviewer evidence before the first measured run. Audit real review receipts: the offline scorer validates declared identities/times/hashes but does not authenticate reviewers, provider responses or live catalog currency. `evaluatePair` now requires identical complete human labels/evidence for common candidate IDs under the same tape, including absent-versus-present labels. Distinct candidate pools may still be scored, and their common candidate truth cannot change between arms. This intentional paired-input correction applies to both mappings.
3. Assemble the real 200-card manifest and blind label package without creating synthetic live stock. Begin with already authorized, retained inputs only when physical identity, original hashes and review provenance are actually available. Record missing strata/labels instead of manufacturing entries. Keep phone/pilot/development examples outside the holdout if they influenced tuning or its reference bank.
4. Run paired baseline/candidate scoring only after the mapper, labels and candidate qualification are complete. Preserve identical physical inputs, truth, reference bank, provider tape and model settings. Report gains/losses; it is not a superiority test. Any later fresh-provider study has its own dated evidence and spend bounds. No provider work or real-corpus score is executed by this packet.

## Next independent load delta

The [comp lane's three-input V5-on default-cron fixture](comp-qualification.md) must first establish durable enabled wiring. That lane owns it; do not duplicate the completed V3 fixture. Then prepare a **new feature-delta protocol**, not another pre-repair-versus-current study:

- Pin the same final reviewed application in both arms; A has sale details off, B on. Keep catalog/contributions/full-resolution off. Use missing/null-offer sold search tapes with exact `/v1/item` responses, plus detail timeout/429 tapes. Existing full-matrix tapes explicitly report no offer and would trigger **zero** detail requests; they cannot qualify this stage.
- Reuse separate driver/intake/worker/observer processes, 4/3/1 pools, two workers, identical seed/history/photo bytes and one/three intake sessions. Freeze idle, ordinary-detail, detail-timeout and detail-429 cells with ABBA, five warmups and 50 measured saves per session/block: 32 blocks, 3,200 measured + 320 warmups, 90-minute cap. This is only the newly activated load difference. Do not launch until exact code/config/tape/client/source seals and a local quiet window are reviewed.
- Preserve all six existing p95 segment gates, pooled and each repetition: B−A ≤ `max(50 ms, 10% of A p95)`; B loaded−B idle uses the same margin against B idle. Independently recompute raw samples, observer/clock/report hashes and loaded overlap. Missing overlap, no actual detail requests, missing samples or incomplete cells means invalid/inconclusive, never pass.
- Hard gates: no extra intake provider call/mandatory step, no added intake error/retry under identical injections, one durable research job per accepted save, none on failure, exact replay without duplicates, unchanged staff edits/cost/channel/originals, bounded two-ID detail cache/deadlines and complete terminal accounting. Keep existing three-lease admission pause/resume and source integrity checks. Background throughput cannot compensate for a slower intake segment.
- Use owned disposable local PostgreSQL and synthetic external transport only. A separate enabled default handler fixture and later hosted/device evidence remain necessary. Qualify catalog or full-resolution changes with their own genuinely exercised off/on tapes and scoped load delta when proposed for activation; do not bundle currently unqualified flags. Preserve prior reports and failed runs.

The owner phone confirmation closes basic real-phone behavior; no additional exact deployment/device receipt was supplied in this lane. A no-slowdown claim under simultaneous staff/provider load still needs the separately preregistered paired phone/hosted observations; DOM timing and this local matrix cannot supply them. Root owns release checks without reopening the accepted phone gate.

## ATLAS interface and two-way proof

**Incoming verified ATLAS-lead report relayed by root on September 21; not independently audited by this lane:** deployed `406466a0`, documentation HEAD `3f4de690`, supports identification V2/new and V1/historical. Its connected-manual runtime has **no catalog transport, research or source-review adapter** and cannot adopt V4/V5 research or source-fact-review V2 yet. Previously reviewed/unadopted catalog baseline is `945812ca` with service/lookup/manifest/proposal v1 and optional V4. Reported source references: `connected-manual-runtime.mjs:48`, `identification.mjs:9`, core `contracts.mjs:59`. Mark saw initial Astra suggestions; correction/learning/final-report acceptance remains open.

Root can hand off a narrow, frozen compatibility packet now: exact Inventory base/result commits and file hashes; V5 with V3/V4 bases and source-fact V2 envelopes; retained V1–V3/source-review legacy fixtures and unsupported-version rejection; real reviewed sports/Pokémon publication/current/history pins with source hashes, explicit coverage/unknowns/revocation and separate image-use grants; intended origins/scopes/private-media readiness. No credentials are needed for this packet. ATLAS owns authenticated transport, durable admission/raw-response acknowledgment/paid-work accounting, adapter tests and release. A process-only scope receipt or package receipt is not adoption. Do not merge whole branches or change its checkout.

After ATLAS's own initial manual grading acceptance and qualified adapter release, use **four separately captured physical cards**:

1. **Inventory → ATLAS, sports:** donor S1 contributes an unreviewed observation; authorized source-bound review publishes reusable facts/diagnostics. A different physical recipient S2 within explicit supported scope consumes that exact pin through the actual ATLAS adapter and receives the supported suggestion.
2. **ATLAS → Inventory, Pokémon:** donor P1 contributes through ATLAS's authenticated producer path; separate review publishes exact printing/language/edition/applicability and depicted-versus-representative evidence. Different physical recipient P2 consumes it in an ordinary after-save Inventory attempt. Neighboring excluded/unknown printing remains unresolved.

Each receipt binds safe donor/recipient identities and independent image roots, input revisions, human approval/source/media hashes, current/history publication pins, producer replay/conflict outcome, consumer attempt/version, actual evidence used and resulting suggestion. Compare with a pinned before/without-reference result and preserve original images, human descriptions, costs and grade authority. Prove stale/revoked consumption fails and newer publication does not relabel history. Execute revocation/conflict controls in isolated qualification evidence, not by altering live published truth for a test. No donor image/listing loop may corroborate its own guess. Until both directions pass, report Inventory pilot status and cross-app reuse **pending**.

## Scorer implementation and frozen compatibility delta

The code change is local and uncommitted by this lane; root supplies the eventual reviewed result commit. Source-contract readback during implementation was at root's `f3563a9490c924f56fa43c9ff266defb818673e5`. Shared-contract files were not edited. V5's source change is already present in `bc7c55d5b0e54f1f8dbf479d66103f7ecc9fdb0c`; source-fact V2 is already present in `134c9df34a18f35aab720d57ca6bcf89245f8428`. These identifiers describe Inventory source; they do not imply ATLAS adoption.

| Wire boundary | Frozen compatibility |
| --- | --- |
| Research result | Envelope `schema_version: 1` stays unchanged. Engine V5 adds `sale_details.schema_version: 1` with explicit V3 or V4 base and at most two exact request receipts. Optional candidate `ordinary_sale_detail.schema_version: 1` preserves search/detail strings, source hashes, timestamps, same item, USD ordinary amount, ended date and sold banner. Missing/unknown offers, active evidence, price ambiguity and conflicts do not become verified amounts. V5/V4 base also requires all existing catalog pins, coverage and printing-scope checks. Unsupported versions reject; legacy fields are not defaulted into historical results. |
| Source-fact review | `setops-catalog-review-evidence/v2` emits `setops-catalog-verification/v2`. Each source uses `factUse: {purpose: catalog_facts, sourceSha256, detail, consumers}` with exact source hash and Inventory/ATLAS roster. Images retain separate unchanged grant records. Full canonical verification hash and human publication authority remain required. Legacy unversioned source-grant review and stored verification/v1 keep their original interpretation and bytes; retained v1 golden verification SHA is `a988de570ef4929ba6cfe04c910a1f9acdaca1931ff3e2d6bda62c2ba9a2c537`. Unknown/mixed schemas, hash/roster drift and source-fact-as-image-grant substitution reject. |
| Shared catalog transport | `card-catalog-service/v1`, `setops-catalog-evidence/v1`, `catalog-evidence-lookup/v1`, `catalog-observation-proposal/v1` remain unchanged. V2 fact use grants no original-photo/source-PDF distribution or grading-reference authority. An older v1-only verification reader must not serve a new v2 publication. |
| Offline study | New corpus `schema_version: 2` uses `inventory-v1-v5-ui-union-20260921`, exactly 200 cards and the unchanged balanced strata, union assertions, price audit and confidence gates. CLI schema export is `--schema-v4-v5 NEW_SCHEMA.json`; `--schema` still emits the original bytes. Ordinary and paired scoring dispatch by corpus schema and mapping without rewriting inputs. Current mapping loads checked-in parser source using existing `tsx/cjs/api`; a stale workspace `dist` is not parser authority. |

Exact SHA-256 file seals, relative to the Inventory repository:

| File | SHA-256 |
| --- | --- |
| `frontend/nextjs-app/scripts/evaluate-staff-comp-holdout.mjs` | `d2ce6d2b4cf0aa2a763cc25d36c520100c9844e5769ed6ef272032d5e3cc4382` |
| `frontend/nextjs-app/scripts/staff-comp-holdout-v4-v5.schema.json` | `7a0a26d8971b79a59d1d250153413a45da03154880c6cddcc2a3a7af5adc2382` |
| `frontend/nextjs-app/tests/staffCompHoldout.test.mjs` | `2c86198f4f59a18786203e0614a63fc82591e8402e820c913ebd8919e0db4a37` |
| `packages/shared/src/staffInventoryResearch.ts` | `efab0b667e1d910bbfc6e259fed4c8f9ae7873083eb99ef90f1dca2140a526ec` |
| `packages/shared/src/staffInventoryResearchSaleDetails.ts` | `3e729dc2b88323d2d886f920650f2a5e3f4c022f96d0a66530243eac836578f8` |
| `frontend/nextjs-app/lib/server/setCatalogEvidenceMedia.ts` | `f94bc723ee6e779f2b3af8f087db1dd74b8df628a77860d4dba537fc24abcd86` |
| `frontend/nextjs-app/tests/setCatalogSourceFactUse.test.ts` | `4bd1753e01f760506e19455fb8d2966cbed64a4acb8d0b5e690758b1f7c06fa3` |
| `frontend/nextjs-app/tests/staffInventoryResearchCompatibility.test.ts` | `2c0fb66971792222d02fa7d9f6698456433b994cd917aac91c8343d08628c5ac` |
| `frontend/nextjs-app/tests/staffInventoryResearchSaleDetailsIntegration.test.ts` | `a980250d5e65da1b5938f73d92161d54745f0888909ca56f3dc45fd6c8e1c332` |
| `packages/card-catalog-evidence/src/index.mjs` | `626fa80afd81138af2258a5fb52673ad1de65a061ed94904de705bfcb5d2b4eb` |
| `packages/card-catalog-evidence/src/index.d.ts` | `c94a5780dbb484649d071eb5f50a40b795ed57cb9d420494d520d91df00d9305` |

Fresh verification: qualified Node `22.23.2`, **21/21** scorer tests, `node --check` and scoped Next lint all pass. The first parser-loading run correctly failed because generated shared `dist` supported only V4; loading checked-in source resolved that mismatch without rebuilding. The first lint invocation used the repository root and found no Next pages; rerunning from the app directory passed. No real corpus was scored, no labels were supplied, and no >90% accuracy claim is made. Existing source-fact and research compatibility suites above are executable delivery fixtures and standing prior proof, not fresh runs by this lane.

Independent review remains requested through root. Real reviewed publication pins/images, independent labels and ATLAS-owned adapter/admission/release remain outstanding. Product acceptance remains contingent on those distinct evidence boundaries; no new approval of the overall plan is requested.
