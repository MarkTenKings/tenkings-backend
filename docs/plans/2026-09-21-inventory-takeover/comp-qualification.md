# Inventory V5 comp qualification — next-action packet

## Current delivery — September 21, post diagnostic

The V5 default-cron persistence fixture, exact-input diagnostic implementation and independent offline review are complete. Root qualified exact source `2c8e0554f847a11ead58ebd1caa87b0262bae234` on the main Preview and submitted one existing Snivy input through its normal human session. The terminal result completed in 96.6 seconds with two searches, two details, two Astra requests and nine candidate-image reads. One detail timed out at its six-second bound; it was retained as failed and not retried. No wrapper limit violation occurred.

The result retained 24 candidates, including one ordinary sale with matching search/detail evidence at $1.08 and one provider-hydrated accepted offer at $1.55. These are price-evidence observations, not selected exact comps. It selected **zero**: printing/treatment remained unresolved with zero legacy references, and it returned **unknown value**, not a fabricated estimate. This one development card is excluded from the independent holdout and does not establish general accuracy or improved coverage. The saved completed research job's complete-row hash is identical before and after.

Private terminal receipt `investigations/20260921-exact-input-preview/snivy-terminal.json` is 512235 bytes, SHA-256 `c6fb1b0ccfcc848177330f0d5dd188c030b57abe5015ca209fb977edc0d1f41b`. Canonical envelope/plan/result and fourteen retained blob hashes independently verify. Actual originals were byte-verified inside the server invocation and remain bound to their immutable managed keys; independent complete wire-request reconstruction requires those original bytes, which are not duplicated in the local bundle. Candidate-image/template structure can be checked offline without another storage/provider call. Both returned models are `gpt-6-astra`; recorded usage is 16,395 input plus 4,881 output tokens. Billing remains unavailable; returned model usage is not an invoice. All nine downloaded candidate images are at most 225 pixels on the long side; 15 final candidates are unimaged. Neither price observation is an ordinary fixed-price proof.

Root set the exact Preview execution flag false and qualified the same-source shutdown build `dpl_7Kbmt1fWpzWPuLVe6Ue3Xo7Pgiqm` (`mo4vommjs`). The normal alias command now assigns the stable main Preview to it; the final independent alias/guard receipt is indexed in the [current index](README.md) and append-only session log. Production remains `9a12cd35` with all optional research enrichment off. Next useful work is genuinely reviewed catalog/printing evidence, independent positive/control labels and the separate actually exercised optional-load qualification. No additional cohort or automatic retry follows from this unknown result.

## Retained initial audit and design history

The dated sections below preserve the evidence available at each earlier stage. Their former “next,” “unclosed” and “design only” labels are historical; completed fixtures and hosted diagnostic evidence above supersede them. Owner-confirmed phone acceptance is closed.

September 21, 2026. Read-only audit at `297fd302b866286824fcea79b8f77c6d995ca3db` in `/Users/markthomas/tenkings/codex-staff-inventory-release-20260910`; application bytes remain `e286ea0a190bfdc6807fe8ca58c47b34c94b55c1`. No implementation, provider request, database connection, UI action, environment change, deployment or ATLAS action occurred. Only this packet is owned by this lane; integration/session log belongs to the lead.

**Next: qualify V5 through the real default cron and durable writer on disposable local PostgreSQL, with synthetic external transports.** Preserve the completed provider study and default-off timing matrix. This closes a specific untested connection before spending more provider calls. Enabled hosted behavior, real-card accuracy and enabled-load acceptance remain separate gates. The greater-than-90% all-card objective is unproven.

## Evidence that stands

| Evidence | Result and limit |
| --- | --- |
| Current handoff/state | [Handoff §2, §5–6](../../handoffs/2026-09-21-ten-kings-inventory-astra-ultra-handoff.md) records READY Inventory Preview `dpl_7xc2akyMGziEALNz3aCZFvWsnPCs` at application `e286ea0a`. Sale details, full-resolution and catalog consumers remain absent/off. This audit did not refresh hosted configuration. |
| Real provider observations | [September 17 report](../2026-09-17-sold-comps-provider-observations.md): four searches, four details, eight image reads; no model. Of 25 sold rows, 24 omit offer status. Exact details confirm two ordinary sales; accepted offer remains unknown; active detail's `ended:true` is insufficient. Execution was disabled after the four checks. |
| Fresh archive verification | Recomputed all four report hashes and `MANIFEST.json` under `/Users/markthomas/Library/Application Support/TenKingsInventory/investigations/20260917-live-provider-qualification`; all match the report. Manifest SHA-256 `21b289fe4ba11aa52bf3658895593a37d78c5db4a3d8e19e5cb76b88ffa256f2`. |
| Offline V5 checks | [SESSION_LOG September 17](../../handoffs/SESSION_LOG.md), line 33422: 113 affected checks, including 16 engine integration cases, passed. Tests cover controls, duplicate reconciliation, timeouts, failure caching, late mutation, V3/V4 compatibility and V5 parsing. They are not live accuracy evidence. |
| Controlled timing | [Full matrix](../2026-09-17-intake-full-matrix-results.md): 3,200 measured + 320 warmup saves; all frozen pooled/repetition/headroom/integrity gates passed. Optional details/catalog/full-resolution were off. Earlier failed receipts remain retained. |
| Default cron wiring | [Functional result](../2026-09-17-default-research-cron-functional-fixture.md): real default handler → worker → engine → PostgreSQL passed, but explicitly V3/details-off. No rerun of this completed configuration is needed. |

The positive exact items are Bo Bichette `377468246486` ($89; search `89`, detail `89.0`) and Snivy `318826793399` ($1.08). Both retained search observations identify **auction** sales. The archive contains no ordinary fixed-price positive; the active fixed-price control `117403547405` is not one. Accepted-offer control `177869917659` remains unrealized. The parallel cohort's downloaded primary belongs to another independently recorded listing, not its accepted-offer detail. No sold-image byte archive/model comparison was produced: 500-pixel decoded dimensions do not prove readable foil/variant cues.

## Code authority and remaining gates

References below are relative to this checkout and the audited application commit.

- `frontend/nextjs-app/lib/server/staffInventoryResearch.ts:229–315`: only eligible exact sold rows missing/null offer status enter detail enrichment. Original facts survive; conflicting duplicates remain excluded. At most **two distinct item IDs per engine attempt**, shared across all three searches, with failure caching/no detail retry. Calls can overlap two at a time; each gets at most six seconds inside the existing 20-second source round with return reserve. `:607–621` selects V5 only for `STAFF_INVENTORY_RESEARCH_SALE_DETAILS=true` and records its V3/V4 base mode.
- `packages/shared/src/staffInventoryResearchSaleDetails.ts:101–146`: confirmation requires exact IDs/title, agreeing integer USD cents, explicit detail no-offer, complete matching date/positive sold banner and no active/offer/range/hydration conflict. This grants price evidence, not variant or final match authority.
- `frontend/nextjs-app/lib/server/staffInventoryResearch.ts:517–538`: final selection still requires supported identity/reference evidence, matching target condition, assessed image and at least two listings with distinct image hashes. Thus two recovered ordinary prices alone cannot establish an estimate.
- `packages/shared/src/staffInventoryResearch.ts:239–274` binds V5 candidates to exact detail receipts and retains older readers. Once V5 persists, disabling enrichment must keep this reader; never downgrade saved evidence.
- `packages/database/src/staffInventoryResearchV2.ts:13–16` allows three attempts per run and nine lifetime attempts. The two-detail cap can therefore become **six per run / eighteen lifetime**; it is not a per-job spending guarantee. The default worker also claims the oldest queued eligible job (`:174`), so calling the normal hosted cron is not an exact-card qualification tool.
- **Unclosed durable V5 gate:** `frontend/nextjs-app/tests/staffInventoryResearchCronPostgres.test.ts:14–36,194–199` requires optional flags absent and V3. V5 integration `staffInventoryResearchSaleDetailsIntegration.test.ts:287–330` rejects malformed results before DB access and uses a simulated reader for valid readback. No real V5-on default-cron persistence proof was found.
- **Unclosed live gates:** same-card positive/control labels, actual model/image decisions, ordinary fixed-price evidence, hosted V5 request/persistence behavior and changed-background-load responsiveness. Catalog-enabled V5/V4 requires its own real reviewed publication pins and useful reference evidence. Do not combine all optional flags into the first qualification.

## Minimal next experiment: one new V5-on durable fixture

Extend the existing disposable cron fixture/runner narrowly; retain the original V3 run/receipt. No runtime seam or capacity increase is needed. The lead owns the implementation/review and one sealed launch; this audit has not changed or launched it.

1. Pin current source, generated-client compatibility and dependencies in a fresh private preparation receipt. Reuse the existing loopback-only runner and 10-minute supervisor/cleanup bound. Set only sale-details true inside its synthetic child environment; catalog, contributions and full-resolution stay off. No hosted/shared database, real provider or external storage access. Preserve all prior archives.
2. Use exactly **three synthetic quantity-one inputs and three owned claims**, without research retries: (a) two independently imaged, exact ordinary missing-offer sales with supported synthetic identity, producing a V5 estimate; (b) protected wrong-card/parallel/grade/raw-condition, accepted-offer and active controls, producing no incorrect confident match/selection or sold/price assertion; (c) missing-offer candidates whose two details return controlled 429/timeout, with valid repeated search refinements, producing durable unknown evidence rather than an optional-detail-induced job retry. Assert the actual default worker/writers, not substituted dependencies.
3. Hard ceiling across the fixture: **zero live calls**, three engine attempts, nine synthetic searches, six synthetic detail calls, nine synthetic model responses, 36 synthetic candidate-image reads. Each attempt retains production deadlines and the two-ID cache. Freeze expected exact per-case counts before launch; ceiling compliance alone is insufficient. No extra claim may be made to obtain a passing case.
4. Require canonical V5 persisted bytes/result hash/readback agreement; original missing/null flags and exact search/detail amounts retained; no source-history drift; controlled failures cached across refinements; no late mutation; source/variant/condition controls fail closed. Turn enrichment off only in the fixture process and re-read the stored V5 unchanged. Record actual counts, terminal states and cleanup. Any unexpected transport, claim, false assertion or cleanup failure fails the run.

This is a functional gate, not a repeated performance study or measured real-card improvement.

## Smallest later live step and release sequence

After that fixture passes, prepare **one exact-input, one-attempt server-side V5 diagnostic** using the existing legitimate server secrets and normal human authorization. The current four-preset provider probe does not invoke the engine/model and cannot supply this proof unchanged. Do not use hosted cron/retry to select arbitrary existing jobs, export secrets, or enable the branch-wide flag merely to run a sample.

Freeze one real card's complete input/photo hashes and an independently reviewed known-positive/control tape first. Reuse archived controls for paired decision comparison. For the single fresh run cap actual calls at **three searches, two details, three Astra requests, twelve candidate-image downloads**, plus two exact original-photo reads and at most four existing reference-image reads; no extra listing views, full-resolution fallback, automatic retry, live source write or catalog publication. Persist only private diagnostic output. Receipt hashes, original provider/model payloads and decision stages must be retained privately for a true same-payload comparison. Request counts are not verified billing units. A missing positive or fixed-price case is inconclusive, not permission to keep searching. Select a fixed-price ordinary case if one is already evidenced; both existing positive auctions need no repeat provider probe.

Then qualify only the new load delta: same candidate application with detail flag off/on, one/three intake sessions, ordinary-detail and bounded 429/timeout load, using the existing frozen ABBA margins (each relevant p95 increase ≤ max(50 ms, 10%), zero added errors/duplicate jobs). Coordinate with the intake lane; do not rerun the entire completed base-versus-candidate matrix. Hosted V5 durable execution and staff phone acceptance still precede operational activation. Preserve V5 readers, one cron owner, private auth/storage and migration-disabled deployment. The lead must record exact planned/observed release actions.

## Independent work and genuinely missing inputs

Can proceed now without Mark repeating approval: prepare the targeted fixture, freeze controls/receipt format, align the enabled-load protocol with the intake lane, and review legacy/V5 readers. Sports/Pokémon publication preparation, phone acceptance and domain work remain separate workstreams. ATLAS owns its adapter/release; no ATLAS edit or takeover is needed here.

Missing evidence is **human truth and a suitable real input**, not generic build permission: confirm exact card/printing/condition and which candidate images are independently correct; identify a real ordinary fixed-price positive if available. The lead should first inspect the existing private corpus and obtain only labels genuinely absent there. An owner-provisioned local key is unnecessary if a reviewed server diagnostic can use the normal existing secret mechanism. Provider billing/account quota must be checked by the authorized lead before interpreting request caps as spending limits.

The accuracy lane owns the independent 200-card holdout in [acceptance §5](../2026-09-16-intake-atlas-acceptance-next-slice.md): all cards stay in the denominator; assess the UI's union of matched assertions and selections, sold-event correctness and verified-value coverage separately. Neither this three-input fixture nor one fresh run establishes >90% all-card coverage.


## September21 implementation and isolated execution

Dedicated V5 test/runner siblings are implemented without changing the original V3 harness or application runtime. Root reviewed fresh seal `3e47594574ad695a812f54835667fed929496ef261dc5f54ba85b87db9632565` and ran it once. `/private/tmp/tenkings-v5-cron-run-20260921-01` passes:3saves/claims/durableV5completions, exact5search/4detail/5model/10candidateimage synthetic calls,0realproviders,8560ms total. Wrongcard/parallel/grade/raw/acceptedoffer/active controls, actual6secondtimeout/429cachedacross3searches, late-response immutability andV5flag-offreadback pass. Fullmigration/secondnoop are confined toownedloopbackPG. Cleanup is complete and everyownedPID absent. Independent review foundnoactionablefinding. Scopedlint/types/syntax and29existingV5enginechecks pass.

The next gaps are a boundedexact-input hosted diagnostic and qualification of actually exercised additionalbackgroundload. This functional synthetic fixture makes no performance, real-providerquality orall-cardaccuracyclaim.

## Hosted exact-input diagnostic — design for lead review, September 21

Inspected at `f3563a9490c924f56fa43c9ff266defb818673e5`. **Design only: no new endpoint or runtime edit, provider call, database connection/write, storage write, flag change or deployment.** The lead requested review of these boundaries before implementation. The completed V5 fixture above closes the old durable functional gap; the earlier audit text records the state before that execution.

### Proposed scope and owned files

Use one server-configured existing Inventory unit as the fixed cohort, not arbitrary client descriptions or queued-job selection. Run the existing pure engine once with V5 enabled only in its injected environment; full-resolution, catalog scope/model, contribution and archive writers remain absent. The output is a private diagnostic, never a research-job completion, Inventory event, SetOps publication or live comp.

Proposed new files, after review:

- `frontend/nextjs-app/lib/server/staffResearchExactInputQualification.ts`: strict plan, read-only input selection, verified-photo preflight, pure-engine wrapper, counters, bounded evidence capture and receipt recovery.
- `frontend/nextjs-app/pages/api/v2/admin/inventory/research-qualification.ts`: human-admin plan/run/status handler; default off; exact Preview-host and same-origin checks.
- `frontend/nextjs-app/components/admin/StaffResearchExactInputQualification.tsx`: a panel in the existing qualification page, with exact-input review, one acknowledgement and no automatic retry.
- New focused service, API and UI tests. Minimal edits only to `pages/admin/inventory-research-qualification.tsx`, `lib/siteRoutes.ts` and their affected tests to mount/allow this panel/route. No worker, engine, database/shared schema, original fixture, runtime flag default or storage-helper change is proposed.

Existing seams support this separation: `lib/server/staffInventoryResearch.ts:606–613` exports the pure engine and selects V5 from injected environment; optional effects are dependency inputs. `pages/api/v2/admin/inventory/provider-qualification.ts:22–39` already supplies current human-admin, no-store, default-off and same-origin checks. `lib/staffResearchQualificationHost.ts` binds the exact configured host to `VERCEL_BRANCH_URL` only in Preview; `lib/siteRoutes.ts:99–103` currently allows only the old page/probe there. The old probe does no model work and has `maxDuration:60`; it cannot qualify this engine unchanged. Proposed new execution envelope: 15 seconds preflight, at most the engine's existing 150 seconds, 15 seconds terminal receipt, within a verified 180-second hosted-function allowance. A shorter deployed allowance is a failed preflight/configuration gate, not a successful partial run.

### Frozen plan and verification before spending

1. The server-owned fixed-cohort configuration pins one `unit_id`, current research `job_id`/`input_hash`, description event/hash, expected front/back managed keys and SHA-256 values, application/deployment identity and a run label. Until a suitable existing physical input is selected and this configuration is complete, execution is unavailable. Selection is read-only; no synthetic stock or new research job is needed. The browser cannot substitute another unit or photos.
2. The authenticated plan reads exactly that current, non-superseded saved input, parses the shared input schema, recalculates its canonical hash, and verifies unit/description bindings as the database reader does (`packages/database/src/staffInventoryResearchV2.ts`, `verified` and `JOB_COLUMNS`). Include exact source/reference snapshot hashes and missing-reference status. Freeze the current read-only legacy reference result; do not make the first diagnostic depend on concurrent catalog publication. Plan SHA covers every binding, limit, model/config setting, billing basis and receipt destination policy. Show the card facts, two existing thumbnails, photo hashes, expected result/control labels when available, and the complete capped-call plan before the typed acknowledgement. Plan GET performs no provider/model call or evidence write.
3. POST requires the unchanged plan SHA/config acknowledgement and a client-generated invocation UUID retained before transmission. Re-read the selected input immediately before execution. Load both originals using `readStaffInventoryPhoto` (`lib/server/staffInventoryIdentification.ts:79–110`): exact managed key, bounded original bytes, content hash and JPEG dimensions, not filename-only trust. Fail without a search/model call if either differs or cannot be verified. Supply those already-verified bytes to `loadPhoto`; the engine otherwise starts searches concurrently with photo verification (`staffInventoryResearch.ts:639–665`). Supply the frozen read-only references in memory. Recheck current input after execution and mark a changed input `stale_input`; the receipt remains evidence of the frozen old input, never authority for the changed one.
4. Permit only a current normal human Inventory admin, exact qualified Preview host (loopback only for offline tests), JSON and same origin; reject service/operator credentials. Require a distinct default-off qualification flag; do not activate the normal worker's V5 flag. Rate/cooldown and client pending state are convenience controls only. No unattended cron, retry button, automatic retry or automatic second cohort.

### Enforced limits and cost meaning

The wrapper counts **dispatches before network I/O**, including failures/aborts, and independently rejects a fourth search, third detail, fourth model request or thirteenth candidate-image request. Only the engine's exact Sold-Comps search/detail endpoints, OpenAI Responses endpoint and validated provider image URLs are allowed; redirects, additional listing-page reads and unknown destinations fail closed. Retain existing per-response, decoded-image and deadline limits. No catalog scope model or reference-image download is enabled in this first V3-base/V5 run; current legacy references have `image:null`. Thus the maximum is **3 searches + 2 details + 3 model requests + 12 candidate images**, with two preverified original reads. Omitting `archiveCandidateImage` avoids ordinary candidate storage writes.

Model request admission verifies `gpt-6-astra`, medium reasoning, `store:false`, no tools and `max_output_tokens:6500` against the actual serialized request (`staffInventoryResearch.ts:465–487`); total configured output ceiling is 19,500 tokens. Keep requested and returned model identifiers separately. Record provider request IDs, HTTP status, elapsed time, actual response usage, candidate/source IDs and failure phase. A rejected/uncertain model request can still be billable; reserve its full allowance and never call absent usage zero.

**Request caps are enforceable now; a defensible numeric dollar ceiling is not present in the inspected code or provider archive.** The plan must label request/credit limits separately from dollars. Before execution the authorized lead can attach the actual account pricing/credit basis and model maximum-input bound to the reviewed configuration, without exposing credentials or adding a database migration. Conservative admission is `3 × search_unit_ceiling + 2 × detail_unit_ceiling + 3 × (model_max_input_tokens × maximum_input_token_rate + 6500 × output_token_rate)`, plus explicitly bounded storage/egress where charged. Use the larger uncached/cached input rate and include reasoning within the provider's output billing definition. Do not invent a model context/tokenization rule, treat repeated images as free, or infer billed cost solely from successful result usage. Admit each dispatch only if its worst-case reserved charge fits the configured total; reconcile returned usage separately. If the account/model charging bounds cannot be substantiated, the page may show a bounded-request plan but must not claim a hard dollar cap or an execution-ready cost seal. A billing dashboard/account quota is separate evidence, not a process-local counter.

### Durable evidence authority and uncertain responses

Use existing private checksum storage, not a new application table. `lib/server/storage.ts:862–923` already supports private, SHA-256-checked PUT and bounded reads exist at `:533`; it does **not** provide conditional-create/atomic once-only claims. Two private objects under a fixed diagnostic prefix derived from plan SHA, actor and invocation UUID are proposed: an initial receipt before paid dispatch and a terminal bounded evidence bundle. They are the only shared writes. The initial receipt includes the acknowledged plan, verified input/photos/references and full reserved ceiling; verify its successful write before proceeding. The terminal bundle records completed/failed/aborted status, all dispatched call receipts, uncertainty, exact normalized result and its canonical hash. Its deterministic invocation location allows authenticated read-only status recovery even if the HTTP response disappears. Hash every retrieved object and bind it to the expected actor/plan/invocation; reject path substitution. Ordinary UI research/job readers never consume these objects.

Retain actual search/detail response bytes, raw model response/usage, candidate image bytes with original/transmitted hashes, bounded reference snapshot and exact model-request reconstruction data. Original images remain referenced by their already-verified immutable managed key/hash, rather than copied into a new stock/media object. Preserve request-body SHA and a reconstruction check using stored text/settings plus the exact image bytes/hashes, so offline replay compares the same evidence rather than regenerated URLs or current provider results. Do not retain authorization headers, query credentials or secrets echoed in bodies; fail closed on such echoes and record an explicit redaction/evidence gap. Proposed aggregate capture ceiling: 8 MiB source/model transcript, 24 MiB candidate images, 1 MiB result/metadata, final encoded bundle at most 48 MiB (below the existing 50 MiB storage bound). Exceeding any bound aborts further dispatch and produces an incomplete receipt, never silent truncation presented as complete evidence.

Store the invocation UUID/pending state in the browser before POST and keep it across reloads for that actor. A timeout/disconnection offers **read-only receipt recovery**, not another run. A terminal receipt can be downloaded and verified; an initial-only receipt means completion, payload retention and final charges are unknown. Absence of a receipt after an uncertain POST also does not establish zero remote work. Keep the full reserved cost in either uncertain case; the lead checks the provider/account request evidence before any separately acknowledged new invocation. No background recovery repeats providers.

This design enforces per-invocation bounds. The existing process-local lock and a read-before-write receipt check cannot guarantee one invocation globally or defeat concurrent replay across instances. Unique IDs, one fixed cohort, no automatic retry and explicit single-run operation limit accidental repetition; they are not an atomic quota. Do not add an unqualified conditional-storage primitive or new quota migration merely to claim otherwise. If a durable global one-shot guarantee becomes required, review that separately before execution.

### Offline acceptance and remaining real inputs

After design approval, use injected in-memory DB/photo/transport/storage adapters to prove: admin/host/origin/default-off rejection; arbitrary input/hash substitution rejection; both actual photo bytes verified before the first search; exact four transport counters including failure/abort and unexpected-destination denial; requested/returned model and usage provenance; source/model/image replay hashes; no business/candidate writer invoked; input changes marked stale; private receipt upload/capture failures; and reload recovery without provider redispatch. API/UI tests should cover an uncertain response followed only by status/download. Existing V5 engine tests and the completed disposable cron receipt remain the engine/persistence evidence; no PostgreSQL launch or performance rerun is needed to test this wrapper.

Genuinely missing execution inputs: one existing physical unit with exact current input/photo pins and independent truth/control labels; actual useful reference evidence (possibly absent); an already evidenced ordinary fixed-price positive if available; and substantiated billing bounds if a dollar ceiling is asserted. Inspect the existing corpus first rather than ask Mark to repeat known labels. A missing positive or unresolved identity is an inconclusive diagnostic result, never permission to spend another batch. Neither a hosted diagnostic nor the three-input fixture establishes greater-than-90% all-card coverage.

### Optional-detail load delta — preparation only

The old matrix cannot be reused by merely toggling an environment flag. `scripts/benchmark-staff-intake-split-role.ts:85–106` supplies `bestOfferAccepted:false` on every ordinary listing and implements timeout/429 only for search; details would never dispatch. Its engine environment at `:182` also omits the sale-details flag. `packages/database/scripts/lib/staffIntakeFullMatrixProtocol.mjs` freezes baseline `60572cec`, distinct baseline/candidate refs and the old 32-block matrix. Preserve that completed seal.

A later dedicated delta protocol should compare the **same reviewed candidate bytes** with detail off/on, with explicit missing/null-offer candidates that force exactly two detail dispatches per qualifying claim. Its only new load cells are ordinary bounded detail responses and detail-specific 429/actual six-second timeout, plus the same-study idle anchor, at one/three intake sessions. Keep full-resolution/catalog off, separated intake/worker/observer processes, existing claim concurrency, terminal/save overlap and clock/stock/job integrity checks. Retain the frozen pooled and repetition p95/headroom margins `max(50 ms, 10%)` for all six existing intake metrics. Freeze repetitions/sample count and exact nonzero detail counts before any future seal; do not borrow old idle measurements across artifacts or reduce samples after seeing results. This is a protocol delta for later review, **not authorization or a new goal to run PostgreSQL performance now**. No old full-matrix repetition is proposed.

## Exact-input implementation for source review

The lead authorized the implementation after the design above. New files are `lib/server/staffResearchExactInputQualification.ts`, `pages/api/v2/admin/inventory/research-qualification.ts`, `components/admin/StaffResearchExactInputQualification.tsx`, and four `tests/staffResearchExactInputQualification*` files under `frontend/nextjs-app`. Existing qualification page, `lib/siteRoutes.ts` and route tests have narrow panel/route additions. No engine, worker, database/shared schema, original cron fixture, business writer, normal feature default or storage helper changed.

The runtime configuration is strict JSON in **`STAFF_RESEARCH_EXACT_INPUT_QUALIFICATION_CONFIG`**, matching `ExactInputConfigurationSchema`: version, run label, exact deployed application SHA, one current job/unit/input/description revision, exact front/back managed keys and hashes, and nullable billing. Execution requires **`STAFF_RESEARCH_EXACT_INPUT_QUALIFICATION_ENABLED=true`**, the existing exact qualified main Preview host, current normal human admin, same-origin JSON and exact plan acknowledgement. Production apex/collect are not admitted; only `NODE_ENV=test` gets loopback. This lane changed no environment or deployment. Root identified the real saved Snivy input through a separate narrow read-only binding receipt; no fabricated stock or checked-in live pin/config was introduced here.

The lead explicitly clarified that **unavailable dollar pricing is not an execution approval gate**. This supersedes any earlier design wording implying a required dollar-cost seal. With `billing:null`, the UI and receipt say dollar cost unavailable and enforce request limits. Optional substantiated per-dispatch maximum charges plus a total in micro-USD are admitted by integer reservation before dispatch; failed/uncertain requests retain their reservation. No invoice or provider price is invented. Legacy matching reference records are shown as legacy observations; this diagnostic does not activate the publication consumer or claim publication pins.

One actual engine execution is bounded to 3 searches, 2 details, 3 model requests and 12 candidate-image reads. Originals are fully hashed/decoded and the initial private receipt is written/read-verified before the first provider dispatch. Source changes before spending fail; changes after work mark the terminal evidence stale. Any missing/corrupt/redacted capture or admission violation poisons further dispatch and makes the result failed. Normal retained HTTP failures remain distinct from tape-integrity failures. The wrapper records requested/returned model, exact request and response hashes, raw response/image bytes, usage, request IDs and request reconstruction text; storing reconstruction as text preserves original JSON key order through canonical receipt serialization.

The entire invocation has a **170-second budget with a 20-second terminal reserve**, within API `maxDuration:180`; individual stages and the unchanged engine's 150-second ceiling can only be shortened. Parent deadlines propagate into nested plan/recovery work and owned storage streams close on abort. Browser disconnection stops new paid dispatch while allowing failure evidence to persist. The final deployment's actual duration allowance must still be verified before a hosted run; source configuration alone is not runtime proof.

Private initial/terminal checksum receipts are the only shared writes. A terminal bundle can contain up to 48 MiB; authenticated recovery returns at most 800 KiB of metadata, an exact receipt hash and a 60-second signed private download URL. The plan is separately capped at 128 KiB and terminal summary at 768 KiB. The large bundle travels server-to-private-storage and is never a Vercel API response. This stays below the platform's documented [4.5 MB request/response limit](https://vercel.com/docs/functions/limitations). Failed link signing preserves truthful terminal status and offers another read. Browser storage retains only actor-scoped invocation/plan identifiers before POST; reloads and uncertain/malformed/mismatched responses offer read-only recovery, with no repeat-run control. Counters are per server execution; storage is not an atomic global claim, concurrent copies can overwrite a logical invocation receipt, and that limitation is explicit in the plan, summary and UI.

Offline verification exercises the actual pure V5 engine through this wrapper, not just a substituted research function. The complete synthetic case produces 1 search, 2 details, 1 model and 2 independently imaged candidates, with verified original bytes and initial receipt preceding every dispatch. It remains unresolved without independent catalog authority, correctly yielding no estimate. Additional tests cover all four dispatch ceilings, optional configured cost reservation, source/photo/revision substitution, credential echoes in body/header, capture poisoning, disconnect/deadline cancellation, receipt failure and actor/hash integrity, 47+ MiB private-bundle recovery with small HTTP metadata, auth/host/origin/default-off guards, and uncertain UI reload recovery without POST. Final validation receipts are in `/private/tmp/tenkings-exact-qualification-validation-20260921`; the lead owns commit, build, runtime verification and any single hosted execution.

Still unproved: actual original Snivy byte verification (performed only inside an acknowledged run), independent physical-card/printing/control truth, useful exact reference coverage, ordinary fixed-price positive coverage, real model decisions, hosted evidence delivery, changed-load responsiveness and greater-than-90% all-card coverage. Root's fixed input pin removes the missing-input selection gap; it does not close those evidence gaps. No extra cohort, provider probe, performance study or automatic retry is authorized by an inconclusive result.
