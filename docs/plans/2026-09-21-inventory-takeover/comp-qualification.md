# Inventory V5 comp qualification — next-action packet

September 21, 2026. Read-only audit at `297fd302b866286824fcea79b8f77c6d995ca3db` in `/Users/markthomas/tenkings/codex-staff-inventory-release-20260910`; application bytes remain `e286ea0a190bfdc6807fe8ca58c47b34c94b55c1`. No implementation, provider request, database connection, UI action, environment change, deployment or ATLAS action occurred. Only this packet is owned by this lane; integration/session log belongs to the lead.

**Next: qualify V5 through the real default cron and durable writer on disposable local PostgreSQL, with synthetic external transports.** Preserve the completed provider study and default-off timing matrix. This closes a specific untested connection before spending more provider calls. Enabled hosted behavior, real-card accuracy and enabled-load acceptance remain separate gates. The greater-than-90% all-card objective is unproven.

## Evidence that stands

| Evidence | Result and limit |
| --- | --- |
| Current handoff/state | [Handoff §2, §5–6](../../handoffs/2026-09-21-ten-kings-inventory-astra-ultra-handoff.md) records READY Inventory Preview `dpl_7xc2akyMGziEALNz3aCZFvWsnPCs` at application `e286ea0a`. Sale details, full-resolution and catalog consumers remain absent/off. This audit did not refresh hosted configuration. |
| Real provider observations | [September 17 report](../2026-09-17-sold-comps-provider-observations.md): four searches, four details, eight image reads; no model. Of 25 sold rows, 24 omit offer status. Exact details confirm two ordinary sales; accepted offer remains unknown; active detail's `ended:true` is insufficient. Execution was disabled after the four checks. |
| Fresh archive verification | Recomputed all four report hashes and `MANIFEST.json` under `/Users/markthomas/Library/Application Support/TenKingsInventory/investigations/20260917-live-provider-qualification`; all match the report. Manifest SHA-256 `21b289fe4ba11aa52bf3658895593a37d78c5db4a3d8e19e5cb76b88ffa256f2`. |
| Offline V5 checks | [SESSION_LOG September 17](../../handoffs/SESSION_LOG.md), line 33422: 113 affected checks, including 16 engine integration cases, passed. Tests cover controls, duplicate reconciliation, timeouts, failure caching, late mutation, V3/V4 compatibility and V5 parsing. They are not live accuracy evidence. |
| Controlled timing | [Full matrix](../2026-09-17-intake-full-matrix-results.md): 3,200 measured + 320 warmup saves; all frozen pooled/repetition/headroom/integrity gates passed. Optional details/catalog/full-resolution were off. Earlier failed receipts remain retained. |
| Default cron wiring | [Functional result](../2026-09-17-default-research-cron-functional-fixture.md): real default handler → worker → engine → PostgreSQL passed, but explicitly V3/details-off. No rerun of this completed configuration is needed. |

The positive exact items are Bo Bichette `377468246486` ($89; search `89`, detail `89.0`) and Snivy `318826793399` ($1.08). Both retained search observations identify **auction** sales. The archive contains no ordinary fixed-price positive; the active fixed-price control `117403547405` is not one. Accepted-offer control `177869917659` remains unrealized. The parallel cohort's downloaded primary belongs to another independently recorded listing, not its accepted-offer detail. No sold-image byte archive/model comparison was produced: 500-pixel decoded dimensions do not prove readable foil/variant cues.

## Code authority and remaining gates

References below are relative to this checkout and the audited application commit.

- `frontend/nextjs-app/lib/server/staffInventoryResearch.ts:229–315`: only eligible exact sold rows missing/null offer status enter detail enrichment. Original facts survive; conflicting duplicates remain excluded. At most **two distinct item IDs per engine attempt**, shared across all three searches, with failure caching/no detail retry. Calls can overlap two at a time; each gets at most six seconds inside the existing 20-second source round with return reserve. `:607–621` selects V5 only for `STAFF_INVENTORY_RESEARCH_SALE_DETAILS=true` and records its V3/V4 base mode.
- `packages/shared/src/staffInventoryResearchSaleDetails.ts:101–146`: confirmation requires exact IDs/title, agreeing integer USD cents, explicit detail no-offer, complete matching date/positive sold banner and no active/offer/range/hydration conflict. This grants price evidence, not variant or final match authority.
- `frontend/nextjs-app/lib/server/staffInventoryResearch.ts:517–538`: final selection still requires supported identity/reference evidence, matching target condition, assessed image and at least two listings with distinct image hashes. Thus two recovered ordinary prices alone cannot establish an estimate.
- `packages/shared/src/staffInventoryResearch.ts:239–274` binds V5 candidates to exact detail receipts and retains older readers. Once V5 persists, disabling enrichment must keep this reader; never downgrade saved evidence.
- `packages/database/src/staffInventoryResearchV2.ts:13–16` allows three attempts per run and nine lifetime attempts. The two-detail cap can therefore become **six per run / eighteen lifetime**; it is not a per-job spending guarantee. The default worker also claims the oldest queued eligible job (`:174`), so calling the normal hosted cron is not an exact-card qualification tool.
- **Unclosed durable V5 gate:** `frontend/nextjs-app/tests/staffInventoryResearchCronPostgres.test.ts:14–36,194–199` requires optional flags absent and V3. V5 integration `staffInventoryResearchSaleDetailsIntegration.test.ts:287–330` rejects malformed results before DB access and uses a simulated reader for valid readback. No real V5-on default-cron persistence proof was found.
- **Unclosed live gates:** same-card positive/control labels, actual model/image decisions, ordinary fixed-price evidence, hosted V5 request/persistence behavior and changed-background-load responsiveness. Catalog-enabled V5/V4 requires its own real reviewed publication pins and useful reference evidence. Do not combine all optional flags into the first qualification.

## Minimal next experiment: one new V5-on durable fixture

Extend the existing disposable cron fixture/runner narrowly; retain the original V3 run/receipt. No runtime seam or capacity increase is needed. The lead owns the implementation/review and one sealed launch; this audit has not changed or launched it.

1. Pin current source, generated-client compatibility and dependencies in a fresh private preparation receipt. Reuse the existing loopback-only runner and 10-minute supervisor/cleanup bound. Set only sale-details true inside its synthetic child environment; catalog, contributions and full-resolution stay off. No hosted/shared database, real provider or external storage access. Preserve all prior archives.
2. Use exactly **three synthetic quantity-one inputs and three owned claims**, without research retries: (a) two independently imaged, exact ordinary missing-offer sales with supported synthetic identity, producing a V5 estimate; (b) protected wrong-card/parallel/grade/raw-condition, accepted-offer and active controls, producing no incorrect confident match/selection or sold/price assertion; (c) missing-offer candidates whose two details return controlled 429/timeout, with valid repeated search refinements, producing durable unknown evidence rather than an optional-detail-induced job retry. Assert the actual default worker/writers, not substituted dependencies.
3. Hard ceiling across the fixture: **zero live calls**, three engine attempts, nine synthetic searches, six synthetic detail calls, nine synthetic model responses, 36 synthetic candidate-image reads. Each attempt retains production deadlines and the two-ID cache. Freeze expected exact per-case counts before launch; ceiling compliance alone is insufficient. No extra claim may be made to obtain a passing case.
4. Require canonical V5 persisted bytes/result hash/readback agreement; original missing/null flags and exact search/detail amounts retained; no source-history drift; controlled failures cached across refinements; no late mutation; source/variant/condition controls fail closed. Turn enrichment off only in the fixture process and re-read the stored V5 unchanged. Record actual counts, terminal states and cleanup. Any unexpected transport, claim, false assertion or cleanup failure fails the run.

This is a functional gate, not a repeated performance study or measured real-card improvement.

## Smallest later live step and release sequence

After that fixture passes, prepare **one exact-input, one-attempt server-side V5 diagnostic** using the existing legitimate server secrets and normal human authorization. The current four-preset provider probe does not invoke the engine/model and cannot supply this proof unchanged. Do not use hosted cron/retry to select arbitrary existing jobs, export secrets, or enable the branch-wide flag merely to run a sample.

Freeze one real card's complete input/photo hashes and an independently reviewed known-positive/control tape first. Reuse archived controls for paired decision comparison. For the single fresh run cap actual calls at **three searches, two details, three Astra requests, twelve candidate-image downloads**, plus two exact original-photo reads and at most four existing reference-image reads; no extra listing views, full-resolution fallback, automatic retry, live source write or catalog publication. Persist only private diagnostic output. Receipt hashes, original provider/model payloads and decision stages must be retained privately for a true same-payload comparison. Request counts are not verified billing units. A missing positive or fixed-price case is inconclusive, not permission to keep searching. Select a fixed-price ordinary case if one is already evidenced; both existing positive auctions need no repeat provider probe.

Then qualify only the new load delta: same candidate application with detail flag off/on, one/three intake sessions, ordinary-detail and bounded 429/timeout load, using the existing frozen ABBA margins (each relevant p95 increase ≤ max(50 ms, 10%), zero added errors/duplicate jobs). Coordinate with the intake lane; do not rerun the entire completed base-versus-candidate matrix. Hosted V5 durable execution and staff phone acceptance still precede operational activation. Preserve V5 readers, one cron owner, private auth/storage and migration-disabled deployment. The lead must record exact planned/observed release actions.

## Independent work and genuinely missing inputs

Can proceed now without Mark repeating approval: prepare the targeted fixture, freeze controls/receipt format, align the enabled-load protocol with the intake lane, and review legacy/V5 readers. Sports/Pokémon publication preparation, phone acceptance and domain work remain separate workstreams. ATLAS owns its adapter/release; no ATLAS edit or takeover is needed here.

Missing evidence is **human truth and a suitable real input**, not generic build permission: confirm exact card/printing/condition and which candidate images are independently correct; identify a real ordinary fixed-price positive if available. The lead should first inspect the existing private corpus and obtain only labels genuinely absent there. An owner-provisioned local key is unnecessary if a reviewed server diagnostic can use the normal existing secret mechanism. Provider billing/account quota must be checked by the authorized lead before interpreting request caps as spending limits.

The accuracy lane owns the independent 200-card holdout in [acceptance §5](../2026-09-16-intake-atlas-acceptance-next-slice.md): all cards stay in the denominator; assess the UI's union of matched assertions and selections, sold-event correctness and verified-value coverage separately. Neither this three-input fixture nor one fresh run establishes >90% all-card coverage.
