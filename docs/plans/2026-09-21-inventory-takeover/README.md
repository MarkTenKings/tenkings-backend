# Ten Kings Inventory — active lead and next delivery

September 21, 2026. Lead task `01a0c55f-aeb0-77d3-912c-0736a7f6c429` accepted the predecessor's local handoff commit `297fd302b866286824fcea79b8f77c6d995ca3db`. This is the working index for the new lead and its five requested `gpt-6-astra` / `xhigh` agents. The [complete handoff](../../handoffs/2026-09-21-ten-kings-inventory-astra-ultra-handoff.md) and [approved blueprint](../../specs/TEN_KINGS_V2_FINAL_MASTER_BLUEPRINT.md) remain the context and product authority.

The lead deliberately owns the existing Inventory release workspace `/Users/markthomas/tenkings/codex-staff-inventory-release-20260910` and coordinates the existing main-site release workspace. The generated detached `89c55d91` worktree is not an Inventory release source. ATLAS grading, its checkout, UI, accounting and releases remain separately owned.

## Current release state — live September 21

**The main website and shared collect application are live on Production source `bc92b1023640ab21499506fb2d4fb5e2bdb188df`.** This later release includes Pokémon preparation, V4/V5 tools, disabled diagnostics and the new comp-value/staff-review feature. Optional execution remains off. This includes the reviewed Inventory `de6fa5b6` catalog-selection repair. The [main-domain packet](main-domain-readiness.md#independent-read-only-postflight--september-21-20542057-utc) retains the earlier main-site DNS/TLS postflight; the latest release and its independent evidence are recorded at the top of that packet and below. [verified-state.json](verified-state.json) is the **historical takeover baseline before today's integration and rollout**, not current deployment state.

| Surface | Verified state | Remaining |
| --- | --- | --- |
| Production: apex, www and collect | `bc92b102`, `dpl_FXLDCapouMhB7WtULnCTb45QXG31`, READY; immutable host `tenkings-backend-nextjs-drrqyvmk1-ten-kings.vercel.app` | Mark reviewed the new website and confirmed it works on September 21. Website acceptance is closed on his confirmation; no repeat sign-in or phone test is requested. |
| Inventory Preview | `e286ea0a`, `dpl_7xc2akyMGziEALNz3aCZFvWsnPCs`, READY | Root's fresh environment metadata shows **no branch `DATABASE_URL` record**; admin catalog and diagnostic flags are false. This Preview is not staging-ready. Sports staging/publication has not occurred. |
| Main Preview, diagnostic closed | Exact `bc92b1023640ab21499506fb2d4fb5e2bdb188df`, `dpl_Hie34Nkq8rhtp1MGjURUAsY7J8yH`, READY; immutable `tenkings-backend-nextjs-hi22vdspr-ten-kings.vercel.app`; actual stable alias verified | Exact-input execution is false. The single Snivy receipt is retained and independently reviewed; no confirmed comp/value. The same source is now promoted to Production; catalog activation has not occurred. Post-shutdown authenticated UI recovery display was not observed after Chrome became unavailable. |
| tenkings.co / www | Both attached and verified; apex A `216.150.1.1` / `216.150.16.1`, www CNAME `47388d6720762a26.vercel-dns-017.com.` | Public/authoritative resolver checks and normal TLS pass. Existing mail, service subdomains and all five prior TXT records are preserved; two certificate-validation TXT records were added. |

The sole enabled research schedule remains `/api/cron/inventory-research`, every minute, now on the exact `drrqyvmk1` Production host. Six natural minute-spaced requests returned HTTP 200 during the earlier main-site rollout postflight; no manual cron invocation occurred. These request logs do not expose job counts or business results. Both inherited platform aliases continue to serve specialist tools, and collect customer/NFC/card compatibility is preserved. The former `dpl_Bo43…` Production and `895a3b34` / `9a12cd35` main Previews are historical release identities. Root retains the historical diagnostic Preview's build/alias/evidence records under `investigations/20260921-exact-input-preview`. The earlier diagnostic shutdown postflight at 23:45:52 UTC verified its then-current disabled artifact and unchanged Production/cron. The original diagnostic artifact `dpl_4Yrs` is historical and no longer owns that stable alias.

Live postflight passed: all 28 routing/private-header checks and six referenced CSS/JS/brand assets, shared build BXAmDdh8X-Ikg6B_GxbR1. The sole minute cron belongs to the new deployment; its natural 01:18:37.171 UTC request returned HTTP 200. No manual cron invocation occurred, and the request log does not establish job outcomes.

The lead rehashed all 228 members across the three retained qualification archives: zero size/hash mismatches. The complete default-off timing study, separate default-cron fixture and seven sports PostgreSQL tests stand without repetition. The unrelated untracked Atlas note still matches its handoff hash. No caches, retained artifacts or business records were deleted.

## Remaining completion work — after owner website acceptance

This is the current Inventory/recognition/catalog scope, as authorized in the September 11–16 blueprint amendments. Main website and basic phone intake are accepted. The remaining work combines integration, real data/review, measurement and Production activation; built foundations must not be described as missing applications.

| Delivery | Already built | Required to finish |
| --- | --- | --- |
| Usable sports/Pokémon catalogs | Import/preparation, immutable publication and review tools; sports selection repair; qualified Pokémon mapping preparation | The combined specialist source is deployed; activate only the qualified admin capability, stage real source bytes, import the 138-row Pokémon checklist, review/publish the narrow sports and Pokémon pilots, and preserve unknown applicability. Extend coverage from actual encountered sets rather than claim exhaustive coverage. |
| Useful visual references | Source/image provenance, scoped reference contracts and review path | Acquire usable independently sourced images with separate image-use permission, review depicted card/finish/language/applicability, publish useful references and verify a different physical card benefits. No pilot image is yet approved. |
| Better sold-comp outcomes | Matching/rejection rules, bounded search/detail enrichment, optional larger-image and catalog-aware research, durable evidence/history | Qualify actual positive/control examples including ordinary fixed-price evidence, complete same-source enabled-load checks, correct measured misses and activate the qualified options. One Snivy unknown does not establish useful coverage. |
| Shared knowledge in everyday use | Versioned lookup/publication/proposal contracts, Inventory consumer/contribution paths and review inbox | Activate reviewed Inventory flows; hand off real publication pins and compatibility evidence. The ATLAS lead owns its missing app adapter, authorization/accounting and release. Prove both directions with different physical cards; no automatic promotion of guesses. |
| Demonstrated accuracy | V1–V5 evaluator, paired comparison rules and synthetic checks | Assemble the independent 200-card sports/Pokémon corpus, obtain human labels, measure all-card coverage and precision, and fix observed weaknesses without tuning on held-out labels. The >90% target is unproved. |
| Final operational rollout | Accepted main website/intake, default-off load proof and durable worker fixtures | Test additional work actually enabled under simultaneous intake and provider failures, then release/activate the qualified catalog/research changes and verify durable real-card results without slowing capture/save. |

Prioritize reviewed pilot data and visual references, then actual consumer/shared integration and sold-comp qualification; finish with independent accuracy/load measurement and Production activation. Normal specialist access and human source judgment are operational inputs, not a request to reapprove the development plan. The wider future V2 commerce phases are not silently added to this Inventory completion checklist.

## Added owner request — visible value and staff comp review

Mark subsequently requested an eBay comp value on the Inventory list and confirmed that staff must be able to remove an incorrect Astra-selected comparison, recalculate the value, and quickly review selected comps beside their Inventory card. The [dated implementation and qualification plan](../2026-09-21-inventory-comp-review.md) records the exact behavior and boundaries. This addition is live in the qualified bc92b102 Production release after 250 application tests, 13 actual PostgreSQL checks, package builds, focused UI types/lint, synthetic actual-component browser checks, live migration/no-op and hosted qualification. All 92 completed saved research jobs are legacy V2 unknowns with unresolved identity and zero references, so there are no numeric estimates or selected comps yet. Manual review cannot turn an empty selection into an estimate. Next coverage work is reviewed identity/reference evidence and current-bound research, followed by sold-price source coverage. The [follow-up recovery audit](../2026-09-21-inventory-research-recovery.md) confirms all 92 are current distinct cards: 18 lack required lookup fields and 74 have no exact legacy set-alias hit. New V3 jobs share the same gate. Ninety can rerun; the two exhausted inputs need a genuine new-input correction or supported audited refresh, not fabricated edits. Narrow positive catalog facts can suffice without an exhaustive reference-image library. Original research evidence and manual expected-price/accounting records stay unchanged.

## Five next-action packets

| Lane | Concrete next delivery |
| --- | --- |
| [Comp qualification](comp-qualification.md) | V5 actual default-handler/worker qualification passed as `f3563a94`: three durable completions using synthetic external transports, zero real provider calls. The bounded exact-input diagnostic is implemented and independently reviewed in `4e1b1c21`; 42 focused offline tests, focused types and lint pass. The one hosted Snivy diagnostic and independent evidence review completed; execution is now disabled on the verified stable Preview. Exact printing/treatment remains unresolved and no estimate was produced. Additional enabled-load qualification, useful fixed-price evidence and independent visual truth remain open. |
| [Sports pilot](sports-pilot.md) | Source packet/runbook is ready and the parent selection fix is live. Staging and human-reviewed publication have not occurred. Preserve 379 identities and nine unknown card/printing relationships; prepare the narrow three-card factual review and separately permitted useful images. |
| [Pokémon pilot](pokemon-pilot.md) | Two-marker/two-scope preparation and truthful taxonomy build outcomes are implemented; parent integration is complete. Twenty focused implementation checks and 17 parent UI checks pass. Actual disposable PostgreSQL qualification **passed: nine results in 16.8 seconds, all 17 locks verified, clean cleanup**. Managed schema/roster preflight also passed. The specialist source is now deployed. Real 138-row import, preparation and publication await qualified admin-flag activation, normal collect sign-in and genuine human review. Recheck the set key immediately before import. The publication recipe covers four cards, not all 138. |
| [Intake, accuracy and shared acceptance](intake-accuracy-acceptance.md) | V4/V5 scorer/schema and paired human-truth validation are committed as `0ebd571a`; 21 focused tests and independent read-only review pass, with legacy schema/score behavior preserved. Assemble real sealed held-out truth and qualify only genuinely exercised optional load. All-card accuracy remains unmeasured. |
| [Main-domain readiness](main-domain-readiness.md) | Production rollout and independent infrastructure postflight are complete. Mark has now accepted the new website as working; website and phone acceptance are closed. Preserve original-origin pending work and the single research schedule. |

Root owns source integration, SESSION_LOG and release decisions. The initial agent audits had disjoint document ownership; subsequent scoped implementation and qualification are recorded in each lane. Root performed today's authorized Production/configuration/DNS changes. No lane owns ATLAS work. These packets identify evidence and implementation tasks, not new approval requirements for the overall plan.

## Catalog-selection repair delivered to Production

The sports audit found that choosing an existing Set ID updated only the ingestion input. The catalog panel depended on a selected pending job, so it stayed hidden for the sports set's retained zero-pending-job state.

The repair gives catalog review its own explicit selected set and displays that target. Existing-set choices, selected ingestion jobs and successful prepared imports can open it. Typing, create-new selection and clearing the selected job clear the catalog target. Legacy draft/version bindings remain independent. A delayed import cannot overwrite a newer explicit catalog choice, and the preparation child retains the exact pending request through set switches. No intake, research, writer, schema, permission or publication semantics changed.

Verification on Node 22.23.2: **17/17 focused UI tests pass**, including three new full-page regressions mounting the actual catalog/preparation children; scoped lint has no warnings/errors. Project TypeScript checking still reports the same 12 unrelated grader-test diagnostics, with zero diagnostics in the changed files. Independent review identified one stale-completion edge, now fixed and covered; final re-review found no actionable defect. An initial test-only step-button selector was corrected without changing product behavior.

The fix was committed as **`de6fa5b6`**, integrated into main as **`9a12cd35`**, and is live in Production. Inventory Preview remains at `e286ea0a`; main Preview has subsequently advanced to the separately qualified candidate above. The current bc92b102 Production artifact now also includes the later V5 qualification/scorer, Pokémon, disabled diagnostic and comp-review commits; their optional execution remains off. Production catalog evidence/consumer and other optional execution flags remain off; deployment of the selection repair is not source staging or publication. Root's separate Preview diagnostic does not change that Production boundary.

## Inputs and coordination still outstanding

Mark confirmed on September 21 that he already tested the phone workflow a couple of days earlier and considers it complete. Phone acceptance is closed on that owner confirmation; it is not a new instrumented device/build receipt. Do not ask him to repeat it. Apex routing/TLS/assets are verified. Mark subsequently confirmed that he reviewed the new website and it works, closing the website acceptance request. This records owner acceptance, not newly instrumented browser subchecks. These surfaces use real business data. Preserve original-origin pending drafts and do not save synthetic stock.

Actual human review of the final source/applicability/publication payload and separate reference-image permission remain required by the approved catalog contract. Preparation and factual checksum agreement do not supply that judgment. The broad greater-than-90% correct-comp goal remains unmeasured.

Fresh managed-database preflight at 21:04–21:08 UTC verifies PostgreSQL 17.11, read-only Repeatable Read, 97 active finished migrations, no unfinished migration and no local checksum mismatch. Catalog schema/guards are present; Sports preserves the pinned 379-card/10-program/6-version rosters, with no publication; the proposed Pokémon key and checked legacy/reference aliases remain absent. See the [operational preflight](sports-staging-review-runbook.md#live-read-only-preflight-and-origin-decision--september-21). This does not remedy Inventory Preview's missing database configuration. Root must choose the smallest supported specialist origin: main Preview has its sensitive database override but intentionally hides SetOps, while collect Production has working database access and the selection repair; the qualified combined Pokémon source is now deployed there, with admin catalog activation still off. That read-only catalog preflight changed no environment flag. The later isolated diagnostic changed only its two main Preview records and is now closed; Production catalog flags remain off. Research catalog/consumer and contributions must stay off when only the admin catalog capability is activated.

The initial incoming ATLAS handoff reported shared identification V2/new and V1/historical support but no catalog/research/source-review adapter. Its previously reviewed catalog baseline was unadopted; do not treat that dated source report as its current release identity. It requests a frozen V5/source-fact-V2 compatibility delta, real reviewed publication pins and transport/media readiness before its own adapter work. Its initial manual grading acceptance and qualified adapter release precede the live different-physical-card proof in both directions. This is an incoming ATLAS report, not an Inventory runtime audit. No credentials were exchanged and no shared-host hold was taken.

The exact-input diagnostic, independent offline review, one hosted invocation and qualified shutdown are complete. The retained 512235-byte terminal receipt has SHA-256 `c6fb1b0ccfcc848177330f0d5dd188c030b57abe5015ca209fb977edc0d1f41b`. It records 2 searches, 2 details, 2 Astra calls and 9 thumbnail reads in 96.6 seconds; one detail timed out without retry. Zero references and unresolved printing/treatment left all 24 candidates unselected and value unknown. Exact original bytes were checked in the run; full local wire replay still lacks those managed originals. The completed research-job row is unchanged before/after. This development input remains excluded from the holdout, and the result is not an accuracy score. Browser evidence download succeeded before shutdown; authenticated post-shutdown recovery remains unobserved after native Chrome access became unavailable. The catalog code, parent integration, live schema/roster read and owned PostgreSQL qualifications are complete. Real source staging/import/publication still await qualified admin-flag activation, normal collect sign-in and actual source/applicability/publication review. Inventory Preview's missing database configuration remains explicit. The completed V5 fixture, scorer and Production postflight stand without repeating the old matrix. Retained old `50ffd` must stay stopped, ATLAS artifacts must not be pruned, and any future shared-host-heavy action needs a fresh bounded coordination window.

## September 21 — concurrent delivery authorized

Mark explicitly authorized work on production/main website, better sold comps, sports/Pokémon catalogs, and accuracy/shared knowledge. Five Astra Extra High agents run in waves within available concurrency. Root owns the release and integration. Today's final main Production release is bc92b102, including de6fa5b6 and the later reviewed Pokémon/scorer/disabled-diagnostic/comp-review changes. Production optional capabilities remain off. The separate Preview diagnostic's one bounded execution, retained evidence and verified shutdown are recorded above and in the append-only session log. Human factual/applicability/publication judgment and separate reference-image permissions remain distinct from implementation authorization and passing tests. Independent labels for the frozen 200-card study remain absent; neither local fixtures nor the live domain release measure greater-than-90% all-card accuracy.
