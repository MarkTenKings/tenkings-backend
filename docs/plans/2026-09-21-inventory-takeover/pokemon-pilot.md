# Pokémon pilot takeover: ingestion, review and publication packet

Updated September 21, 2026 after local implementation, completed parent integration and the owned PostgreSQL qualification. The initial read-only audit used Inventory HEAD `297fd302b866286824fcea79b8f77c6d995ca3db` and application source `e286ea0a190bfdc6807fe8ca58c47b34c94b55c1`. This packet creates no catalog authority, real source import, human review, publication, image permission or live inventory. The qualified run is bound to the exact source/dependency seal below, not to an inferred production release.

## Decision

The complete Legendary Treasures worksheet and the narrow marker/scope preparation path are implemented and locally qualified. Parent UI integration is complete, and the actual preparation service passed owned PostgreSQL qualification. Real import and publication remain unperformed: current-key reconciliation, exact target/release/flag checks, and an authenticated human reviewer must precede the one **unreviewed** queue submission. The shared publication recipe still covers **four cards**, not the entire 138-row roster.

The initial audit performed only local source reads, checksum/JSON comparisons, one PDF render, visual review and independent PDF coordinate extraction. Subsequent authorized work added the implementation, focused local tests, mounted parent UI checks and one owned-loopback disposable PostgreSQL run. No real provider, production database, hosted UI, media storage, deployment/configuration, shared-host or ATLAS operation was performed by this lane. The original source and retained evidence remain unchanged. Root owns release decisions and SESSION_LOG.

## Verified identities and bytes

| Item | Verified value |
| --- | --- |
| Category / year / language | `POKEMON` / `2013` / `en` in the prepared recipe; year is present in the source copyright |
| Display/source label | `Black & White—Legendary Treasures` |
| Proposed exact SetOps key and parent program label | `Black & White-Legendary Treasures` |
| Normalized program key | `black-white-legendary-treasures`; a normalized key is not a database row ID |
| Publisher / manufacturer | `Pokémon` / null in the shared manifest recipe |
| Complete worksheet | 138 distinct printed numbers: `1`–`113`, then `RC1`–`RC25` |
| Shared-publication selection | `6` Snivy, `7` Servine, `RC1` Snivy, `RC2` Servine |
| Separate Radiant Collection program | Not established by this page; none is proposed |
| Current real draft/program/card/source/printing/scope/publication IDs | Not established in this audit; prepared wrapper retains null IDs |

The pinned original is [the retained Pokémon PDF](</Users/markthomas/Library/Application Support/TenKingsInventory/investigations/20260916-catalog-pilot-sources/pokemon-checklist.pdf>), acquired September 16 from `https://assets.pokemon.com/assets/cms2/pdf/trading-card-game/checklist/bw11_web_cardlist_en.pdf`. This audit verified one page and the exact local byte stream; it did not reacquire or attest the current remote URL.

| Artifact in `docs/plans/catalog-pilot-20260916` unless stated otherwise | Bytes | SHA-256 rechecked September 21 |
| --- | ---: | --- |
| Retained `pokemon-checklist.pdf` | 2,227,474 | `d597ba707c20f1f8ec11e4809bb1f6e85bb0e502c05a93df41f36116a56dc923` |
| `source-manifest.draft.json` | 2,852 | `32d69d353d106ce7fb909ec13ebce68c7448bcf7c29fa5fdd81c5689f567780a` |
| `pokemon-complete-checklist.unreviewed.json` | 47,364 | `e318dad8f283abb15c8d0a65736c81f1744a9d681c9bc3375836481c4d6345c1` |
| `pokemon-complete-import.unreviewed.json` | 211,681 | `a36171566277e391764aae473df273a3c5fe26e54d4faa53d7802c2843d65a21` |
| `pokemon-source-fact-use.proposed.json` | 607 | `815affeb960ee607937b999cda2fdb06d2e1c61e4eded81f22148f02b2df9c4b` |

Independent coordinate extraction from the retained PDF matched all **138/138 number/name/source-column tuples**, with zero mismatches. Full-page visual review confirms the marker legend, the distinct RC-number roster and RC11's filled rare star. All 138 wrapper rows match the transcription's names, numbers, markers, rarity and source columns; all retain null rookie, physical finish, collector denominator, edition, format and channel values.

Recomputed transcription totals are 81 `standard set`, 57 `standard set foil`, and 101 `parallel set` observations; rarity counts are common 37, uncommon 41, rare 13, rare Holo 30, rare Holo EX 12 and rare Ultra 5. Markers overlap. Sixteen repeated-name groups remain separate numbered identities, including Snivy `6`/`RC1`, Servine `7`/`RC2`, and Meloetta-EX `RC11`/`RC25`.

The last managed absence observation is **September 17 at 04:10:36.594Z**, receipt SHA `9c1a77a7993b8fd49f360e7d4ea82285845a83d80cd03ae5f102c22d70c55495`. It enumerated 242 set keys and scoped draft/job/source/card/legacy/reference/publication aliases, finding no Pokémon match. It is dated evidence, not today's absence proof. The earlier auth-database receipt with missing tables must never be substituted.

## What the evidence does and does not establish

The adapter matches declared source pins and validates every original input row before normalization can deduplicate it. It explicitly records `sourceBytesVerifiedByAdapter=false`, `transcriptionVerified=false`, `humanReviewed=false`, and `rightsVerified=false`. Browser parsing is a bounded shape/consistency check only. The fresh local hash and extraction above strengthen preparation evidence; they do not turn those unreviewed persisted claims into human authority.

For the existing four-card publication recipe:

| Card | `standard set` marker relationship | `parallel set` marker relationship |
| --- | --- | --- |
| 6 Snivy | supported literal marker | supported literal marker |
| 7 Servine | supported literal marker | supported literal marker |
| RC1 Snivy | supported literal marker | unknown |
| RC2 Servine | supported literal marker | unknown |

These six positive and two unknown relations concern the printed checklist markers. They are not physical finish confirmation. Missing RC parallel markers are not exclusions. No `6/113`, `RC1/RC25`, Reverse Holo, edition, format, channel or odds fact is introduced. `standard set foil` remains in the complete worksheet metadata but is outside this two-marker publication recipe.

The lookup implementation computes effective applicability as `unknown` while a printing's edition/format/channel is null, even when a literal relation is recorded as supported. Omitting language from a query also leaves language unresolved. It returns no candidate images unless effective applicability is supported. Adding photos alone therefore cannot qualify finish recognition. Explicit evidence must resolve those dimensions; never fill `not_applicable` merely to unlock images.

**Source facts and image permission are separate.** Current source-fact review V2 requires exact source hashes, factual purpose/scope, intended Inventory/Atlas consumers and actual human review; it does not require an invented blanket manufacturer license. The older grant-only wording in the pilot README, mapping and initial ingestion review is superseded by `source-fact-use-correction-review.md` and the current implementation. Preserve those historical files; this packet records the correction rather than treating them as a new owner-approval requirement.

The existing Pokémon fact-use proposal names card names, printed numbers and program membership. Before reviewing marker/applicability publication, explicitly include the literal marker vocabulary and positive row-marker relations in the proposed factual-scope detail. This is still an unreviewed proposal, not a legal attestation, PDF-distribution authority or image-use grant.

No pilot image was acquired or approved. A useful image addition requires separately documented `owned_original`, `licensed` or `permission` evidence, intended consumers, exact bytes/hash/MIME/dimensions, actual depicted card/printing identity, representative printing scope, visible source-supported diagnostics, and parent/source lineage. Model agreement, a selected comp, public accessibility, a PDF header graphic or a private intake image is insufficient. The same listing/image and its crop are not independent corroboration. Precision grading references remain separate.

## Exact operational sequence

These are real operational actions for the coordinator/operator after the local qualification below; none was performed against the live catalog by this lane. Existing owner implementation authority remains in place; the genuine human evidence-review boundary remains required.

1. **Verify the actual target.** Use the coordinator-confirmed Inventory `/admin/set-ops-review` route and exact deployment from the current release record, with normal human reviewer access. The initial audit referenced legacy Preview `tenkings-backend-nextjs-61fqqazwp-ten-kings.vercel.app`; that historical URL does not prove the new preparation code is deployed. Specialist SetOps host policy and main-domain release remain under root control. The last recorded catalog flag was `SET_CATALOG_EVIDENCE_ENABLED=false`; only the disposable fixture enabled it locally. Root must qualify the exact release and deliberately manage activation before real preparation; this packet does not activate it. Check taxonomy ingest separately: `TAXONOMY_V2_FORCE_LEGACY`, `TAXONOMY_V2_DEFAULT_ON` and `TAXONOMY_V2_INGEST` govern draft taxonomy creation, independently of the shared-catalog flag. Reconcile the current schema with the live ledger rather than assuming the old 96-migration fixture is today's schema.
2. **Take a fresh, bounded, read-only exact-key/alias snapshot.** Check drafts, every job status, source/program/card/parallel/scope rows, versions, approvals, legacy identity/reference matches and published aliases. Account for row limits; a signed-out response, missing schema, cap or 404 from the wrong route is not absence. Exclude concurrent imports/replacement/populate work on this set through create/build. If any matching identity exists, stop the new-set path and reconcile it; do not overwrite or delete it.
3. **Load the complete wrapper into Prepared checklist import.** Select `pokemon-complete-import.unreviewed.json`, not the original transcription or historical four-row request. Check set key, source URL/provider and 138 rows. The dedicated parser limits files to 768 KiB and sends exactly its serialized `requestDraft`. Generic upload intentionally rejects the envelope because it would rebuild source metadata.
4. **Queue once.** Use **Queue prepared checklist for review**, which sends `POST /api/admin/set-ops/ingestion`. Preserve the exact request body privately, its hash, target origin, time, actor reference, returned draft/job IDs and receipt. Do not preserve tokens. Expect one `QUEUED` job and one new `DRAFT` set before build. See uncertain-create recovery below before doing anything if the receipt is missing.
5. **Build once for the recovered exact job.** Use **Build Draft From Selected Job**, sending `{ingestionJobId}` to `POST /api/admin/set-ops/drafts/build`. Read both the new response and the persisted result, not just HTTP status: require response `taxonomyIngest.outcome="applied"` and persisted job `taxonomyIngest.applied=true`, adapter `pinned-pilot-checklist-v1`, official checklist source bound to that job, one program, 138 cards, and zero variations/parallels/scopes/odds/conflicts/ambiguities/bridges. Require latest version row count 138, no blocking errors, draft and job `REVIEW_REQUIRED`, and no approved version.
6. **Capture a real postflight.** The implemented build API and integrated parent UI expose a separate taxonomy outcome/result/message; require that contract on the actual target. Older deployed responses without it remain unconfirmed. Ingestion GET still returns selected source metadata without that result. Retain the new API receipt, then use a bounded read-only postflight to verify `SetIngestionJob.parseSummaryJson`, exact source linkage and persisted row metadata. The build handler catches taxonomy errors and can still return HTTP 200 with a review version. A null/failed taxonomy result is not success. Build's `reviewedAt` timestamp is machine bookkeeping, not human review.
7. **Review the complete worksheet against the original source.** Use the full roster review and raw metadata alongside the visible worksheet; the table does not display every marker. If a factual correction is needed, use **Save New Draft Version** and retain prior version/hash plus the correction reason. The serializer preserves source metadata and unknown rookie values. It does not update previously created canonical cards/source rows; any visible correction that changes canonical identity needs explicit reconciliation before publication. Legacy `versionHash` excludes raw metadata and cannot approve its provenance.
8. **Prepare the missing marker definitions/scopes through the qualified path.** After source-byte staging and before ordinary approval, use **Preview missing mappings**, inspect its exact existing draft/version/job/source/program binding, then deliberately select **Prepare pending mappings**. Preserve the receipt. The implemented endpoint adds only `standard set` and `parallel set` rows plus two scopes against the source-backed parent program, with null variation/serial/finish/format/channel. It preserves all 138 cards and source observations and grants no per-card/physical-finish authority. Parent integration and disposable PostgreSQL qualification are complete; release/flag qualification and the actual operation remain. If prior editing/rebuilding changed the original job/latest-version binding, reconcile that state rather than weakening the preparation guard. The generic two-row marker input remains unsuitable; its earlier score was 53.85 below the unchanged 70 threshold, and the pinned adapter is worksheet-only. Do not pad rows, invent odds, select Topps as provider, reuse the sports stage endpoint or directly fabricate IDs.
9. **Complete ordinary human SetOps approval deliberately.** After worksheet/taxonomy reconciliation and inspection of every pending job attached to the draft, use the exact clean latest version. Existing `POST /api/admin/set-ops/approval` runs variant synchronization before approval and bulk-updates pending jobs for that draft; it is not a review-only checkbox. Preserve seed receipt/status and verify no active seed/replacement remains. A failed or ambiguous approval requires inspection of jobs, side effects and approval records before another action.
10. **Download current taxonomy and compile the narrow publication.** With the catalog feature deliberately qualified/enabled, use **Download taxonomy mapping** (`GET /api/admin/set-ops/catalog/taxonomy?setId=...`). It is a read-only snapshot, not approval. Run `prepare-pilot.mjs --pilot pokemon --taxonomy <real-export> --selection <exact-selection> --source-fact-use <reviewed-proposal-file> --out <new-private-directory>`. Fill selection with the exact real program, four card, two parallel, two scope and source IDs. No fuzzy mappings or synthetic IDs. Preserve export hash, latest clean draft/version/hash and predecessor/current pins. The compiler still publishes partial four-card coverage, no aliases and no images.
11. **Stage, validate, inspect, then publish.** Load `{manifest,reviewEvidence}` into **Reviewed shared catalog evidence**. Stage the exact retained PDF through **Stage source / image files**; 2,227,474 bytes fits the 3 MiB browser bound. Validate with `/catalog/preview`. The server rereads private checksum-addressed bytes, verifies full manifest/taxonomy/source/current-history authority and emits manifest and verification hashes. The authorized human inspects the complete original, all identities/relations/unknowns and intended uses, then deliberately selects the acknowledgement and **Publish reviewed catalog evidence**. Its request binds both hashes, expected current/history and `PUBLISH REVIEWED CATALOG EVIDENCE`. It is a separate publication approval, not the legacy draft hash.
12. **Verify the actual publication and its limits.** Record publication/revision/hash, verification V2/hash, approval/reviewer record, current pointer and immutable predecessor; load current publication through the normal API. Run bounded read-only Inventory and ATLAS-consumer lookup checks against that exact publication pin, including same-name/different-number controls and unresolved scopes. This is factual catalog availability, not full visual coverage, 138-card shared publication, enabled research qualification, >90% accuracy or different-physical-card cross-app acceptance. The latter still requires the separate ATLAS lead and real card evidence.

## Uncertain-create and partial-build recovery

The prepared UI synchronously latches the exact request within its mounted workspace, including failures. That latch is a React ref, not cross-reload durable idempotency. The backend performs draft upsert, job creation and audit sequentially without one transaction or request key. A timeout/error can leave nothing, a draft only, or a completed job without its client receipt. Reloading or reopening must not become a retry mechanism.

- Preserve the original request and attempt time. Read all jobs for the exact key using the singular `/api/admin/set-ops/ingestion`, including non-pending statuses; its `total` is returned length, not a full database count, and the maximum is 500. Match source/provider/parser/fetch metadata and then verify full persisted raw payload privately through scoped read-only evidence. The queue DTO alone cannot prove exact raw-payload equality.
- If exactly one matching job exists, recover its ID and continue from its actual state. Do not create another job. If it already has a version/taxonomy result, inspect that result before Build.
- If there is only a draft, zero/multiple candidates, an unmatched payload, concurrent changes or incomplete reads, preserve the state and report the exact ambiguity. A manual new submission is appropriate only after the coordinator establishes the prior attempt cannot still commit and reconciles any partial state. Do not auto-retry, delete, relabel or infer success from an empty UI list.
- Build is also a multi-step operation. Taxonomy may commit before version/job/audit updates; another Build may create another version. After an uncertain build, inspect the original job summary, linked sources/taxonomy, all recent versions, source links and audit records. Recover the persisted result; do not automatically requeue or rebuild.
- Shared publication has a different contract: same draft/revision, both hashes and same reviewer can return an exact replay; differing evidence or changed pins fail. Preserve that request and inspect current/history before any deliberate replay. Never generate a fresh revision merely to hide an uncertain publication.

## Next independent work and human inputs

| Owner/lane | Concrete useful next task | Completion evidence |
| --- | --- | --- |
| Root / release coordinator | Qualify the exact target release containing completed preparation and parent integration; manage the catalog flag through the existing release process. | Exact deployment/schema/flag evidence and normal human access. Local PostgreSQL and mounted UI proofs below are complete; they are not production activation. |
| Operator + catalog reviewer | Fresh exact-key/alias reconciliation, one observed queue/build, retained truthful taxonomy result, and exact 138-card/source/version postflight before additive preparation. | Real recovered draft/job/source/program/version IDs, successful applied result and unchanged identity/source observations. Missing receipts require recovery, never an automatic new queue/build. |
| Catalog preparer | Prepare the exact two-marker scope/source-fact packet from the retained PDF; keep selected four-card coverage and unknown dimensions explicit. | No synthetic IDs, broadened finish labels or implied RC exclusions. Explicit factual-scope wording includes the marker relations being reviewed. |
| Human catalog reviewer/approver | Inspect the original one-page source and complete 138-row draft, resolve actual alias/correction questions, then review the complete narrow publication and intended consumers. | Real authenticated approvals bound to the exact versions and full evidence hashes. An agent's transcription check is not this input. |
| Owner/reference contributor + catalog reviewer | Supply or identify useful genuinely permitted images and evidence resolving depicted identity, finish diagnostics and scope dimensions. | Separate image grant and byte/lineage review; original private intake media stays private unless explicitly authorized for this use. No generic request for a manufacturer license is needed for factual names/numbers. |
| Lead + ATLAS lead | Arrange later different-physical-card use in each direction once actual evidence is published and compatible readers are released. | Inventory evidence helps another ATLAS card, and an ATLAS contribution helps another Inventory card; original physical-card/accounting/grading authority stays separate. |

Standing qualification is retained: source adapter/compiler checks, the metadata-preserving actual-handler workflow, prepared-input mounted UI checks, private ingestion cache fix and e286ea0a hosted qualification. Their counts overlap and must not be summed into a new test total. This packet adds source-byte/roster verification and the separately scoped local implementation/PostgreSQL results below. It still does not establish managed-database or real-human acceptance.

## Implementation references

- `frontend/nextjs-app/lib/preparedChecklistImport.ts`; `pages/admin/set-ops-review.tsx:1073` — prepared envelope and attempt latch.
- `frontend/nextjs-app/pages/api/admin/set-ops/ingestion/index.ts:188` — separate draft upsert/job/audit writes and bounded queue DTO.
- `frontend/nextjs-app/pages/api/admin/set-ops/drafts/build.ts:108` — original-input validation; `:191` taxonomy result; `:227` version/status writes.
- `frontend/nextjs-app/lib/server/taxonomyV2PilotChecklistAdapter.ts` — exact pins, original-input checks and cards/program-only output.
- `frontend/nextjs-app/lib/setOpsDraftEditor.ts`; `pages/api/admin/set-ops/drafts/version.ts`; `lib/server/setOpsDrafts.ts:735` — metadata preservation, immutable version creation and legacy hash boundary.
- `frontend/nextjs-app/pages/api/admin/set-ops/approval.ts:174` — automatic seed before ordinary approval; `:349` pending-job status updates.
- `frontend/nextjs-app/lib/server/setCatalogEvidence.ts:188`; `setCatalogEvidenceMedia.ts`; `setCatalogEvidenceAuth.ts` — full preview/publication, source-fact V2, independent image grants and human authority.
- `frontend/nextjs-app/components/admin/SetCatalogEvidenceReview.tsx`; `lib/server/setCatalogTaxonomyExport.ts` — real review UI, taxonomy export and staging controls.
- `docs/plans/catalog-pilot-20260916/prepare-pilot.mjs:40`; `packages/card-catalog-evidence/src/index.mjs:325` — four-card recipe and unresolved-scope lookup behavior.
- `docs/plans/catalog-pilot-20260916/source-fact-use-correction-review.md` supersedes historical grant-only factual-source prerequisites.


## Local implementation handoff — 2026-09-21

Mark authorized the parallel Inventory implementation tracks after the prior phone acceptance. This lane changed the files listed below and this packet, without provider calls, real ingestion/catalog/media writes, deployment/configuration, commits or SESSION_LOG edits. Root subsequently completed the parent integration and ran the reviewed owned PostgreSQL fixture. The earlier main release freeze from `de6fa5b6` is separate from this feature; use the current release record rather than treating the local qualification as deployed behavior.

### Owned files

- New `frontend/nextjs-app/lib/server/setCatalogPokemonPreparation.ts` and `setCatalogPokemonPreparationPlan.json`.
- New `frontend/nextjs-app/pages/api/admin/set-ops/catalog/pokemon-preparation.ts`.
- New `frontend/nextjs-app/components/admin/SetCatalogPokemonPreparation.tsx`.
- New `frontend/nextjs-app/tests/setCatalogPokemonPreparationFixture.ts`, `setCatalogPokemonPreparation.test.ts`, `setCatalogPokemonPreparationApi.test.ts`, `setCatalogPokemonPreparationUi.test.tsx`, and `pokemonPilotDraftOutcome.test.ts`.
- Edited `frontend/nextjs-app/pages/api/admin/set-ops/drafts/build.ts` and `frontend/nextjs-app/tests/pokemonPilotDraftWorkflow.test.ts`.
- New actual-SQL fixture `frontend/nextjs-app/tests/setCatalogPokemonPreparationPostgres.test.ts` and sibling runner `packages/database/scripts/testPokemonCatalogDisposable.mjs`; original sports fixture/runner/seals remain unchanged.

### Contract and deliberate limits

The fixed plan preserves exactly the one original checklist source/job/program, original latest clean worksheet, and all 138 canonical card identities and metadata hashes. It pins the retained PDF bytes, original raw payload, original source fetch metadata, exact worksheet content and legacy version hash. The worksheet content pin excludes only the build-time `generatedAt`; the complete live data, including that timestamp, remains in the preview snapshot and replay baseline.

Run preparation after the original successful 138-card build and before ordinary SetOps approval. Stage the exact PDF through the existing private artifact UI first. GET previews without writes. POST uses an explicit human reviewer, UUID, proposal hash, expected snapshot and exact existing draft/version/job/source/program-row binding. It inserts two literal markers (`standard set`, `parallel set`), two program scopes, and one actor-bound audit receipt in one transaction. It never creates jobs/sources/cards/programs/versions, edits metadata, adds odds, approves jobs or publishes evidence. Serial/finish/variation/format/channel stay null. Physical applicability stays unknown. Existing unrelated parallel/scope rows require reconciliation instead of an upsert.

Source bytes are read before taking database locks. The short write transaction uses 750 ms lock timeout, 3 s statement timeout, 1 s maximum connection wait and 8 s transaction timeout. It locks the catalog/taxonomy plus associated draft/work/publication tables in SHARE ROW EXCLUSIVE mode because existing generic build/approval writers do not uniformly lock the draft first. It does not lock Inventory tables. This catalog-wide lock footprint is deliberate for the bounded admin operation. The actual PostgreSQL contention, timeout, atomic rollback and replay proof is complete below; shared-host load and live concurrency were not tested. No automatic retry is added.

Exact same-reviewer/key/evidence replay verifies both the original baseline and all created row bytes. Changed evidence, lost/tampered mappings, a different reviewer or a new key against occupied mappings is rejected without repair. An uncertain response stays in session storage as exact original request bytes without a token; only deliberate **Retry same preparation** sends it again. A typed 409 tied to that exact key and expected snapshot permits a new explicit preview and preserves rejected bytes. Other failures keep the pending request for reconciliation. Reload never auto-dispatches. This idempotency belongs only to the new preparation operation: original queue/build uncertain-create recovery above remains mandatory.

The build API now returns `taxonomyIngest: {outcome, result, message}` separately from review-version HTTP success. `applied` requires the real ingestion result's applied flag; errors return `failed`, disabled/unavailable adapters return `skipped`. The result retains adapter/source/counts. An error's internal detail remains in the saved job/audit; the browser receives `taxonomy_ingest_failed`. All responses use private/no-store caching. No new approval or automated re-build follows a failed/skipped result.

### Parent integration — completed

Root mounted `SetCatalogPokemonPreparation` beside the sports preparation in `SetCatalogEvidenceReview.tsx` and integrated the build API's taxonomy outcome/message in `set-ops-review.tsx`. Failed, skipped and absent taxonomy results display an explicit error while retaining the successfully saved worksheet and review navigation. The earlier import/mount/status snippets were implementation instructions and are now completed history; no further parent edit is pending from this lane.

Root reports **17/17 actual mounted checks passed** and scoped lint clean for the integrated surfaces. This lane independently confirmed the mounted component and explicit failed/skipped/absent-result handling in current source. Full-project TypeScript remains at the same **12 known unrelated grader diagnostics**; it is not a globally clean typecheck. The preparation/API/fixture import-closure checks were clean. Mounting the component does not activate the server catalog flag or perform a catalog action.

### Completed local verification

Twenty focused local checks passed: nine service, seven mounted UI, one API authority/boundary, two existing 138-row workflow, and one actual-handler failure/disabled outcome check. These cover source/request/actor boundaries, 138 identities and metadata preservation, every insertion/receipt rollback, exact replay/lost response, stale snapshots, row/count mismatch, occupied targets, changed bindings and build timestamps, absent/changed source authority, UI storage failures/remounts/double clicks/late responses, wrong receipt binding/key/approval state, and actual-handler taxonomy success/failure/disabled outcomes. The fixture models transactions in memory and explicitly forbids real DB/network operations; concurrent test calls are serialized by that fixture and are not PostgreSQL proof.

Scoped TypeScript import-closure validation reported zero diagnostics, and Next lint for the ten owned TypeScript/TSX files reported no warnings/errors. The focused tests use the retained real local PDF for byte validation and the real unreviewed wrapper for identities; fixture row IDs are synthetic local test data, never proposed live IDs. No existing expensive qualification matrix was rerun.

### Actual PostgreSQL qualification — passed, cleanup complete

Root ran the reviewed sibling runner once in `/private/tmp/tenkings-pokemon-preparation-run-20260921-r1`. This lane independently read the complete TAP log, summary, migration ledger, second-deploy log, launch seal and cleanup receipt. The actual service passed **9/9 TAP results** (eight subtests plus the parent), with zero failures, cancellations or skips. Total runner time was **16,825 ms**; this elapsed time is a functional-run observation, not a performance result.

- All **96 canonical migrations** in the sealed source completed on the fresh `tenkings_pokemon_preparation_disposable` database at `127.0.0.1`; all ledger entries finished without rollback. The second deploy reported no pending migrations and the ledger remained unchanged. This is the owned fixture's schema evidence, not the managed database's migration ledger.
- Actual ordinary UPDATE locks on all **17 protected tables** forced the service to fail at its configured 750 ms lock timeout. Each measured attempt satisfied the fixture's 600 ms to under 5 s bound, with no partial creates. All five create boundaries (four data rows and the receipt) rolled back through real PostgreSQL trigger failures.
- Actual concurrent calls using the same key recorded once and returned identical replay receipt bytes. Discarding a successful response and replaying the original request returned the same receipt. Wrong hash, all five wrong binding fields, wrong source bytes, non-human authority and a changed replay actor were rejected.
- Changed card/version/source/job state and a **501-version roster** were rejected. The original **138 cards and metadata, one program, one source, one job, one draft and one version** remained byte-for-byte unchanged in the fixture's full-row comparisons. Only two marker definitions, two unknown scopes and one audit receipt were added. The original source stayed pending and invisible to the approved-source filter; no approval, odds, bridge or publication appeared.
- The real append-only audit trigger rejected update/delete. Altered mapping rows were rejected without repair, and restoring the fixture-only altered field recovered the same valid receipt.

The run's launch seal exactly matches the reviewed preparation bytes: SHA-256 `a93059df162f8feb02ee7dc37a32225e391bd7e62c18c292a2d1c0d57f4b2bb6`. The runner also required source/dependency seal equality after the fixture before recording success. [Summary](/private/tmp/tenkings-pokemon-preparation-run-20260921-r1/summary.json), [TAP log](/private/tmp/tenkings-pokemon-preparation-run-20260921-r1/pokemon-preparation-functional.log), [migration ledger](/private/tmp/tenkings-pokemon-preparation-run-20260921-r1/database-and-migrations.json), and [cleanup receipt](/private/tmp/tenkings-pokemon-preparation-run-20260921-r1/cleanup.json) are retained privately.

Cleanup reports all groups stopped, PostgreSQL stopped, temporary cluster removed, no retained temporary root and no errors. This lane independently checked all five owned PIDs — `30960`, `30971`, `30979`, `30985`, `30990` — and each was absent. The receipt states `complete=true`, `functional_integration_pass=true`, **`release_pass=false`**, **`performance_pass=false`**, and zero real provider calls.

The SQL proof used real pinned PDF bytes and exact source transcription, but synthetic fixture row IDs and a synthetic human session. It did not exercise a hosted/authenticated browser, production database, real catalog flag activation, shared-host load, actual ingestion, human approval, grant or publication. Original sports qualification artifacts and seals were preserved.

### Current remaining operational inputs

Implementation, parent integration and bounded SQL qualification are complete. Root still owns the exact release/target/schema/flag qualification and normal human reviewer access. A fresh exact-key/alias snapshot must establish current reality before one observed real queue/build; retain actual IDs and source/version receipts, and follow uncertain-create recovery instead of retrying automatically. Stage the retained PDF, preview the exact pending binding, then deliberately prepare the two mappings before ordinary approval.

The human reviewer must inspect the complete original and 138-row worksheet, resolve any actual correction/alias questions, review every pending job involved in ordinary approval, and separately review the narrow four-card publication with exact real mapping IDs and full evidence hashes. The source-fact proposal still needs explicit scope for literal marker vocabulary and positive row-marker relations. Image permissions, images, physical-finish/scope evidence, expanded 138-card publication and later different-physical-card Inventory/ATLAS acceptance remain separate, unfulfilled inputs. No completed local test substitutes for them.
