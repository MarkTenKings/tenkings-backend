# Refined staff inventory sold-comps research

Owner direction: Mark's September 11 evening test confirms faster, more accurate intake. Fix research that stops after a poor first SoldCompsAPI search, confusing rejected matches, absent prices, and small listing presentation. This is implementation authorization for staff research only. Preserve the successful intake and independent financial/ATLAS paths.

1. Inspect saved source evidence to distinguish unavailable provider prices from display suppression and evaluate the actual search. Show source-reported amounts with explicit labels when the amount cannot establish a verified sold price; never substitute a listed/unknown-offer amount into a market estimate.
2. Let Astra propose a refined query from the current card, photographs and observed search results when the first pass produces inadequate matches. Validate query anchors and permitted terms, never change the saved identity, retain bounded search history, deduplicate listings, and cap the attempt at three distinct searches with a hard deadline fitting the existing worker lease. Preserve useful first-pass evidence if an optional refinement fails. SoldCompsAPI is the only search provider; no SerpAPI.
3. Keep exact catalog/variant and sale-evidence requirements for verified market estimates. Refining queries cannot certify a variant or manufacture price evidence. Distinguish strong visual research candidates from verified estimate selections, other inspected results and rejected mismatches; keep the audit available without presenting unrelated cards as matches.
4. Redesign the results with larger private comparison photos, prominent source prices, full listing titles, sold dates and clear matching/price status. Show what was searched and refined, with existing results remaining readable. Preserve start/retry behavior and exact item/revision isolation.
5. Use additive, backward-compatible result fields so existing stored hashes and results remain valid. Verify persistence, size bounds, retries, signed previews and timeout/lease compatibility. Avoid migration unless evidence proves necessary.
6. Validate provider-format handling, query refinement/deduplication/limits, stale input, uncertain prices, wrong player/card/grade, partial failures and native phone/desktop layout. Review and deploy the isolated collect branch, then re-run research on Mark's tested existing cards through the existing authorized research writer. No inventory or financial mutations are part of that verification.

Ownership: Astra research agent owns engine/shared research schema/tests; Astra UI agent owns research panel/presentation tests; Astra review agent inspects persistence/runtime compatibility and deployment implications. Coordinator owns live source/provider evidence, integration, independent review and release. Initial implementation source is app bb10f235, checkout HEAD 5f45b846. Preserve untracked ATLAS consultation notes and main deployment hold.

Compatibility and release: keep the stored envelope at schema version 1, accept both research engine versions, and leave new result fields optional without parse-time defaults or transforms. Existing result bytes and hashes must remain unchanged. No database migration is required for these bounded JSON additions. Once an engine-v2 result is written, any serving or recovery release must include the compatible v1/v2 reader; restoring the old v1-only application would make those results unreadable. Preserve saved evidence and repair forward if needed. The existing 180-second lease and 240-second worker run remain; every claim reserves transaction time, and each attempt is additionally bounded by its actual lease expiry with 20 seconds left for persistence.

## Confirmed source evidence and implementation decisions

- Read-only live inspection found five completed research jobs and 56 candidate listings. All 56 retain a source price; none has eligible cents because 46 omit offer status and 10 explicitly report an accepted offer. Daron Holmes has 13 listings, all with prices, with 2 accepted-offer and 11 unknown-status records. The dash problem is display suppression, not missing source price data. Preserve those amounts as reported evidence without silently making unknown offer status false.
- Official SoldCompsAPI documentation at https://sold-comps.com/docs was opened September 12 UTC. The existing search endpoint supports explicit completed-listing detection, exact keyword matching and accepted-offer hydration. Enable those bounded request options. Hydrated amounts require affirmative provider proof, correct source field and currency; otherwise keep the reported/listed label. Hydration can consume at most one additional quota request per successful search, independent of item count. No provider account/settings change is required.
- Add optional `accepted_offer`, `research_queries` and `comparison_assessments` result fields. Retain schema_version 1; accept old and new engine versions. No default insertion or transformation of old parsed results because stored hashes bind their exact canonical values. Existing DB byte/identity checks already permit this extension; no migration is expected.
- The worker must reserve enough invocation time before claiming a 150-second research job: 20 seconds claim + 150 research + 20 completion within 240 seconds. Claims after 50 seconds are refused in that invocation; another cron invocation can claim eligible work. The 180-second lease and global concurrency cap remain unchanged.
- Protected baseline at `/Users/markthomas/Library/Application Support/TenKingsInventory/backups/20260912-before-refined-comps`: five existing private research jobs and 28 immutable workflow events. Live financial sync is current at sequence 28, seven held cards, 1700-cent cost and 6500-cent expected proceeds. Verification must preserve these original workflow rows; legitimate new user entries may append later.
