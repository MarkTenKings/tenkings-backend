# Inventory automatic comps — current release and retained lead handoff

Owner direction: **Every card must automatically get eBay research and comp retrieval. Staff review exceptions and correct mistakes; they do not research each card manually. Missing internal catalog records must not block searches.** Apply this to existing and future cards. The separate owner-facing replacement is task `01a0c746-65c6-7523-851b-a25a50f017a9`, created as `gpt-6-astra`, effort `ultra`, with fresh context, pinned and opened in the app. It replaces the temporary `/root/inventory_lead_ultra` subagent and owns implementation and verification after the root's HANDOFF READY message. All prior writers have stopped. Phone and website acceptance are already complete.

## Current handoff — September 22 v6 Production

Production is exact `db465c1c57659233d4841f8679cca0aac3ecdd8f` / `dpl_CyDxyASkDk2HGGRwjDtCGqovtLQX` (`41olp9f3x`). Direct readback at 15:35:59 UTC verifies all five aliases, project target and sole minute cron; 28 routes/assets pass at build `HdwiGQMJw0yDYUgMuhh83`. Stable Preview is execution-disabled `dpl_2VJgRGbJA1TUm9iikGZQvEX6WtLy` (`p6s7ca002`) at the same source. Production recovery and `STAFF_INVENTORY_RESEARCH_SALE_DETAILS` are true; catalog/admin-consumer, full-resolution, contribution and diagnostic execution remain off. No new migration or host restart accompanied this release. Root released the Inventory coordination hold for ATLAS after the first preservation snapshot; ATLAS keeps its separate ownership.

Automatic retrieval now uses usable saved identity without catalog or verified-original prerequisites. Missing originals/printing evidence still prevent unsupported match/value authority. Existing known individual units remain eligible after loading; exact bulk units need their own one-unit description/distinct photos, never inherited group photos. V6 supports private positive photo identity without catalog publication, while preserving exact-card/treatment/grade/known-sold-price/distinct-image/mean checks, nonnull staff facts, exact revisions, reviews and finite history budgets. The old backlog receives one versioned recheck without six-hour delay; unchanged or lost catalog evidence cannot cause recurring paid refreshes.

The first canonical snapshot at **15:36:24.434 UTC** preserves all 99 jobs, 93 original nonnull result bytes and every manual retry/attempt/photo/scope/source/refresh prefix. There are 98 eligible individual cards, 11 ineligible shared-bulk units, 92 legacy unknown results, four failed and two queued jobs; 1,500 historical candidates, zero selected/estimated and **no v6 result or appended attempt yet**. This is initial preservation, not completed recovery. Root will record subsequent natural-worker outcomes. Original 98-card and historical 92-card cohorts remain distinct.

**Latest canonical checkpoint — September 22, 15:48:10.922 UTC:** 98 eligible cards include nine current v6 results with 77 candidates, 50 assessed comparisons bound to retained image hashes, two matched assessments, zero selected comps and zero estimates. Eight of the nine results retain comparisons; two record an initial comparison failure and one an optional comparison failure. Failure markers can overlap retained comparisons: a completed job means a result was persisted, not that every model assessment succeeded. Thirty-six jobs now have the v2 recovery revision and 36 automatic refresh entries have been appended. Current statuses are 28 queued, one running, four pre-existing failed and 65 complete, including legacy results. All 99 original inputs and history prefixes and all 93 original result payloads remain preserved (84 current, nine in attempt history), with zero stale bindings or integrity failures. This is a dated, incomplete checkpoint; matched labels alone do not establish selected value or measured accuracy. See the [recovery record](../plans/2026-09-21-inventory-research-recovery.md) for receipt SHA `aaeb1d3d…` and the original 98-card and historical 92-card cohorts.

Source qualification passed 520 staff tests and 133 focused tests; the unchanged recovery dependency slice passed 26 disposable PostgreSQL cases. That local prerequisite explicitly has no whole-release or hosted-performance conclusion. The real Snivy timing check completed two responses beyond 45 seconds but retains 24 possible, zero matched/selected and unknown value. A duplicate manufacturer observation in the second optional answer failed strict validation; prior valid findings survived (six assessed, three comparison-failed, fifteen unassessed). This P2 availability limitation remains open. There is no supported Snivy price, fixed-price coverage or all-card accuracy claim.

**Rollback boundary — already crossed:** persisted v2 recovery states and v6 results are present in the 15:48:10.922 UTC checkpoint. A v2 claim can persist `behavior_revision` before paid research or an estimate; old strict readers cannot safely read it. Roll forward with v6-compatible code; do not restore 6GK, reset budgets or rewrite history. A qualified recovery-disabled artifact pauses recovery/automatic refreshes, not all ordinary first research. Environment edits alone do not change serving artifacts.

Current outcome evidence: `20260922-v6-expanded-postpromotion-outcome-preparation/runs/first-expanded-natural-progress/outcome.json` (SHA `aaeb1d3d3567952d26fefb222c5517a6881da1a81fe4eb09fd50df17e6f20dd4`). Release and earlier evidence: `20260922-v6-timing-production-preparation` (metadata `6797af97…`, routes `e7c51549…`); `20260922-v6-timing-postpromotion-outcome-preparation/runs/first-outcome/outcome.json` (SHA `d08fc7d71dc27c1c1acf7a1222766d13a4628ec714836c28a83d9bbb455d53e2`, baseline `c7b81c33…`); `20260921-v6-real-diagnostic-preparation/snivy-timing-independent-v6-analysis.json` (SHA `6a098a975338b841c5a40a350d57fb4d7fa7bdc91a4ad1815bd564e54cb5ea8e`). See the [recovery record](../plans/2026-09-21-inventory-research-recovery.md) for complete receipts, bounds and historical evidence. Next work is observed natural outcomes and the retained optional-assessment limitation; do not rerun a paid diagnostic or reopen accepted phone/website tests without a concrete task.

## Historical September 21 transfer snapshot

The remaining text is retained handoff evidence. Its old Production identities, ATLAS hold, partial/untested status and instructions to finish initial implementation are superseded by the current release above; do not execute those historical next steps as a current plan.

## Where to work

- Authoritative checkout: `/Users/markthomas/tenkings/codex-staff-inventory-release-20260910`, branch `codex/staff-inventory-release-20260910`, pre-handoff HEAD `9f90d50cc0ac5b2cf98dd0725392ec1d493eba1b`.
- Main release sibling: `/Users/markthomas/tenkings/codex-main-site-release-20260916`, sparse branch `codex/main-site-release-20260916`. Use exact Git-connected deployment; never source-upload the sparse checkout.
- Default attached worktree `3a02` is unrelated ATLAS, not Inventory authority. Preserve the untracked `docs/plans/2026-09-10-atlas-staff-intake-reuse-update.md`.
- Required project docs and full approved blueprint still apply. The newest owner correction is appended to that blueprint. Earlier handoff files contain superseded deployment and phone-acceptance states.

## Verified problem

The original 92 current completed jobs have no qualified estimate and no selected comps. They nevertheless retain 1,500 returned listing candidates from earlier searches. Those candidates are not all correct matches or verified prices. Recovery assessed 98 individual cards, but made **zero new sold-search attempts** and produced **zero new estimates**. Its source finder retrieves possible catalog links, not usable checklist facts.

Two different gates are involved:

1. Recovery requires an approved catalog reference and diagnostic before it queues research. With recovery enabled, this also blocks newly queued first searches. See `staffInventoryResearchRecoveryIdentity.ts`, the shared recovery schema, `staffInventoryResearchRecoveryV2.ts`, and `staffInventoryResearchV2.ts`.
2. The research engine/shared result schema separately require published catalog identity before selecting comps for a value. The engine already can fetch and retain candidates without a catalog entry. Do not confuse search eligibility with value eligibility or simply remove every price/match check.

Start with automatic candidate retrieval independent of internal catalog coverage; retain real evidence and clearly label uncertainty. Inspect the separate private matching/value path, sold-price detail integration, and UI handling of failed/partial research. No invented catalog records or seller-title-only identity proof. Preserve wrong-card/printing/grade checks, unknown Best Offer prices, distinct-image evidence, arithmetic mean of selected verified prices, staff corrections, history, bounded work and intake responsiveness. Show actual useful comp outcomes before declaring recovery complete.

## Live state and coordination

Production source `9e8a9c1d99cce0b4b8a4a5dbde241fc916cdb439`, deployment `dpl_6GK1WtpAY19dBQh5csQbZCR6qP8B`; main website and Inventory are live. `STAFF_INVENTORY_RESEARCH_RECOVERY_ENABLED=true`; optional catalog, sale-detail and full-resolution integrations remain off. One existing minute cron owns research. Live DB has 99 active migrations and 13 retained rolled-back entries. No migration or deployment accompanies this handoff.

ATLAS task `01a0c70b-0130-7d70-8d8c-610000b78a06` currently holds shared host/schema/grant/lifecycle mutations during its own cutover. Normal Inventory cron writes and bounded read-only audits are allowed. Root promised no Inventory release during the cutover; wait for its explicit release. Do not deploy ATLAS or edit its checkout.

Private evidence root: `/Users/markthomas/Library/Application Support/TenKingsInventory/investigations`. Current audits are `20260921-recovery-outcome-audit`; deployment tools/receipts are `20260921-recovery-source-fix-release` and `20260921-recovery-source-fix-qualification`. The completed read-only Eddy Curry audit is `20260921-eddy-comp-diagnosis/observed.json`; it verifies this is one of four failed jobs rather than one of the 92 completed jobs. Details are below.

Communicate plainly and briefly. The owner does not need another report of passing tests while cards still have no usable comps. Preserve actual evidence and be clear about what is built, live, and still unproven.

## Transfer to a separate visible lead task — partial local work, not qualified

Mark clarified that the replacement must be a **new task in the sidebar**, not a subagent. That task is created and pinned as identified above. The temporary subagent team stopped at its local write boundary; no feature commit, deployment, restart, schema mutation or provider call was made by this team. Feature base remains the documentation-only commit `436750a7`, followed only by this handoff documentation commit. Preserve the dirty work below, but do not treat it as finished or release-ready. **No tests have run on these partial edits.**

- Main research work is in `packages/shared/src/staffInventoryResearch.ts`, `frontend/nextjs-app/lib/staffInventoryResearch.ts`, and `frontend/nextjs-app/lib/server/staffInventoryResearch.ts`. The shared schema adds explicit `staff-inventory-research-v6` and a separate optional `photo_identity` block with positive observed name/year/set/number/treatment, exact side/hash provenance, and both original sides. Existing catalog identity and v1–v5 bytes retain their meanings. A supported v6 photographic identity may support a private selection without claiming catalog approval. The engine has the new model schema and a `photographedDescription` validator, **but they are not yet wired into its prompt, selection validation, result initialization, comparison/final checks or UI**. This partial helper will likely cause unused-code lint until integrated. Explicit blueprint authority for the private photo-based estimate path, focused adversarial/legacy tests, UI wording, integration and real evidence qualification remain required.
- Recovery work is in `staffInventoryResearchRecoveryIdentity.ts`, `staffInventoryResearchRecoveryContext.ts`, `staffInventoryResearchRecoveryWorker.ts`, `packages/shared/src/staffInventoryResearchRecovery.ts`, and `packages/database/src/staffInventoryResearchRecoveryV2.ts` / `staffInventoryResearchV2.ts`. Draft identity-v2 assessments permit bounded retrieval from distinct original photos plus usable saved/photo-derived name; missing anchors/catalog and description conflicts remain recorded needs, without preventing retrieval. Nonnull saved facts stay unchanged. A persisted behavior revision makes old six-hour-wait assessments due once without resetting paid/manual/history budgets. A shared database eligibility helper now permits bulk-origin exact existing unit IDs only when the current description targets that one physical unit and has distinct photos; regrouped descriptions supersede prior research, and recovery discovery uses the same helper. This was edited but is **entirely untested**, so re-read the diff before continuing. Existing tests explicitly expect catalog blocking and must change; add v1 byte compatibility, v2 warning-ready/context, bulk and revision-backoff PostgreSQL proof. The disposable runner hardcodes 21 tests and must be adjusted if cases are added; stabilize the source before sealing/running it.
- Root's new read-only Eddy audit succeeded at `2026-09-22T03:57:30Z`: private `20260921-eddy-comp-diagnosis/observed.json`. Eddy Curry is a separate failed job, not one of the original 92 completed unknowns. All saved anchors and both photo keys exist: 2001–02 Upper Deck Hardcourt, number 116, Rookie On Court, Basketball. Four attempts of a six-attempt ceiling, one manual retry; latest error `MALFORMED_RESPONSE`, preceding two `PROVIDER_ERROR`. Recovery reports only `MISSING_CATALOG_REFERENCE`, recognition not needed, zero automatic refreshes. A bounded v2 refresh should be possible without touching prior failures. Audit aggregate: 98 current nonsuperseded jobs, 92 complete unknown/4 failed/2 queued; 87 completed jobs retain 1,500 candidates, 294 historically source-eligible, 69 model-matched, zero selected/estimated. This is a bounded row audit, not a new canonical journal replay.
- Provider assessment: existing v5 exact-item detail integration narrowly repairs absent/null Best Offer flags, at most two item IDs across three searches, six seconds each, no retry. It must not turn an accepted offer with unknown actual amount into the advertised price. The 294 older source-eligible candidates lack current affirmative sold-event evidence and cannot be silently promoted into v6. Actual prior Pokémon Snivy v5 diagnostic made two searches/two details/two model calls/nine image reads; one ordinary auction was confirmed at $1.08, one detail timed out, and a separate hydrated accepted offer was eligible at $1.55. Catalog gating prevented selection. Actual sports/Pokémon provider auction positives and negative controls also exist. Recommendation is explicitly enable the already bounded sale-detail flag on a qualified release, preserving off/false semantics. Full-resolution has real larger-image retrieval proof, not model/estimate-quality proof. The provider agent was locating the exact receipts and a fresh diagnostic route when paused.

Next lead should finish and review the local changes, preserve exact legacy/history validation, prove real original-photo-to-eBay outcomes, and coordinate the ATLAS release hold. Root has the final paused-agent reports. There are no active test servers or child processes owned by the temporary implementation lead.
