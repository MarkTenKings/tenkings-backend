# ATLAS completion and acceptance

Owner clarification recorded September 7 Pacific / September 8 UTC, 2026. The canonical V2 blueprint contains the approved ATLAS amendment. This checklist describes the actual intended staff system; a synthetic preview is an intermediate milestone.

## The finished workflow

1. Approved staff sign in with a phone code and receive a separate, revocable ATLAS session.
2. Staff intake a physical card and its Front/Back photos. Preserve original bytes, preparation attempts, measurements and all raw findings.
3. The existing ATLAS grading system performs deterministic preparation, measurements, detection and grading. Astra operates the bounded workflow and proposes identity/finding corrections through scoped tools. It cannot certify, publish, approve learning or perform commercial actions.
4. Human reviewers receive a queue of completed ATLAS reports. Each reviewer can inspect the actual evidence, correct the draft, save and approve the exact final revision. Corrections retain all earlier findings and decisions, and trigger deterministic recalculation/rendering.
5. Approval makes the approved ATLAS report public and marks grading complete. Subsequent changes require a new review/approval; public output must not quietly display an unapproved draft. Stable report links and prior approved versions are retained.
6. The finishing workspace prepares the exact label and NFC job for that card/report. The supported existing code targets the Dell Windows workstation, ACS ACR1552U, FEIJU F8215 and GoToTags Desktop 4.37.0.1. A human performs the current Start Encoding action and physical tag placement. Only verified exact-URL readback and permanent-lock evidence from an approved helper can mark NFC verification complete.
7. Staff print the label and assemble label, NFC tag, card and slab, then sonic-weld it. Record the human finishing confirmation separately from software grading/NFC facts; software cannot independently observe assembly or welding.
8. Complete a fixed ten-card supervised batch. Review actual outputs and failures, measured costs and elapsed time. Expansion follows accepted results.

## Current evidence and remaining work

- Working local staff preview: sample phone-code login, assignments, queue/filter/search, protected Front/Back illustrations, notes, checklist, versioned drafts, conflicts and logout. 36 tests and a separate production-denial build check pass at 2d3e5720. It has no durable or live authority.
- Existing centering/release corrections and offline machine/job contracts were accepted in the earlier integration. Reuse the existing grading formulas and report semantics; do not invent another grader.
- Prepared-evidence implementation d06959be and its complete migration-chain follow-ups are integrated. Lead acceptance passed the combined legacy build, 243 affected integration tests and the exact unchanged 93-migration chain with all 16 Location UUID foreign keys and 18 preparation checks retained. Its approved preparation release is deliberately null pending current worker/storage operational acceptance.
- Durable staff identity/session/challenge, revocation, assignments and immutable saved reviews are implemented in the app-owned `atlas_staff` namespace. Local validation passes 41 unit checks, 22 real PostgreSQL scenarios, both full migration chains with no-op second deploys, and 12 actual web checks including restart recovery. The dedicated Verify transport and exact production activation gate are implemented but inactive; live provider/host/roster acceptance remains.
- Deterministic report rendering and exact human approval storage now work in the dedicated app, with immutable approved versions, a trained-human gate and approved queue state. A separate public app now delivers immutable approved report versions, with its own activation and a database role that can execute only the approved-report read function. Its initial view displays identity, grades and measured findings; public photographs/trace inspection still need the approved-evidence adapter. A private source/evidence/full-review bridge and exact finding corrections are now implemented with a fixed ten-card reservation policy, durable execution claims and atomic analysis commits. Staff controls support remove, restore, type correction and trace editing using the original trace editor/codec. The production preparation release remains unadmitted, so real grading is inactive. Public approved images, Astra orchestration, learning approval, ATLAS NFC/helper protocol, label finishing and operational pilot acceptance remain.
- The existing NFC helper's Ten Kings URL/purpose and permanent-card writer cannot be used as ATLAS authority by substituting a display name. ATLAS needs a separately scoped signed job/public-report binding; preserve all existing Ten Kings paths and data.
- The consumer-facing visual website draft is a separate owner input. It does not block a narrow public reader for approved ATLAS reports.

## Acceptance evidence required

- Durable restart/concurrency/rollback tests, exact phone/service/challenge binding, active-session and old-deployment revocation, host/CSRF/card/role/assignment denial.
- Exact original/prepared bytes, current worker/map/Memory policy, source-bound measurement and complete immutable draft/approval history.
- Bounded jobs and spending, held reservations for unknown provider outcomes, safe reconciliation, no silent repeated paid work or model/worker substitution.
- Human corrections recalculate the draft; only a fresh exact human approval can publish it. Machine and learning authority remain separate.
- Complete declared migration chains with every constraint retained and a second migration deploy that changes nothing.
- Built staff/public/helper artifacts and their exact release configuration; private storage and current worker/workstation acceptance.
- Real ten-card pilot and NFC/print/assembly acceptance with Mark or his designated trained reviewers. Mock tests do not establish physical grading quality or hardware readiness.

No source commit alone authorizes a remote deployment, live migration, provider configuration change or unbounded paid run. Make each operational action concrete after the relevant implementation is reviewable.

## Current continuation — report integration

Canonical grading extraction is committed at 25081a15 (parent durable staff milestone da2a34da). Eight original modules remain byte-identical in the shared core; 191 existing regression tests, five new report/integrity tests, strict TypeScript and both app builds pass. The new content adapter verifies saved grades by recalculation from confirmed quads and measured findings, preserves unreviewed draft findings and explicitly projects approved public findings. It is now wired to immutable staff analysis/approval storage and the reviewer grade summary. Validation now covers 31 real PostgreSQL scenarios, 21 actual paired-app web checks with saved-review and approval recovery after restarting the web process, 41 unit checks, and independent staff/public build-boundary checks. A browser reviewer save/approve/public-view cycle also passes. The only report/training seed used so far is explicitly synthetic; no real card has been approved.

The implemented private bridge reuses a specifically linked preserved Speedster session and full review service through an authenticated ATLAS case adapter. Do not import legacy HTTP handlers or give staff a Ten Kings administrator session. Preserve source `sessionId/createdByUserId`, write exact case/actor/operation bindings, retain all existing worker/map/Memory and checkpoint validation, and append an immutable ATLAS analysis snapshot with the review result. Intake can link a specifically authorized existing captured session for the initial ten-card batch; it must not invent a Ten Kings user, create HOUSE/PACK ownership, complete-label or commercial facts. A purpose-scoped internal server bridge is a temporary extraction option within the approved scoped-adapter boundary; activation remains a separate exact release action.

Report approval requires current trained-human access, assignment and the exact analysis/evidence/review hashes under locks. Store an immutable approved public projection and retain prior approvals. The default public report can select the latest approval; NFC/printed physical output must bind the exact approved version so later edits cannot silently change what a slab's label represents. Publication and physical completion remain independent facts. Unknown/in-flight grading operations must be resolved before a report can be approved.

## Scoped correction bridge acceptance (September 8, 2026)

The source-only private endpoint is `POST /api/internal/atlas/bridge`; staff use their own named grading/evidence operations, never a legacy administrator session. Requests bind the exact active deployment, staff session, assignment, case and evidence. The adapter checks fresh preserved preparation, source owner/revision, current worker policy and all original map/Memory/checkpoint behavior. Initial import requires a specifically authorized captured source with no existing detector result, avoiding an old cached initialization being treated as a new admitted run.

A fourth additive app migration stores independent bridge activation, ten-card budget policy and execution claims. It preserves prior staff and all 93 historical public migrations. The pilot policy names exactly ten present specimens and explicit worst-case operation/card/batch reservations. Mathematical grading policy remains distinct from spending policy. Lost replies and post-dispatch uncertainty cannot trigger a duplicate run; successful results retain reservations until cost evidence is supplied. Corrections atomically reset the human checklist and retain the prior public approval.

Local validation: 43 PostgreSQL scenarios with full unchanged historical migration chain and no-op second deploy; 152 existing review/learning/trace regressions; 51 core/bridge/staff unit checks; four private release-denial unit checks; combined/staff/public builds; 12 staff, seven public and four private-bridge built-production denials with no cookies or provider configuration. The paired actual-web fixture still passes 21 checks including both process restarts. Synthetic fixture results do not establish optical quality or hardware readiness.

### Approved photographs and traces (September 8, 2026)

The public report now includes front/back photographs, enlargement, saved measurement outlines and exact approved bitmap traces. A fifth additive staff migration requires each new approval to bind both immutable image descriptors to its exact evidence and public metadata. Version-pinned public routes return verified bytes/pixels only; no private storage reference, removed finding or trace provenance is sent over HTTP. Production media uses a separate purpose-signed, explicitly activated adapter with dedicated read-only storage credentials; it cannot call the grader. Historical approvals remain immutable, and old approved records without photographs remain readable.

The current database suite passes 47 scenarios, including all earlier staff/approval/bridge cases plus media version retention, corruption/revocation, rollback and approved-only trace delivery. Both migration chains and second-deploy ledgers remain intact. All three builds pass; public output has eight page entries including two GET/HEAD media routes. Current production denials pass 12 staff, 11 public and eight private bridge/media cases. See the session log for final paired-web/browser and source-commit evidence.

Next work: durable Astra machine jobs and provider/cost reconciliation, operator intake/roster/pilot tools, separately scoped NFC/label/physical finishing and trusted learning. The consumer visual draft remains separate. No real card or paid ten-card batch has run.
