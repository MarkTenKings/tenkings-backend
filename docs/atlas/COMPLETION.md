# ATLAS implementation and operational acceptance

Latest deployment evidence — September 10, 18:37 UTC: HEIC/HEIF import is live and ready for owner testing. Public gateway `bfd9bd9b0cce8a5610d5acd839bfebb911eb4d4d` / `dpl_CiougwDQGm57vQKx3v64buPxKFEb` is READY and all four public aliases are verified. Staff remains `857ae45676b5fda8d5f8a910b37aa5fc4e06c95f` / `dpl_GaRq1wZoqAa9EtaUQy6JPUMta46e`. The gateway CSP override is resolved: six live checks confirm exact staff blob-preview/direct-Spaces/WASM-worker policy, unchanged public policy and customer nonce policy, and common headers. Twenty-six anonymous hosted checks passed for pages, redirect, license, converter, worker and WASM. Both actual 3024×4032 Display P3 HEIC photos passed complete local UI conversion/upload/reload/original-download tests; 335 staff tests, five public tests and all14 PR370/main checks passed. Chrome was refreshed to staff sign-in. Authenticated production photo-pair acceptance remains owner testing; no production user-photo upload, OTP or model request was sent by root. StaffControl8/workspace2/source2/STAFF SMS6, all26 staff/six public environment records, private C, CPU A2, customer, pilot and existing rows/holds remain unchanged. All five generated roots are restored. Final receipt `heic-20260910/release/gateway-fix/FINAL_ACCEPTANCE.json` SHA256 `7abd8ea148fc69a9b0c47075d7b45abac64aa92d6baacb94a512433759918aaf`. Older C-only paragraphs below describe the historical13:45 checkpoint; SESSION_LOG records the subsequent exact releases.

Updated September 10, 2026. The owner-approved ATLAS amendment in [the canonical V2 blueprint](../specs/TEN_KINGS_V2_FINAL_MASTER_BLUEPRINT.md) governs scope. Runtime, database and source evidence determine what is implemented. Historical checkpoints remain in [SESSION_LOG](../handoffs/SESSION_LOG.md); older test counts do not describe the final candidate.

Mark selected his **MacBook for grading and NFC finishing**, keeping the same ACR1552U reader and F8215 tags. The new target is automatic write/readback/lock inside ATLAS after human report approval and physical card selection. The implemented finishing entry below describes the existing Windows integration; the direct Mac writer and automatic queue are additional unfinished work. [MAC_NFC.md](MAC_NFC.md) records the research, native probe and remaining implementation.

September 10 deployment update: merged C `89478881` passed all 14 PR/main checks and all 39 private tests. Staff/public C are live with their 26/6 environment bindings root accepted. Signed private C is running, passed health verification and all 15 private HTTPS/denial checks, and its route preserves the existing Caddy container. CPU A2, 95 public/22 staff migrations and three scoped roles are accepted and unchanged. B is stopped and retained under its archival name. StaffControl7/workspace1/source1/STAFF SMS5 were committed and verified by read-only reconciliation, without SQL replay. All four public aliases and the production target are verified on C; public/admin/account HTTPS checks and the www redirect pass. Mark’s screenshot and root’s authenticated Chrome read verify that the live C staff queue and Add Cards page open; real-photo workflow acceptance remains pending. Real-photo grading and physical finishing remain pending. [WEBSITE_RELEASE.md](WEBSITE_RELEASE.md) records exact bindings and receipts; earlier package/test totals below remain historical.

## Implemented components

The deployed intake is live, while a complete grading workflow still requires actual browser and real-card acceptance. Source C includes Add Cards, recoverable paired-photo uploads, manual original-source actions and expanded original-photo/report Astra tools. The private service and saved-run dispatcher have verified production packaging, bindings, runtime and HTTPS evidence. All four public aliases serve the accepted C release. All ten pilot cards will use fresh photos. [GRADING_WORKSPACE.md](GRADING_WORKSPACE.md) records current source evidence and required first-card acceptance before the full ten-card run.

The owner further requires a human-populated **Waiting to grade** queue at the start, shared by human and Astra operators, and a separate **Human review** queue at the end. Batch photo intake, saved pairing/readiness, exclusive claims, activity and pause/STEP/takeover are implemented in deployed staff C and have synthetic browser/database evidence. The intake controls are verified and active; claims, preparation and Astra dispatch remain inactive and require separate processing admission. Humans can load all ten cards up front through the live intake; first-card validation concerns processing order, not a restriction on filling the queue.

1. **Staff access:** dedicated phone-code authentication, separate revocable ATLAS sessions, owner-managed roster, assignments and current human permissions. Fresh sign-in can open in another tab without discarding the current workspace.
2. **Intake and preparation:** operations staff link an explicitly authorized preserved capture and exact Front/Back bytes. A separately signed human admission creates one supervised initialization job. Its execution uses current original preparation/map/Memory authority, fresh detection and the original grading engine, then creates one bounded Astra run. Historical initialization/checkpoints cannot substitute for that fresh run.
3. **Astra review:** exact report/evidence/crop adapters, immutable proposals, bounded provider requests, fenced jobs and receipts, shared worker/model cost reservations and atomic submission to the human queue. Astra cannot approve a report, publish, approve learning, control money or perform physical finishing.
4. **Human correction and approval:** original deterministic finding/trace corrections and explicit proposal decisions; structured identity correction. Only changes preserving original category/layout/map keys may retain the analysis. Other identity changes return a reprocess advisory without saving or starting a job. Corrections preserve raw findings, calculations and notes, reset the review checklist, and retain prior public approvals. Exact trained-human approval publishes an immutable report version and its exact photographs/traces.
5. **Finishing:** immutable version-bound label copies and QR, separate signed ATLAS NFC jobs, exact hosted verification receipts, and separate human assembly/weld confirmations. Each label face is 2.73 × 0.83 inches with the approved NFC reserve. Physical facts are never inferred from software grading or a printed preview.
6. **Trusted learning:** independently authorized fresh reviewers select the exact candidate subset and record an immutable approval or rejection with a reason. Approved decisions remain `APPROVED_PENDING_APPLICATION`. This workflow does not write to the global learning bank; report certification grants no learning authority.
7. **Operations and recovery:** intake, roster/assignment, exact ten-member pilot preparation, cost inspection/invoice reconciliation, cancellation of never-dispatched initialization, and explicit abandonment of uncertain work. Abandonment retains charges, receipts and budget holds and prevents silent same-evidence reenqueuing.

The existing Windows NFC helper retains the Ten Kings paths. ATLAS uses distinct job purpose, server trust, workstation token and signed results. Optional NFC configuration is disabled by default. Installer/updater/start-script wiring applies only to a Windows NFC workstation. The selected MacBook path requires the first-party native adapter and hardware qualification now being developed; the old helper is retained for historical Ten Kings compatibility.

## Local verification

- The retained workspace checkpoint passes193 complete PostgreSQL scenarios with owned-cluster cleanup verified across95 public/22 staff migrations. The three added actual-SQL scenarios validate corrupt-upload rejection/replay/replacement, immutable history and revoked/stale access. Its combined core/operator/bridge/private/staff tests passed606 and full Speedster/source/adversarial tests passed821; normal staff and Node22 legacy Next release builds passed. C's later startup-order/cleanup change separately passes39 private tests and all14 PR/main checks. Exact receipts are recorded in [SESSION_LOG](../handoffs/SESSION_LOG.md). Actual Chrome fixture evidence covers ten fresh synthetic pairs, recovery and explicit human map-failure decisions. Local CPU service11/original preparation and Color geometry19 groups also pass. These fixture counts do not establish real-card acceptance.
- All **128 actual PostgreSQL scenarios** passed on the complete **93 original public + 13 additive staff** migration chains; both second deployments were no-ops. Committed historical migrations are byte-identical. This includes actual restricted-role identity-correction success, lost-reply readback, disabled/rotated replay, rollback, revocation and original numerical evidence preservation.
- Paired staff/public development processes passed **55 HTTP checks**, including both actual process restarts, immutable approved media/labels, retained decisions, fresh sign-in and inactive supervised controls.
- All three app builds, the locked Sharp runtime verifier, **495 JavaScript/TypeScript tests** and **70 built-production denial checks** pass. Exact suite counts and evidence are in [LOCAL_ACCEPTANCE.md](LOCAL_ACCEPTANCE.md).
- Browser verification covers desktop/mobile report, label, operations and correction layout; retained label recovery after separate-tab sign-in; preserved Back/Forward history; and bounded parent draft recovery. Browser/handler tests cannot establish actual card identity quality or optical/physical performance.
- The existing NFC helper compiled with .NET 8: **18 tests passed, eight Windows-specific cases skipped**. Five Windows-configuration source fixtures passed. The PowerShell runtime suite, Windows signing/PCSC/GoToTags cases, real encoding/locking and physical printing remain workstation acceptance.
- The closed operator artifact packages actual locked Node/Prisma/engine/source bytes. The earlier reproducible Darwin arm64 Node22.23.2 artifacts remain evidence for that platform. The Linux x64 Node20.20.1/Prisma5.22 Debian/OpenSSL3 closure checkpoint verified 100 source inputs and imported its runtime/client/native engine in the pinned full Bookworm image with no network and a read-only artifact. Those earlier probe bindings were synthetic and inactive. The separately bound C artifact/manifest is now mounted read-only in the accepted private host; host acceptance does not admit a paid operator run.

## Remaining operational acceptance

CPU A2 and private C are admitted and serving. Intake controls are verified and active; claims, preparation, Astra dispatch and production NFC remain inactive. All four public aliases and the production target serve C, and public/admin/account HTTPS and the www redirect pass. StaffControl is revision7; customer binding is unchanged. The September 9 customer SMS and September 10 00:06:06 UTC staff-session readbacks are historical c5ba35fb acceptance, not C session evidence. The Mac is unlocked and the authenticated C staff portal opens; hosted real-photo workflow acceptance remains pending. The owner's reviewer certification remains null. [Website release status](WEBSITE_RELEASE.md) records current bindings and accepted private/public checks. No real-card Astra evaluation, paid model/worker request, real print, production NFC finishing, assembly or weld acceptance is claimed. The successful Mac diagnostic write/readback and phone scan remain separate from permanent-lock qualification.

Mark's September9 website request is implemented and deployed: `/admin` mounts the private staff app; `/account` provides phone-code accounts, deferred profiles, submission snapshots, dealer drop-off/mail-in and a per-card tracker connected to restricted staff intake. Its historical release checkpoint passed155 PostgreSQL scenarios,13 connected synthetic HTTP groups and39 configured-disabled hosted checks. The latter preceded exact login/SMS activation. See the [website release](WEBSITE_RELEASE.md) and [deployment procedure](WEBSITE_DEPLOYMENT.md) for current evidence. Mark prioritizes staff grading/Astra testing before further submission iteration. Dedicated storage, funded RunPod startup, OpenAI project/key access and C service bindings are verified; complete real-photo workflow acceptance, exact ten-card admission, measured grading/review/public-report acceptance and complete physical finishing remain. The ten-card grading pilot can precede qualified finishing, but both are required for full acceptance. [OPERATOR.md](OPERATOR.md) records176 synthetic operator tests; live Astra quality, cost and latency remain unmeasured.

The intake transaction is reconciled without replay and the public promotion is verified. Next, unlock the Mac and verify the existing Chrome staff flow against the accepted C hosts. Require ten fresh verified Front/Back pairs and a fresh cost/hold review before the first processed card; initial processing remains limited to one distinct card. Physical NFC acceptance targets the MacBook with the unchanged reader/tags; the new native writer, protected signer and integrated queue must be completed and verified before the finishing pilot. Windows helper configuration is historical integration guidance, not the selected ATLAS setup. This document records acceptance boundaries and does not itself authorize a live action.

The final supervised acceptance batch is **exactly ten real cards**. Record their actual physical identities/captures and trained-reviewer authority while preserving the existing pilot identity, time window and caps. Mark authorized$100 total for SMS/image/Astra, with at most$10 reserved for the current owner-only SMS pilot and at most$90 for later admitted worker/Astra work. Record measured quality, failures, elapsed time, invoices, exact labels/QR/NFC readback and human assembly/weld confirmation. Expansion follows owner acceptance. Broader launch also needs actual dealer/mailing instructions, pricing/service commitments; the original public marketing presentation is restored. Payment/carrier integrations are not implied by the current pilot.

Current runtime details: [OPERATOR.md](OPERATOR.md), [operator release instructions](../../packages/atlas-operator/README.runtime.md), [Windows configuration](../../scripts/ai-grader-nfc/ATLAS_CONFIGURATION.md). The private local release record names evidence and remaining deployment inputs without publishing credentials.
