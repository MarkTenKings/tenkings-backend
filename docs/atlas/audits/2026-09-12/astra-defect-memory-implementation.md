# ATLAS reviewed memory and Astra defect assistance

September 12 Pacific / September 13 UTC, 2026. Implemented locally under Mark's
ATLAS-only instruction using multiple `gpt-6-astra` / `xhigh` specialists and an
independent Astra review. This is a source and synthetic qualification record.

## Completed source

The paired manual workspace has a human-requested Astra analysis action, separate
blue unreviewed proposals, visible uncertainty and image limitations, and explicit
accept/correct/reject controls. Correction uses the existing trace editor.
Acceptance and correction save an exact trace before the unchanged CPU measurement
path. Model areas, grades and approval claims cannot become measured findings.
Manual grading remains available during an uncertain analysis result.

The existing deliberate **Confirm findings** action after both sides are inspected
is the durable reviewed-memory publication intent. The authenticated original
confirmer must retain REVIEWER and card edit authority. Final report certification
remains separate. A publication failure preserves the committed review and exposes
retry of that same confirmation, without a second findings approval.

The new memory package retains immutable crop/trace/source and human/proposal
provenance for accepted, corrected, rejected and human-added findings. Later edits
to an accepted outline are correctly published as corrections. Later confirmations
supersede earlier lessons immediately, including an empty reviewed list. Historical
publications remain immutable. Current status distinguishes edited findings from
the prior acknowledged publication.

Each new analysis freshly retrieves relevant published examples with a version and
selection hash. It excludes the target card and identical original photos; design
matching is conservative and negative design examples require an exact known card
number. A pending relevant publication prevents silently using an older lesson set.
These are retrieved examples supplied with the request, not online model-weight
training or proof of detection improvement.

The adapter uses exact `gpt-6-astra` / `xhigh`, both prepared inspection images,
overlapping crops at unchanged pixel resolution and at most12 reviewed examples.
Each example includes its original crop and a separately labeled overlay of the
exact reviewed mask. Original WebP source hashes and PNG encoding hashes remain
distinct and checked. Bounded structured output contains only advisory taxonomy,
simple outlines and observations. No SAM3 dependency was introduced.

The browser journals an exact request before sending it. The server retains exact
request bytes outside SQL and claims dispatch once. Raw replies and actual usage
are retained before interpretation; uncertainty never causes a paid redispatch.
Undispatched stale/expired requests can be durably retired under the same card lock
used by prepare/claim. A retired command cannot dispatch later. Dispatched requests
cannot be retired this way. Identity, source and physical-frame changes stale the
suggestions; later finding/review revisions still allow deliberate sequential review.

Code is in `packages/atlas-defect-memory`, `packages/atlas-defect-analysis`, the
connected-manual composition, manual workflow and dedicated staff page. The two
ordered staff migrations are additive proposals, byte-identical to their package
SQL. The runtime flags are `ATLAS_MANUAL_DEFECT_MEMORY_ENABLED` and
`ATLAS_MANUAL_DEFECT_ANALYSIS_ENABLED`; analysis also requires the existing
server-only OpenAI key and the new restricted grants. Startup never applies SQL.

## Verified evidence

- Final integrated Node20.20.1 suite: **163/163**, no skips. Includes actual compiled
  JSX/parent components, browser command races, workflow/service actions, exact
  Sharp pixel crops/overlays, provider bounds and receipt recovery.
- Final owned PostgreSQL chain: **95 public /40 staff migrations**, both replayed
  with no changes, **16 assertion groups**. Includes memory publication/retrieval,
  original-confirmer/edit/observer authority, revocation, restart, supersession,
  concurrent dispatch and prepare/claim/retirement races.
- Expanded manual-role qualification: **2,937 columns /13 tables**, exact grant
  replay, restricted function privileges and ownership checks.
- Existing connected HTTP/auth/storage-fixture/actual CPU workflow: **eight
  assertion groups passed**. New provider responses remain synthetic.
- Independent source review: PASS, with four retained regressions. Its discovered
  outline-classification, identity-staleness and expired-request issues were fixed.
- All owned test clusters were stopped and their regenerable data removed, with
  cleanup receipts retained.

Evidence root:
`/Users/markthomas/.codex/atlas-handoffs/atlas-connected-manual-20260912/release-readiness/astra-defects`.
Independent review:
`release-readiness/review/astra-integration-source-review.json`.

## Remaining release and acceptance work

The already authorized sealed storage canary ran once with unchanged helper and
SDK versions. Spaces accepted an incorrect checksum with HTTP200. The canary
stopped `FAILED_SAFE / CHECKSUM_REFUSAL_NOT_PROVEN`, deleted its sole65-byte object
and verified authenticated HEAD and GET404. It performed12 requests, one PUT and
one DELETE. The later collision/private-read qualification steps were not reached.
CORS preflights for the recorded staff origin passed. Preserve the exclusive
execution intent; do not rerun that sealed canary. Resolve actual integrity and
conditional-write behavior before admitting real grading photos.

Docker Desktop remained unresponsive after Resume and a requested preserving
restart; successful daemon recovery was not observed. The earlier qualified Linux
image does **not** contain these new features. A new exact-source Linux/staff build,
required release checks, coordinated migration/grants/private-service/web release,
and real standard sports/Pokémon acceptance remain outstanding. No production
migration, deployment, real-card upload or live defect-model call occurred here.

Measure real defects, outlines, correction effort, latency/cost and next-card
improvement with human-reviewed, held-out examples before claiming quality.
The full autonomous replacement-operator initiating-cause prerequisite remains
unresolved. SAM3 evaluation remains deferred to measured gaps under Mark's direction.
