# Connected native-photo manual workflow

September12,2026. Implemented on baseline
`fd91bbb8a681e819af82f299576dc2f7cae5c99b`. Mark approved the SDR working-view
recommendation and requested fresh Astra Extra High agents to build the connected
milestone. Two fresh gpt-6-astra/xhigh specialists completed native photo/intake,
then bounded review and integration tasks. A third fresh spawn was refused by
the session agent limit; the lead handled application/runtime composition.

The connected source, final container packaging and core runtime are qualified
locally as described below. Disk exhaustion was resolved through authorized
cleanup and Docker recovery. This is not a production release. The three additive SQL
proposals remain inactive outside owned fixtures. The previously observed
serving-I failure and unresolved initiating-cause prerequisite for a replacement
Astra grading operator remain unchanged. No paid identification/model call,
production storage write, live migration, remote restart or deployment occurred.

## What is connected

The actual dedicated staff app now has a feature-gated manual card list and
native Front/Back intake, automatic shared-engine eight-field suggestions,
durable corrections, paired geometry/defect tools and separate exact report
approval. The staff app remains on the approved Vercel `/admin` architecture;
new native work runs in a separate private Linux service. The new card routes
carry signed method/path/body/cookie/CSRF/origin/timestamp/nonce evidence and
reauthenticate ordinary staff access on the private side. The web artifact
contains only the new signed transport and browser clients, not native CPU code.

Native upload plans are independent per side/version. The browser retains the
exact Blob and upload identity across interrupted transfers; completed originals
are verified before preparation. Definite pre-plan refusals can be discarded
without abandoning an uncertain grant, verified local copies can be cleared,
and original size is checked before reading/hashing. There is no product card
allowance or old operator cohort dependency in the new manual intake.

Each original remains immutable alongside a rich decoded primary and separate
full-resolution sRGB RGB8 SDR working PNG. Apple's qualified P3 HDR-gain-map
container is admitted only with the explicit owner-approved SDR-base policy.
The original still truthfully records HDR data; gain-map rendering is not claimed.
No spatial resampling occurs in SDR conversion. Preparation and grading retain
the existing card/inspection grids and scoring. Physical optical acceptance is
still a human task.

Shared identification preserves the extracted Ten Kings prompts, field meanings
and request/model settings. One unique claim per current photo pair gates two
OCR requests and one identifier request. Exact immutable request/reply artifacts,
usage and uncertain dispatches are retained. Suggestions cannot overwrite touched
human fields. A narrow SQL receipt append can preserve a matching late reply after
staff expiry without authorizing result adoption, a new request or report approval.
Post-initialization identity edits belong to the manual report workflow.

CPU/storage operations run outside short per-card transactions. Source and detail
checks are repeated at actual commit. Front replacement preserves exact unchanged
Back geometry, findings, inspection and preparation. An approval racing a source
replacement cannot commit; earlier approved snapshots remain immutable history.
Saved defect candidates survive measurement failure, and recovery reuses the
exact pending action. Both sides must be inspected before the single Confirm
findings action; final trained-human approval is separate.

Full-resolution image bytes bypass the Vercel response cap through short private
object grants. Before review display, the browser checks streamed size, optional
expected byte count and SHA-256, then creates a Blob without reencoding. Expiring
URLs are excluded from pixel lineage, so renewal does not itself erase edits.
Stale fetches are cancelled and replaced/unmounted Blobs are revoked.

## Qualification evidence

Evidence root:
`/Users/markthomas/.codex/atlas-handoffs/atlas-connected-manual-20260912/`.

- 486 combined local application/intake/service/workflow/transport tests pass,
  including the ordinary auth boundary and actual Next empty-body parser format.
- 107 photo/core tests and47 workspace tests pass in specialist qualification.
  The24 storage tests include actual streamed-byte checks and bounded signed reads.
- Native PostgreSQL fixtures replay95 public/35 staff migrations and the three
  proposals, with second-deploy no-op and restricted role grants. Intake passes10
  grouped assertions, connected composition8, plus independent late-receipt,
  session-expiry, replay and source-race probes. All specialist-owned clusters
  are stopped with retained cleanup receipts.
- The corrected Node20.20.2 Linux/amd64 image passes215 runtime checks, covering
  native HEVC/SDR, CPU preparation/measurement, verified browser-image logic,
  signed HTTP transport and web/private configuration. It runs under local Docker
  emulation on the Mac; this is not a readback from the eventual production host.
- The final refreshed candidate passes all 251 expanded Linux tests with no
  failures or skips, including intake, connected composition, durable service,
  workflow recovery and the corrected worker-cancellation handshake. Its image is
  `atlas-connected-manual:final-verified-20260912`, digest
  `sha256:b6eaf704a8241afd470d9da81b4402cbd8f16184ecd6c608c99b5396f6ae48f1`.
  All 497 source files inside it match final manifest SHA256
  `b4f41db57f95ad1177f46f036e6a259f4e700c7599e669f356d4d1164e02e8bf`.
  Its staff artifact boundary passes with 26 browser chunks, 12 server traces,
  2,058 checked trace entries and the generated Debian Prisma engine.
- Both retained iPhone originals decode at3024×4032 in Linux. Every primary and
  SDR RGB sample equals the independently checked Mac reference pixels exactly.
  Original SHA-256/bytes remain unchanged. The Mac primary also matches native
  heif-dec output; its independent ICC matrix/TRC oracle has at most one RGB8
  channel-code difference on211,437 sampled pixels per side.

Earlier failure evidence is retained. Next initially bundled native modules into
the Pages router; the final web composition uses a pure signed proxy and checked
artifact boundary. Browser testing identified Next's empty parsed GET object and
empty text string; safe normalization now requires absent/zero framing and no raw
or unread body. The first Linux image compiled but its isolated decoder could not
resolve transitive libde265 without loader environment variables. Registering the
reviewed native libraries with ldconfig corrected this while preserving decoder
environment isolation. Test-only extraction references are read-only mounts and
never serving imports.

The actual Next browser flow passes eleven grouped scenarios through the real signed
private HTTPS boundary and direct image grants: native uploads, synthetic shared
identification, durable identity correction, input freezing during an uncertain
save, physical/printed edits and CPU re-preparation, failed-grant recovery with an
unsaved Back outline, measured human trace, both inspections/one confirmation,
separate report approval with lost-response recovery, and mobile/navigation
readback. Only one approval dispatch occurs in the interrupted-response case.
A subsequent independent review found and corrected delayed navigation losing a
new edit. The final eleven-scenario full browser pass proves that an actual trace
started during a delayed image-grant read remains on screen and unsaved after the
read completes. This final run also includes the exact-command recovery fix.

Final recovery review also found overlapping React recovery clients could clear
a later command's journal. Exact serialized-command comparison now guards every
clear, and dispatch snapshots the original payload. Four dedicated race tests
plus the existing client tests pass. No fixture confirmation is represented as
Mark's approval.

The fourth container build failed when the Mac reached111MiB free. Buildkit
reported an input/output error, and the owned browser PostgreSQL fixture also
stopped after a disk-full PANIC. Lead cleared only this task's regenerable Next
cache and restored only that disposable cluster, then used a fresh owned fixture for the
final full browser pass after the crashed fixture still failed its staff health. The
successful third candidate build/runtime evidence has its original source
manifest; it must not be described as the final UI/client race-fix image.

The owner subsequently authorized easy cleanup. Removing installed-app installer
copies and unused download caches freed 5.864 GB. Docker still required recovery;
its normal restart timed out, and one stale backend remained after controlled
shutdown. After verifying that the VM disk had no open handles, stopping that
stale process and reopening Desktop restored the engine without removing images
or volumes. The recovered inventory contained no containers or local volumes.

The exact final serving/native/web image then built successfully from the checked
497-file closure. Both retained photos again passed full-resolution primary and
SDR pixel equality against the Mac reference. Expanded Linux verification exposed
a cancellation-test PID-file race: transient empty content was interpreted as
PID0. An instrumented fixture reproduced that exact false failure. Fresh Astra
Extra High review confirmed the issue and reviewed the test-only correction:
atomic PID publication, a separate cancellation file, explicit positive/live
second-child readiness and guaranteed cancellation cleanup. Production worker
code did not change. A one-file test overlay on the successful full image avoids
recompiling unchanged dependencies on the nearly full Mac. Both manifests and
the exact delta are retained. Full exact-head release checks and actual
target-host/provider qualification remain pending release work.

Final evidence is `linux-runtime/tests-final-corrected.log`,
`linux-runtime/final-source-and-boundary.log`,
`linux-runtime/retained-photos-final.json`, `linux-verified-image.json`, and
`linux-final-test-overlay.json` under the evidence root. The retained-photo proof
ran on the full-build parent; the only subsequent image/source change is the
reviewed measurement-test correction. Original failed and diagnostic runs remain
available alongside successful final evidence.

## Remaining release and product work

The concrete [candidate release procedure](../../runbooks/CONNECTED_MANUAL_RELEASE.md)
records the source closure, private/web auth bindings, separate manual role,
private storage/CORS, exact deployment checks, rollback and fresh-card acceptance.
Actual provider conditional-write/read semantics, target-host startup and matched
Vercel/private activation still require release qualification. The deploy runbook
requires separate authorization for its live storage canary. No current live DB
migration count, active deployment binding or reviewer certification is inferred
from disposable fixtures.

Fresh standard sports/Pokémon photos need the owner's physical detail/color and
workflow acceptance. Automatic defect assistance, learned corrections, variation/
comps research, public report publication, Mac NFC/label/finishing and the later
Astra grading operator remain separate milestones. This candidate implements the
connected manual workflow and unchanged shared identification, not those later
stages.
