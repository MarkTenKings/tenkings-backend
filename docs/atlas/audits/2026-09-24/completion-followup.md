# ATLAS completion follow-up — September 24 UTC / September 23 Pacific

> Historical checkpoint: this file retains earlier release/pilot evidence. Current deployed source is e29567c2, and the new customer kiosk/mail-in requirements are recorded in the [fresh Ultra lead handoff](../../handoffs/2026-09-24-fresh-ultra-lead.md). Older release identities, caps, manual Start instructions and missing-research claims are superseded where that handoff says so. Do not replay completed activation instructions.

The successor software release is **live at application source `1b880d0690dd0b3a64a76103557815cf40675029`** on staff/public and the private native service. Batch, presentation and SoldComps are enabled; the physical station remains disabled. Mark’s owner-authorized internal reviewer permission is active for 90 days. This supersedes the [September 23 release](../2026-09-23/workflow-completion.md). Real-card human acceptance, printing and permanent NFC locking are still incomplete. All deployment/control/promotion/reviewer intents described here are consumed and must not be replayed.

Mark requested four fresh Astra Extra High specialists, one for each remaining workstream. All four completed their assigned local implementation pass; a fifth fresh specialist researched printers at his request. Root integrated and reviewed the changes. The approved blueprint and label artwork remain unchanged.

## Implemented corrections

- Manual final-report preview projects current reviewer eligibility outside immutable report bytes. The UI prevents approval/printing before eligibility and verified-image checks, explains an expired authorization and supports a read-only refresh. The server still checks actual authority at commit.
- Batch intake ignores delayed responses from a previous staff identity. After a legitimate access-version change, the same owner can explicitly resume saved jobs against exact current photos and revision. Active leases cannot be taken; saved review reports and partial review intents do not go through machine analysis again.
- ATLAS requests one SoldComps page with a40-result ceiling and rejects HTTP redirects. Definitive authentication/quota/rate refusals remain distinguishable from uncertain transport failures; saved reasons survive reconciliation and appear in staff guidance. Ten Kings's shared default query count, parsing/ranking and grading semantics are preserved.
- Mac distribution has a normal command-line workflow for exact Developer ID signing, final-byte manifests, signed DMG, one-time notarization submission, reconciliation, stapling and Gatekeeper checks. Signing does not qualify hardware or create a station identity.
- The offline real-card evidence reader accepts unaltered PostgreSQL timestamps with known timezone offsets while rejecting impossible dates and ambiguous times. It still cannot determine real physical acceptance.

## Validation

Root's integrated affected-package run passed **350/350, zero skips**, including actual pinned Python/native measurement. The complete staff suite passed **571/571, zero skips**. Shared SoldComps tests passed26/26. A new ten-card worker/repository/auth lifecycle on owned disposable PostgreSQL and the existing fifty-card repository suite passed; the new lifecycle is now included in the existing disposable PostgreSQL CI job. These synthetic specimens establish recovery behavior, not real grading accuracy or throughput.

Independent review reported no actionable P1/P2 issues in the new Mac distribution and batch repository changes;24 focused tests passed. The actual unsigned Mac review bundle verified23 files and byte-identical PDFs for both supported layouts. Production Developer ID/notarization has not run. No historical migration or new grant is required by these source changes.

## Actual provider qualification

Mark found his original SoldComps key in Notes and entered it through the hidden local receiver. Root verified protected-file custody without displaying the value. The existing Ten Kings key and deployment configuration were not rotated or changed.

At `2026-09-24T00:40:18.485Z`, one bounded lookup using the existing saved Jamal Murray draft identity returned HTTP200,13 normalized candidates and17,343ms elapsed. The response reported665 of10,000 account requests used. There was no retry, pagination, hydration or publication. Fresh DB readback has zero manual approvals, so this proves authentication/transport/parser behavior only. Approved-report search, selection and public presentation still need a genuine human-approved report.

## Shared Inventory architecture

All20 files of the shared recognition V2 package match current Inventory source exactly. ATLAS already uses that engine with its own durable evidence. Current Inventory application20643dea contains newer v6 photo comparison/variation/value research; ATLAS has not adopted that engine or the reviewed catalog service adapter.

The reviewed next adoption boundary is one neutral shared research package with an Inventory compatibility wrapper and an ATLAS-owned subject/photo/effect adapter. Inventory IDs, jobs, storage conventions, company financial records and same-grader valuation rules cannot be imported into ATLAS. Existing scoped catalog discovery/lookup/media/proposals remain the host service boundary; no duplicate catalog schema or broad shared-database grant is needed. Current human-selected report references can operate independently of this later adoption. Shared infrastructure alone does not establish reciprocal learned knowledge.

## Verified live release

| Component | Actual identity |
| --- | --- |
| Application source | `1b880d0690dd0b3a64a76103557815cf40675029` |
| Native image | `sha256:2039ae10cf0ea9c0f04a5216fe5d15d5ce23e631f79adb4d6bf2ae12036e1e27` |
| Native container | `f7949d0161814778bfbe077a43ae92aa7dde906a969f8c9c7e7c608f43ba135e`; started `2026-09-24T01:19:22.450118188Z`, restart0 |
| Staff READY | `dpl_27Dj9ngpLzn7yqw271e6Bo2KyhPJ` / `atlas-grading-staff-6nj7nzb98-ten-kings.vercel.app` |
| Public READY and promoted | `dpl_DjGX2yStocGFYPf8PP1kgALuJtx2` / `atlas-grading-public-cqnjk3k2w-ten-kings.vercel.app` |
| Controls | Staff25 / STAFF SMS23 / PublicReader3; customer7/SMS5 unchanged |
| Configuration | `/opt/atlas/manual-workflow-v4-1b880d06/private.env`; batch/presentation/market=true, station=false |

All four public aliases and production target select the new public release; www→apex308 is preserved. Customer deployment remains `dpl_7GzDxKz2MP36vESc8YkZq87Xht44`. Prior container4f11 is retained stopped/detached as `atlas-manual-connected-20260917-retained-c0a42a24-v4`. Caddy and all other preexisting containers/configurations remain unchanged. No migration or grant was needed: staff47 and public112 ledger entries (99 successful,13 historical rollbacks), all18 existing manual histories, catalog/ACL and unrelated controls are preserved.

Exact-source CI [35939784180](https://github.com/MarkTenKings/tenkings-backend/actions/runs/35939784180) passed all14 jobs. Exact native qualification passed936 Linux package tests with6 explicit Darwin-only skips,547 staff tests and detector tests. The unchanged strict first qualifier stopped on the six skips; that original evidence is retained. Exact-source Mac distribution10/10 covers those six platform-specific contracts. Combined qualification records this split explicitly and makes no actual signing/notarization/hardware claim. Eight direct signed/unsigned HTTPS transport checks and12 public/staff GET checks passed.

The original SoldComps key was installed only in dedicated ATLAS private configuration. The earlier one-request provider proof was not repeated. Approved-report search, selection and public enrichment still need Mark’s genuine report approval. The authorized CenterCourt directory remains contact-only, with no invented offer, program, geocoordinates or commercial terms. Newer Inventory v6 research/shared-learning adoption remains future scoped work, as described above.

## Reviewer authorization and first-card acceptance

Mark explicitly chose **90 days from activation**. At `2026-09-24T01:32:51.855Z`, the owner provisioning transaction changed only the existing reviewer’s authorization expiry to `2026-12-23T01:32:51.855Z` and accessVersion1→2, and appended one truthful NULL-actor owner audit. Expiry is **December22,2026 at5:32:51p.m. America/Los_Angeles**. No external training/certification, HUMAN session, operations grant, trusted-learning authority or report approval was asserted or created. The transaction’s committed receipt and independent read-only receipt matched exactly; post-activation schema/history/control/ACL census remained unchanged apart from its observation time.

This expiry is an internal permission; it is not shown as a customer report expiry and does not invalidate existing issued reports. Mark must sign in again through [ordinary staff access](https://atlasgrading.com/admin?reauthenticate=1). The first actual test is **+ Add card → one fresh original Front/Back pair → Batch grading → Ready to queue → select that card → Grade 1 cards**, followed by Mark’s physical inspection and deliberate approval. No real-card test has yet completed. The disposable ten/fifty-card tests above are synthetic software validation only.

## Direct Mac finishing and remaining physical inputs

Mark explicitly requires NFC operations directly in ATLAS on the Mac, with **no phone or GoToTags operational dependency**. Earlier PC handoffs are specification/history references only; no PC implementation was imported in this follow-up. Consumer phone compatibility may be evaluated separately, but phone use is not an operator acceptance step or station prerequisite.

After Mark connected the ACR1552U and placed a designated unused tag, direct native reads succeeded. The September24 sample has manufacturer byte53, CC E1103E00 and496 advertised data bytes; the earlier sample had manufacturer byte1D. A separate bounded read-only prefix census at `2026-09-24T01:30:28.501171Z` made34 documented READ BINARY calls through page47. It found an empty NDEF at byte16 and no Lock/Memory Control TLVs in176 observed data bytes. The remaining320 data bytes and dynamic-lock region were not read. No raw UID/payload was retained and no write or lock ran. This observation does not establish unused full memory or a qualified permanent-lock map.

Historical product evidence identifies OKAVAD FPC-215-126 / Amazon ASIN B0GX1LSPJ2. Old GoToTags lock indications and its pre-transmission overwrite refusal do not establish native lock commands. The NFC Forum standard supplies a descriptor/default mapping route, but this stock still lacks bound chip/revision semantics and irreversible coverage evidence. No NTAG215 address/mask is inferred from its product name or capacity. The production profile registry remains empty and correctly refuses writes/locks. Historical Mac diagnostic write/full-readback remains valid evidence of basic encoding, not production locking.

Printer research compares Epson P700, xTool O1 and eufyMake E1 under the$5,000 ceiling, with ColorWorks as a later production alternative. Exact substrate/slab fit and print quality/throughput require actual samples. Neither UV model has a verified unattended interface to the existing CUPS/PDF station. No printer is selected or purchased. A legitimate Developer ID identity, final signed/notarized distribution, enrolled station key, exact native lock qualification, printed sample, assembly and ten distinct real-card acceptance remain incomplete. Dealer commercial terms likewise require genuine partner inputs.

## Evidence index

All paths below are relative to the local `readiness/full-completion-v4/` evidence root. They are evidence, never replay instructions.

| Proof | SHA256 |
| --- | --- |
| `release-prep/native-platform-qualification-1b880d06.json` | `cee2fa6316cbc5d1c4a4410e4d851ccbe3284004d85127041c30ff3960ad1159` |
| `release-prep/ci-final-1b880d06.json` | `0c036b96e14b54d99149361f3fd89c3f9810148f301862b9901d1cf40b3910dc` |
| `release-prep/offline-config-1b880d06-1/proof.json` | `c9e25d1e6e6fe02e47b2d96a07bb65d92d763c2a0a547620f86a59107c077c16` |
| `release-prep/controls-evidence-1b880d06-1/result.json` | `5ebe1e309daa27412f0b5834fbb3d3662e3e8cff1aeeb447247b029dfe426564` |
| `tls-postcontrol-1b880d06-1/proof.json` | `7dce8b8c6502abde69dac1d5968c7f7d5f69ad13fa5c165bb894fd6d8cadabe4` |
| `promotion-dispatch-1b880d06-1/result.json` | `f9953512c3fa5ee67a2ac46162f78bbd0b0f7de8b4047fbb92e034967158ab65` |
| `reviewer-activation/production-evidence-1/result.json` | `562a074e7bb939e1fda72ffaecbbc0db42b078ea87d56d007bb705fd1abc6c1f` |
| `release-prep/schema-after-reviewer-1.json` | `fd1e0d1ee44a088425308a7cc56fa4fe5d4e384dc26456b127a4e2dfe87ad713` |
| `step1/post-release/https-smoke-assessment-1.json` | `3dba0b23f8b93bf1220d7349bcf410abd52a12d415d1b84ddab3a3de82c3e5b0` |

Detailed local evidence and printer comparison: `/Users/markthomas/.codex/atlas-handoffs/atlas-workflow-completion-20260923-09df/readiness/full-completion-v4/`. Credential contents remain outside Git and all evidence summaries.
