# ATLAS completion follow-up — September 24 UTC / September 23 Pacific

This is the successor software candidate to the [verified September 23 release](../2026-09-23/workflow-completion.md). At this checkpoint, serving application source remains `c0a42a24`; the fixes below are locally qualified and awaiting the new release. No old deployment, migration, control or promotion action may be replayed.

Mark requested four fresh Astra Extra High specialists, one for each remaining workstream. All four completed their assigned local implementation pass; a fifth fresh specialist researched printers at his request. Root integrated and reviewed the changes. The approved blueprint and label artwork remain unchanged.

## Implemented corrections

- Manual final-report preview projects current reviewer eligibility outside immutable report bytes. The UI prevents approval/printing before eligibility and verified-image checks, explains an expired authorization and supports a read-only refresh. The server still checks actual authority at commit.
- Batch intake ignores delayed responses from a previous staff identity. After a legitimate access-version change, the same owner can explicitly resume saved jobs against exact current photos and revision. Active leases cannot be taken; saved review reports and partial review intents do not go through machine analysis again.
- ATLAS requests one SoldComps page with a40-result ceiling and rejects HTTP redirects. Definitive authentication/quota/rate refusals remain distinguishable from uncertain transport failures; saved reasons survive reconciliation and appear in staff guidance. Ten Kings's shared default query count, parsing/ranking and grading semantics are preserved.
- Mac distribution has a normal command-line workflow for exact Developer ID signing, final-byte manifests, signed DMG, one-time notarization submission, reconciliation, stapling and Gatekeeper checks. Signing does not qualify hardware or create a station identity.
- The offline real-card evidence reader accepts unaltered PostgreSQL timestamps with known timezone offsets while rejecting impossible dates and ambiguous times. It still cannot determine real physical acceptance.

## Validation

Root's integrated affected-package run passed **350/350, zero skips**, including actual pinned Python/native measurement. The complete staff suite passed **571/571, zero skips**. Shared SoldComps tests passed26/26. A new ten-card worker/repository/auth lifecycle on owned disposable PostgreSQL and the existing fifty-card repository suite passed; the new lifecycle is now included in the existing disposable PostgreSQL CI job. These synthetic specimens establish recovery behavior, not real grading accuracy or throughput.

Independent review reported no actionable P1/P2 issues in the new Mac distribution and batch repository changes;24 focused tests passed. The actual unsigned Mac review bundle verified23 files and byte-identical PDFs for both supported layouts. Production Developer ID/notarization has not run. No historical migration or new grant is required by these source changes.

## Actual provider qualification

Mark found his original SoldComps key in Notes and entered it through the hidden local receiver. Root verified protected-file custody without displaying the value. The existing Ten Kings key and deployment configuration were not rotated or changed.

At `2026-09-24T00:40:18.485Z`, one bounded lookup using the existing saved Jamal Murray draft identity returned HTTP200,13 normalized candidates and17,343ms elapsed. The response reported665 of10,000 account requests used. There was no retry, pagination, hydration or publication. Fresh DB readback has zero manual approvals, so this proves authentication/transport/parser behavior only. Approved-report search, selection and public presentation still need a genuine human-approved report.

## Shared Inventory architecture

All20 files of the shared recognition V2 package match current Inventory source exactly. ATLAS already uses that engine with its own durable evidence. Current Inventory application20643dea contains newer v6 photo comparison/variation/value research; ATLAS has not adopted that engine or the reviewed catalog service adapter.

The reviewed next adoption boundary is one neutral shared research package with an Inventory compatibility wrapper and an ATLAS-owned subject/photo/effect adapter. Inventory IDs, jobs, storage conventions, company financial records and same-grader valuation rules cannot be imported into ATLAS. Existing scoped catalog discovery/lookup/media/proposals remain the host service boundary; no duplicate catalog schema or broad shared-database grant is needed. Current human-selected report references can operate independently of this later adoption. Shared infrastructure alone does not establish reciprocal learned knowledge.

## Remaining physical and human inputs

Mark is the first reviewer. The current roster has one active reviewer, no report-approval authorization expiry and zero currently authorized certifiers. Root explained that this is an internal account permission, not customer-visible reviewer expiry; issued reports do not expire when it expires. An actual owner-selected expiry and ordinary human review are still pending.

Mark reports the reader and designated test tags available, but fresh USB and PC/SC checks find no ACS reader attached to this Mac. No new tag APDU or write ran. Exact F8215 lock semantics/qualification and a legitimate Developer ID identity remain unavailable. Existing historical Mac write/readback and phone proof remain valid historical evidence.

Printer research compares P700, xTool O1 and eufyMake E1 under the$5,000 ceiling, with ColorWorks as a production alternative. Thin insert/media/slab fit and actual print quality/throughput require samples. Neither UV model has a verified unattended interface to the existing CUPS/PDF station. No printer is selected or purchased. One real card, ten distinct real cards, actual print/assembly/permanent lock/phone checks and dealer commercial terms remain incomplete.

Detailed local evidence and printer comparison: `/Users/markthomas/.codex/atlas-handoffs/atlas-workflow-completion-20260923-09df/readiness/full-completion-v4/`. Credential contents remain outside Git and all evidence summaries.
