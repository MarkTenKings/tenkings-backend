{
  "name": "tenkings-monorepo",
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "private": true,
  "workspaces": [
    "backend/*",
    "frontend/*",
    "packages/*"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "lint": "echo \"(placeholder) configure ESLint\"",
    "format": "echo \"(placeholder) configure Prettier\"",
    "db:generate": "npm run generate --workspace @tenkings/database",
    "db:migrate": "npm run migrate:dev --workspace @tenkings/database",
    "db:deploy": "npm run migrate:deploy --workspace @tenkings/database",
    "db:push": "npm run db:push --workspace @tenkings/database",
    "vercel:build": "pnpm --filter @tenkings/database run generate && mkdir -p frontend/nextjs-app/node_modules && cp -R packages/database/node_modules/.prisma frontend/nextjs-app/node_modules/.prisma && pnpm --filter @tenkings/database run build && pnpm --filter @tenkings/shared run build && pnpm --filter @tenkings/nextjs-app run build"
  }
}
